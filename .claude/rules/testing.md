---
description: 单元测试、集成测试、端到端测试策略
globs: src/**/*.test.ts, src/**/*.spec.ts
alwaysApply: false
---

## 测试策略

### 单元测试（Vitest）
- 事件 checksum 计算
- JSON Schema 验证函数
- 组卷算法（策略 → 题目列表）
- 评分计算逻辑

### 集成测试
- 完整事件写入 → 投影 → 回放流程
- 红线触发 → 批量熔断 → 状态验证
- 报告生成 → 导出 → 锁定

### 端到端测试（手工验收）
- 教师创建测评 → 学生答题 → 线下评分 → 报告查看
- 情绪中断 → 恢复 → 继续答题
- 安全红线触发 → 会话强制终止 → 安全事件记录
