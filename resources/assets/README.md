# Runtime visual assets

Approved visual assets are copied below this directory using the lowercase `runtime_path` from
`doc/assets/asset-manifest.json`. The files are served through `app://asset/<asset_id>` after the
Manifest projection is written to `asset_resource`.

Planned, generated, composited, reviewing, rejected, and retired files must not be placed here as
shipping assets.
