# Current State
- 任务：DRAG题拖拽渲染组件实现（drag-render）
- 当前 Vibe Coding 步骤：待 /vibe-impl
- 已完成：
  - drag-render Mini-PRD → doc/features/drag-render-prd.md（v0.1）
  - 会话启动/结束文档 HANDOFF 循环机制建立
  - 587 vitest 全通过 | typecheck 通过
- 产出物：
  - doc/features/drag-render-prd.md

# Next Action
- 运行 `/vibe-impl doc/features/drag-render-prd.md` 生成步骤化实现文档

# Blockers
- None

# Key Decisions
- DRAG题激活需先确认 asset_resource 中 10 个资产（9 商品图 + 1 背景图）均为 ACTIVE，再用 sqlite3 CLI 执行 UPDATE
- DragDropQuestion.vue 使用 HTML5 原生 DnD API（Electron 环境需 dragstart 设置 dataTransfer.setData + effectAllowed）
- swap 逻辑：拖入已占用 zone → 浅拷贝整个 Record，同时更新两条映射，单次 emit update:modelValue

# Relevant Files
- doc/features/drag-render-prd.md（PRD 输入）
- src/renderer/src/views/AssessmentView.vue（待修改的父组件）
- src/shared/types/assessment.ts（score 类型需从 0|2 改为 0|1|2）
- src/shared/types/event-payloads.ts（AnswerSubmittedPayload.score 同步修改）
