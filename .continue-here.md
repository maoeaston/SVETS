# Project Direction

- 项目总目标：先把 Pilot R0 做到可追溯、可验收，再继续修订并交付 Full Product 1.0；Pilot 不是项目终点。
- 必须保留的产品决策：唯一产品合同是 `doc/specs/MVP_PRD_v1.0.9-authoritative.md`；当前工程 Schema 基线是 `0.1.18-event-batch-v2.2`；安全红线聚合键为 `student_id + job_code + task_code`。
- 不变边界：不读取、初始化或修改默认数据库；生产 mutation 必须保持 v2 durable command/batch/gate 边界；legacy service 仅可作为无 production 写能力的 planning clone oracle。

# Current Milestone

- M5B Event Batch Runtime 已完成收口。production cutover、自动化验收、针对当前 HEAD 的独立 R3 审查和人工教师/学生/安全红线/可访问性验收均有真实证据。
- M5B-14 production cutover 已完成：target inventory 为 75 channels（29 READ / 46 MUTATION）、36 batch-domain / 10 gate-only，pending 为 0。
- M5B-15 当前为 `PASS`：原生 Electron ABI 迁移、isolated full verifier、target/scope gate、Electron health/login/logout smoke、当前 HEAD 独立 R3 和 isolated userData 人工闭环均已通过。

# Current State

- 任务：M5B-15 已收口；BASE_ABILITY Step 1 已重验，42+8 实施计划已重审。
- 当前 Vibe Coding 步骤：`/vibe-accept step 1` 为 `PASS`；首轮 `/vibe-review impl` 为 `CONDITIONAL_PASS`（已保留）。第二轮 R3 复审（doc/features/base-ability-42plus8-impl-r3-revalidation-review-2.md）结论为 `PASS`（审查者与作者/修复者跨会话独立，证据可复算）。P1-01 与 P1-02 均已关闭；下一原子动作为 BASE_ABILITY Step 3 评估。
- 已完成：提交 `210b3a2` 的命令状态修复，以及 M5B-15 运行时修复（planning clone、assignment projection seed、answer target、aggregate sequence key、renderer optional payload）；完整测试 1,476 项、typecheck、lint（0 error）、build、M5B isolated verifier、target/scope gate、M4/M5B Electron UI、独立 R3 和人工验收均通过。
- Step 1 历史证据：定向 13/13 tests、authority/gate build/check、workbook↔SQL 逐字段对账、typecheck、lint（0 error）和文档索引均实际通过；当时 authority hash 为 `sha256:52c27a71596f7c32e1aab287af40c9c88268ed807bbfdee187a683b4f40d0201`。见 `doc/features/base-ability-42plus8-step1-revalidation-acceptance.md`。
- 已关闭 P1-01：实施计划已改为当前 `v0.1.18-event-batch-v2.2` / `5f050d6` 基线，Step 2A/2B/2C 已明确为 M4/M5A `ACCEPTED`、M5B-15 `PASS` 的前置，生产事件引用已改为 `INV-EVT-001/003/004/005/006`，残余风险不再要求重做 Step 2。`npm run docs:index:check`、`git diff --check`、`npm run typecheck` 均为退出码 0；`npm run lint` 为 0 error、663 条既有 warning；本轮未访问默认运行库。
- 已关闭 P1-02：`buildBaseAbilityAuthority()` 现在在候选重算前强制调用 `assertWorkbookMatchesSql()`；`authority:check` 与复用 authority 的 `gate:check` 均会在 workbook↔SQL 字段不一致时失败。定向测试 15/15 通过，其中复制件 workbook 或 SQL 的 `GA-FM-001.materials` 漂移即使刷新结构化输入 hash 绑定也会被正常 authority check 拒绝。authority/gate build/check 均通过；新 authority hash 为 `sha256:1ebd14451caffcf71a73c409831d068723d7436823f75ebc45910059e313875f`。本轮未访问默认运行库、未改 Schema 或题目状态。
- 产出物：`doc/features/event-batch-v2.2-runtime-m5b15-r3-review.md`、`doc/features/event-batch-v2.2-runtime-validation.md`；M5B-15 source delta 已登记且 M5B-1 至 M5B-14 冻结工件未重写。
- 当前分支：`feat/multi-device-m2-prd`。本轮未访问默认运行库；正式部署或实际运行库迁移仍应遵循受控备份流程。
- `AGENTS.md` 的工程基线摘要已同步为 v0.1.18-event-batch-v2.2（M5B-15 PASS）；当前权威仍以 `doc/specs/baseline.yaml` 与 `src/main/db/schema.sql` 为准。另 `MVP_PRD_v1.0.9-authoritative.md:4553` 仍有 v0.1.17 / Step 2B 的历史阶段文字，不能覆盖当前基线。

# Next Action

- 第二轮 R3 复审已完成（结论 `PASS`，审查者跨会话独立、证据可复算，详见 review-2 记录）。下一原子动作为 BASE_ABILITY Step 3 评估（v1.2 策略/组卷合同/SESSION_STARTED v2 快照链）；未进入 Step 3 实现前，仍不修改 Schema、题库状态、默认运行库或 M5B 冻结工件。

# Goal-mode Handoff: BASE_ABILITY Plan Independent R3 Re-review

## Goal objective

已于 2026-07-31 完成 BASE_ABILITY Step 1 重验（`PASS`）、P1-01 当前基线重建（`PASS`）和 P1-02 来源对账强制门禁（`PASS`）。当前目标是在 v0.1.18/M5B 现场对修订计划进行新的独立 R3 复审；在该复审真实 `PASS` 前不得进入 Step 3。

## Required boundaries

- 不访问、初始化或修改默认数据库；不激活题目，不修改题目 `DRAFT`/`ACTIVE` 状态，不生成激活授权。
- 不改写已接受的 M5B-1 至 M5B-14 冻结工件或 M5B-15 source delta；如发现基线冲突，先标记 `[!]` 并停在文档/审查层。
- 不把历史文档中的旧 Schema、旧 base commit 或“待实现”阶段状态当作当前事实。`doc/specs/baseline.yaml`、`src/main/db/schema.sql` 和真实 Git 状态优先。

## Required reading

先读取 `AGENTS.md`、`doc/会话启动.md`、`.continue-here.md`、`doc/specs/baseline.yaml`、`doc/specs/project-invariants.md`、`doc/ai/vibe-workflow-contract.md`，再读取 `doc/features/base-ability-42plus8-pilot-readiness-impl.md`、`doc/features/base-ability-42plus8-impl-r3-revalidation-review.md`、`scripts/lib/base-ability-42plus8-authority.mjs`、`scripts/lib/base-ability-42plus8-source.mjs`、`scripts/__tests__/base-ability-42plus8-authority.test.mjs` 和 `src/main/db/schema.sql`。

## Execution order

1. 检查分支、工作树和最近提交；不得 reset、restore 或清理用户改动。
2. 独立核验 P1-01：实施计划的当前 Schema/base commit、Step 2A/2B/2C 前置状态、事件不变量和残余风险已与 `baseline.yaml`、`schema.sql`、当前交接对齐；历史基线仅作历史背景。
3. 独立核验 P1-02：`buildBaseAbilityAuthority()` 在 candidate 重算前调用 `assertWorkbookMatchesSql()`；`authority:check` 和 `gate:check` 均经此路径，且 workbook/SQL 字段漂移负向测试使用刷新 hash 绑定后的复制件仍失败关闭。登记 `INV-DATA-001`、`INV-DATA-003` 的影响。
4. 以 `/vibe-review impl` 输出新的独立审查记录；复审如非 `PASS`，只记录问题并停下。复审 `PASS` 后，才把下一原子动作改为 Step 3 评估。

## Completion evidence

已留下 Step 1 实际命令输出、authority/gate hash 与题集统计、无数据库写入证据及实施计划复审记录：`doc/features/base-ability-42plus8-step1-revalidation-acceptance.md`、`doc/features/base-ability-42plus8-impl-r3-revalidation-review.md`。P1-01 已以实施计划引用逐项对齐、`npm run docs:index:check`、`git diff --check`、typecheck 和 lint 0 error 关闭；P1-02 已以 authority/gate build/check、15/15 定向测试（含 workbook/SQL 字段漂移负例）、typecheck 和 lint 0 error 关闭。只有两项 P1 修复后的计划复审真实 `PASS`，才能将下一原子动作改为 Step 3 评估。

## Stop conditions

发现当前基线与计划仍冲突、P1 证据与实际 diff 不一致、出现 P0、或任何命令未实际执行时，停止并如实记录，不得补写推测性通过结论。新的独立复审未完成前不得把计划重审写为 `PASS`。

# Milestone Queue

1. P1-01：重基线 BASE_ABILITY 42+8 实施计划（`PASS`）。
2. P1-02：把 workbook↔SQL 对账接入强制合同门禁并补负向测试（`PASS`）。
3. 对修订计划执行 R3 复审（`PASS`，审查者跨会话独立、证据可复算；详见 review-2 记录）。

# Blockers

- None。BASE_ABILITY Step 3 可开始（第二轮独立 R3 复审已 `PASS`）。M5B-15 无 blocker；真实课堂试测/外部审核/学校授权仍为外部依赖，不阻断工程 Step 3；不得为后续工作改写已接受的 M5B 工件。

# Key Decisions

- 不清理、reset、restore、push、merge 或发布，除非用户再次明确授权。
- M5B scope gate 以不可变 `9a0cbb6` accepted checkpoint 为基线；新增路径必须精确登记，不能使用目录级豁免。
- 对已经开始物理 PREPARE 写入的 append/fsync 异常不得写入 `FAILED`；命令必须保留 `PROCESSING`，由启动恢复以冻结 PREPARE 事实处理。
- 不修改 M5B-1 冻结的 legacy oracle 文件：`reports-service.ts`、`task-closure-service.ts`、`report-service.ts`、`report-reducer.ts`、`report-builders.ts`、`report-contract.ts`。

# Relevant Files

- `doc/features/base-ability-42plus8-pilot-readiness-impl.md`
- `doc/features/base-ability-42plus8-impl-r3-revalidation-review.md`
- `doc/specs/baseline.yaml`
- `src/main/db/schema.sql`
- `doc/specs/project-invariants.md`

# 新会话启动 Prompt

```text
读取 `doc/会话启动.md`、`.continue-here.md`、`AGENTS.md`、`doc/specs/baseline.yaml`、`doc/specs/project-invariants.md` 和 `doc/ai/vibe-workflow-contract.md`，恢复 SVETS 当前现场。

BASE_ABILITY Step 1 已重验为 `PASS`，P1-01 与 P1-02 均已关闭：实施计划现以 `v0.1.18-event-batch-v2.2` / M5B-15 `PASS` 为当前基线，并将 Step 2A/2B/2C 记为已验收前置。`authority:check` 会先执行 workbook↔SQL 逐字段对账，`gate:check` 复用 authority；定向 15/15 测试已证明复制件 workbook 或 SQL 字段漂移即使刷新 hash 绑定也失败关闭。当前 authority hash 为 `sha256:1ebd14451caffcf71a73c409831d068723d7436823f75ebc45910059e313875f`。42+8 实施计划仍为 `CONDITIONAL_PASS`，仅因为修复后的计划尚未经过新的独立 R3 复审；不得重复 Step 1、M5B production cutover 或任何默认数据库操作。

请以 Goal 模式执行 `.continue-here.md` 的 `Goal-mode Handoff: BASE_ABILITY Plan Independent R3 Re-review`。先读取 `.agents/skills/vibe-review/SKILL.md` 和 `vibe-coding-skills-v2/commands/vibe-review.md`，再独立执行 `/vibe-review impl doc/features/base-ability-42plus8-pilot-readiness-impl.md`。核验 P1-01/P1-02 的实际 diff、权威基线、实现和命令证据，重新执行定向合同测试、authority/gate check、typecheck、lint、文档索引和 diff check。不要修改 Schema、题库状态、运行库或 M5B 冻结工件，也不要进入 Step 3。保留已有的 `CONDITIONAL_PASS` 记录，另存新的 R3 审查记录；仅在复审真实 `PASS` 后，才把下一原子动作改为 Step 3 评估。
```
