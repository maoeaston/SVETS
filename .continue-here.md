# Project Direction
- 项目总目标：先把 Pilot R0 做到可追溯、可验收，再继续修订并交付 Full Product 1.0；Pilot 不是项目终点。
- 必须保留的产品决策：`doc/specs/MVP_PRD_v1.0.9-authoritative.md` 是当前唯一产品合同；AI 自动评分和 AI 岗位推荐必须进入 Full Product 1.0；`doc/specs/FULL_PRODUCT_PRD_v2.0-draft.md` 仍是未批准草案。
- 不变边界：安全红线、历史可追溯和教师责任边界不得降级；题目审核通过前不得激活；不得把 AI 作为无人工确认的录用或淘汰最终裁决者。

# Current Milestone
- Pilot R0 基础底座：F2 至 F6 已完成；当前推进 F7 报告页面框架，完成门槛是实现文档、报告 IPC/UI、权限、锁定、导出框架和验收门禁全部通过。

# Current State
- 任务：F7 Step 9 教师报告列表、详情与操作界面已实现，自动化验收通过。
- 当前 Vibe Coding 步骤：逐步实现第 10 步（待独立审查、全量门禁和真实 Electron 见证）。
- 已完成：`src/renderer/src/stores/report.ts` 通过 Pinia 内存状态调用九个 `reports:*` IPC 并在操作后重读；`src/renderer/src/views/teacher/ReportListView.vue` 和 `src/renderer/src/views/teacher/ReportDetailView.vue` 已接入 `/teacher/reports` 与 `/teacher/reports/:reportId`；`src/renderer/src/components/report/ReportSectionList.vue` 只渲染 shared presentation sections，不解释 raw report JSON；`src/renderer/src/components/report/report-page-state.ts` 与测试覆盖历史、repair、capability、候选和错误状态；`src/main/window-config.ts` 与测试覆盖生产 content size 1280x720、最小 1024x640 和 E2E allowlist 1366x768 / 1280x720 / 375x812；教师导航和首页任务报告入口已启用。
- 已完成验证：`npx vitest run src/shared/__tests__/report-presentation.test.ts src/renderer/src/components/report/__tests__/report-page-state.test.ts src/main/__tests__/window-config.test.ts` 通过（3 files / 13 tests）；`npm run typecheck` 通过；`npm run lint` 通过（0 error，661 条既有 Vue 格式 warning）；`npm run build` 通过；`git diff --check` 通过；报告 UI 新增/修改文件扫描未发现 `v-html`、`innerHTML`、SQLite/真实库访问。
- 未执行验收：默认 `npm run db:verify` 会指向真实运行库 `xc-career-guide.db`，按本轮约束未执行；全量 `npm test`、Electron 实机报告闭环和三尺寸截图仍待 Step 10。
- 产出物：当前 HEAD commit `feat(report): add teacher report workspace`；上一已推送提交为 `301849c feat: add report framework and html export`。

# Next Action
- 按 `doc/features/report-page-framework-impl.md` 的 Step 10 完成独立审查、全量门禁和真实 Electron 见证；继续不得接触真实 `xc-career-guide.db`，Electron 见证需使用隔离临时 userData。

# Milestone Queue
1. 当前：F7 Step 10 独立审查、全量门禁和真实 Electron 见证。
2. 后续：Q1 BASE_ABILITY 42+8 正式施测补料。
3. 后续：Q2 JOB_SKILL R1-R6 与 270 项素材/Pilot 门禁。

# Blockers
- None。

# Key Decisions
- 报告直接渲染持久化 `report_content_json` 的 shared presentation；页面和导出不得各自解释 raw report JSON，不引入 Markdown 或 PDF 报告渲染。
- F7 只导出默认脱敏、无脚本和外部请求的自包含 HTML；取消不写事件，成功导出创建独立 `REPORT_FILE` asset，`LOCKED` 报告导出后仍保持 `LOCKED`。
- BASE_ABILITY 完整报告只能从教师确认的活动 `task_closure` 生成；闭环修正走 replacement，不覆盖历史 binding。

# Relevant Files
- `doc/features/report-page-framework-impl.md`
- `doc/specs/baseline.yaml`
- `doc/specs/project-invariants.md`
- `doc/ai/vibe-workflow-contract.md`
- `.agents/skills/vibe-accept/SKILL.md`
- `vibe-coding-skills-v2/commands/vibe-accept.md`
- `src/renderer/src/views/teacher/ReportListView.vue`
- `src/renderer/src/views/teacher/ReportDetailView.vue`
- `src/main/window-config.ts`
