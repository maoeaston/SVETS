# Project Direction
- 项目总目标：先把 Pilot R0 做到可追溯、可验收，再继续修订并交付 Full Product 1.0；Pilot 不是项目终点。
- 必须保留的产品决策：`doc/specs/MVP_PRD_v1.0.9-authoritative.md` 是唯一产品合同；M4 安全聚合当前使用 `student_id + job_code + task_code`；默认数据库不得读取或修改。
- 不变边界：安全红线、事件追溯、责任归属和工作区中未授权的 BASE_ABILITY 改动不得静默降级或覆盖。

# Current Milestone
- M4 Step 2A 已 PASS / CLOSED；BASE_ABILITY 42+8 已完成检查、验证和独立本地提交。

# Current State
- 任务：完成 BASE_ABILITY 42+8 权威草案及其门禁的独立验证与提交。
- 当前 Vibe Coding 步骤：BASE_ABILITY 已完成验证并本地提交；正式 `/vibe-review` 记录仍未补做。
- 已完成：M4 Step 2A 已独立 `/vibe-review code` 和 `$vibe-accept`；commit `5dbb670` 已推送；BASE_ABILITY commit `ce5831d` 和独立文档提交均已本地提交。
- 产出物：M4 验收记录 `doc/features/multi-device-m4-safety-rekey-validation.md`；BASE_ABILITY authority/gate 产物已固定。

# Next Action
- 用户授权后推送当前分支的两个本地提交；之后再按授权启动 M4 Step 2B / M5A Command Bus Boundary。

# Milestone Queue
1. BASE_ABILITY：实现与交接文档提交已完成，待用户授权推送。
2. M4 Step 2B / M5A Command Bus Boundary：用户明确授权后使用 `vibe-feature` → `vibe-impl`。
3. Step 2C batch runtime：草案，待 Step 2B 完成后裁决。

# Blockers
- None；当前工作区只剩交接与文档索引改动，BASE_ABILITY 已独立提交。

# Key Decisions
- 不使用 `git add .`，不把 BASE_ABILITY 与 M4 混在同一提交中。
- 默认 `xc-career-guide.db` 不读取、不修改；真实数据库验证必须使用隔离临时目录。
- M4 已完成，后续任务不得回写或篡改其验收证据。

# Relevant Files
- `doc/features/base-ability-42plus8-activation-gate-v1.json`
- `doc/features/base-ability-42plus8-activation-gate-v1.schema.json`
- `doc/features/base-ability-42plus8-pilot-readiness-impl.md`
- `scripts/lib/base-ability-42plus8-gate.mjs`
- `scripts/lib/base-ability-42plus8-authority.mjs`
- `scripts/__tests__/base-ability-42plus8-gate.test.mjs`
- `scripts/__tests__/base-ability-42plus8-authority.test.mjs`
- `package.json`
