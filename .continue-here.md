# Current State
- 任务：多设备 v2.2 M3 Grant/Assignment Step M3-2 migration runner 与 CHECK 表重建已完成。
- 当前 Vibe Coding 步骤：逐步实现 Step M3-3。
- 已完成：
  - `src/main/db/migrations.ts` — 新增 M2 历史常量，当前版本切到 `0.1.15-multi-device-m3-grant-assignment`；新增 M3 T7/T8、五个索引、D1/D9-D11 结构断言、漂移拒绝和 `applyM3Migration()`。
  - `src/main/db/migrations.ts` — M3 migration 会重建 `assessment_session` 扩展 `delivery_phase` CHECK，并补齐 `domain_event_projection.aggregate_type` 的 `BUSINESS_SESSION` 支持。
  - `src/main/db/__tests__/migrations.test.ts` — 覆盖 v0.1.12 -> M1/M2/M3、v0.1.14 -> M3、M3 ledger 补写、M3 漂移/残缺对象拒绝。
  - 验证：`npm test -- src/main/db/__tests__/migrations.test.ts` 通过；`npm test -- src/main/db/__tests__/migrations.test.ts src/main/db/__tests__/schema-scoring-closure.test.ts` 通过；`npm run typecheck` 通过；`npm run lint` 通过（0 error，保留既有 160 个 Vue warning）；`git diff --check` 通过。
- 产出物：
  - `src/main/db/migrations.ts`
  - `src/main/db/__tests__/migrations.test.ts`

# Next Action
- 按 `doc/features/multi-device-v2.2-migration-impl.md` 的 Step M3-3 新增本地 runtime/auth 引导服务骨架，只在 assignment handler 需要时调用，不接入 `initDatabase()`。

# Blockers
- `doc/features/multi-device-v2.2-migration-impl.md` 提到的 `src/main/db/__tests__/connection-migration.test.ts` 当前仓库不存在；Step M3-2 已用实际存在的迁移与 schema 测试覆盖。

# Key Decisions
- M3 migration 必须 fail closed：同名错误 D1/D9-D11 trigger 或 M3 index 不覆盖，残缺 M3 表结构直接拒绝。
- `assessment_session` CHECK 升级通过显式事务内表重建完成；重建前后校验行数、主键和关键字段不变。
- `domain_event_projection.aggregate_type` 在旧库迁移中同步补入 `BUSINESS_SESSION`，否则后续 Assignment 事件无法进入 SQLite projection。

# Relevant Files
- `doc/features/multi-device-v2.2-migration-impl.md`
- `src/main/db/schema.sql`
- `src/main/db/migrations.ts`
- `src/main/db/__tests__/migrations.test.ts`
