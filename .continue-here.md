# Current State
- 任务：5.4 测评功能逐步实现，Step 6b（createSession handler）已完成，下一步 Step 7（答题 + 情绪中断/崩溃 handler）
- 当前 Vibe Coding 步骤：逐步实现，Step 6b 完成待提交，Step 7 待开始
- 已完成（5.4 全程）：
  - Step 1（2618583）：EventType + payload types
  - Step 2（ac38f94）：assertStudent / assertSessionOwner + seed helpers
  - Step 3（558710c）：paper-generator 纯函数（模块均衡）
  - Step 4（61503e3）：level-judge 纯函数（§17.6.1 九用例）
  - Step 5（6ab86f2 + 26499f5）：assessment-reducer 幂等投影（/vibe-review 通过，230 tests 绿）
  - Step 6a（8327919）：assessment IPC 类型 + preload 白名单（3 文件纯类型）
  - Step 6b：createSession handler + seedAssessmentErrorCodes + IPC 注册 + 14 单测（244 tests 全绿）
  - 题库源切换（b56894b）：CSV→XLSX + 6 处文档引用同步
- 产出物：`src/main/ipc/handlers/assessment.ts`（新）+ `ipc/index.ts`（改）+ `shared/types/assessment.ts`（taskCode 补漏）+ `__tests__/assessment-create.test.ts`（新）

# Next Action
- Step 7：扩展 `src/main/ipc/handlers/assessment.ts`（submitAnswer / emotionInterrupt / emotionResume / abortSession）+ 2 个新测试文件
- 核心路径（submitAnswer STUDENT）：assertStudent + assertSessionOwner → 校验 session.status=ACTIVE → 校验 question ∈ 本 session ONLINE → 校验无 VALID answer_record（ALREADY_ANSWERED）→ 按 question_type 校验 answer_payload 结构 → 计算分数 → 事务{ writeEvent(ANSWER_SUBMITTED) + applyAssessmentEvent }
- 核心路径（崩溃计数）：从 domain_event_projection 查 EMOTION_INTERRUPTED 后无 EMOTION_RESUMED 的次数（事件溯源查询，非冗余字段）
- 详见 `doc/features/assessment-impl.md` Step 7（line 362-405）

# Blockers
- 5.3 题库未交付：组卷/单测用 mock 题库，端到端验证推迟

# Key Decisions
- **reducer 是投影唯一写入者**：handler 只调 writeEvent + applyAssessmentEvent，不另 INSERT（Step 6b 验证此路径——createSession 调 writeEvent(SESSION_STARTED) + applyAssessmentEvent，reducer 承担 INSERT session/question + status=ACTIVE）
- **SESSION_STARTED → status=ACTIVE**：无独立 SESSION_ACTIVATED 事件，保冷启动重放忠实
- **answerPayload 复用 AnswerPayloadDetail**：IPC 与事件 payload 1:1，无映射层。[!] impl.md Step 7 文本（line 378-380）字段名 `selected_option`/`slots` 与 event-payloads.ts `selected`/`placements` 不一致，Step 7 实现 handler 时同步文档
- **writeEvent mock 模式（Step 6b 建立，Step 7/8 可复用）**：vi.mock + vi.hoisted 共享 db 引用，mock 内 INSERT domain_event_projection（reducer FK 依赖 event_id 存在）+ 不写 jsonl。assessment 是首个写领域事件的 handler。
- **taskCode 在 CreateSessionParams（Step 6a 遗漏，6b 补）**：schema assessment_session.task_code NOT NULL，PRD §17 要求 TEACHER 传 task；jobCode 从 strategy_config 派生（岗位属策略属性）
- **QUESTION_BANK_INSUFFICIENT 写审计**（ERROR + UNRESOLVED）：区别于 FORBIDDEN/NOT_FOUND 等正常业务拒绝，给运营可见性（题库需补题）

# Relevant Files
- `doc/features/assessment-impl.md`（Step 7 规范，line 362-405）
- `src/main/ipc/handlers/assessment.ts`（handler，Step 7 扩展 submitAnswer 等）
- `src/main/ipc/handlers/__tests__/assessment-create.test.ts`（writeEvent mock 模式参考）
- `src/main/domain/assessment-reducer.ts`（reducer，Step 7 调用）
- `src/shared/types/assessment.ts`（IPC 类型契约，含 taskCode）
- `src/shared/types/event-payloads.ts`（AnswerPayloadDetail / AnswerSubmittedPayload）
- `src/main/utils/auth-context.ts`（assertStudent / assertSessionOwner，Step 7 调用）
- `src/main/db/test-helpers.ts`（seedStudent / seedQuestionBank / seedCaller 复用）
