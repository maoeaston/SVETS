# Project Direction
- 项目总目标：先把 Pilot R0 做到可追溯、可验收，再继续修订并交付 Full Product 1.0；Pilot 不是项目终点。
- 必须保留的产品决策：`doc/specs/MVP_PRD_v1.0.9-authoritative.md` 是当前唯一产品合同；AI 自动评分和 AI 岗位推荐必须进入 Full Product 1.0；`doc/specs/FULL_PRODUCT_PRD_v2.0-draft.md` 仍是未批准草案。
- 不变边界：安全红线、历史可追溯和教师责任边界不得降级；题目审核通过前不得激活；不得把 AI 作为无人工确认的录用或淘汰最终裁决者。

# Current Milestone
- Pilot R0 基础底座：F2 登录会话与权限、F3 数据保存与恢复、F4 暂停恢复与安全红线、F5 基础页面与异常中心均已完成；F6-1 和 F6-2 已完成，准备进入 F6-3 基础能力线下评分提交入口。

# Current State
- 任务：F6-2 已完成：`RESULT_CALCULATED` payload 支持策略字段，`result_record` 会投影 `strategy_id/strategy_type/module_type`，训练、红线、岗位技能、操作通过率现有结果生产者已补齐字段。
- 当前 Vibe Coding 步骤：逐步实现第3步（F6-3 基础能力线下评分提交入口）。
- 已完成：`doc/features/scoring-framework-dual-track-prd.md` 标记 [!] PRD §17.6.2 与旧 schema 默认 `score_scope` 冲突；`doc/features/scoring-framework-dual-track-impl.md` 将 F6 拆为 F6-1 至 F6-6。
- 已完成：F6-1 修改 `src/main/db/schema.sql`、`src/main/db/migrations.ts`、`src/main/db/__tests__/schema-scoring-closure.test.ts`、`src/main/db/__tests__/migrations.test.ts`，移除 `offline_score_record.score_scope` 默认值并补旧库迁移与漏写失败测试。
- 已完成：F6-2 修改 `src/shared/types/event-payloads.ts`、`src/main/domain/assessment-reducer.ts`、`src/main/domain/training-reducer.ts`、`src/main/ipc/handlers/assessment.ts`、`src/main/ipc/handlers/job-skill-result.ts`、`src/main/ipc/handlers/operation-scoring.ts` 及对应测试，结果事件和投影补齐策略字段，旧事件缺字段仍落 NULL。
- 已完成验证：F6-2 相关 `vitest`（5 files / 95 tests）、`npm run typecheck`、`git diff --check`、`npm run lint`（0 error，622 warnings）、`npm run docs:index:check`、`npm test`（82 files / 919 tests）、`npm run build` 全部通过；交接更新后已复跑 `npm run docs:index:check`、`git diff --check`、`npm run typecheck`。
- 产出物：`doc/features/scoring-framework-dual-track-prd.md`、`doc/features/scoring-framework-dual-track-impl.md`，并同步 `doc/index.md` 自动清单。

# Next Action
- 执行 F6-3：新增基础能力线下评分提交/读取入口（建议 `assessment:submitOfflineAbilityScores` / `assessment:getOfflineAbilityScores`），仅允许 TEACHER/ADMIN，对 `BASELINE_ASSESSMENT` / `MOCK_EXAM` 的 `OFFLINE_PENDING` 会话写显式 `score_scope='OFFLINE_ABILITY'` 的 `OFFLINE_SCORE_SUBMITTED` 事件；验证题目必须属于该 session 的 `BASE_ABILITY`、`question_phase='OFFLINE'`、`item_usage='SCORED_ITEM'`，提交失败必须事务回滚。暂不在 F6-3 做基础能力自动结算（F6-4）。

# Milestone Queue
1. 当前：F6 评分计算框架与双轨隔离，剩余 F6-3 基础能力线下评分提交入口、F6-4 基础能力结算核心、F6-5 统一结果读取、F6-6 双轨隔离回归。
2. 后续：F7 报告页面框架。
3. 后续：Q1 BASE_ABILITY 42+8 正式施测补料；Q2 JOB_SKILL R1-R6 与 270 项素材/Pilot 门禁。

# Blockers
- None（F6-3 可用内存库、临时库和测试夹具继续）。
- 非阻塞背景：工作区长期 dirty，包含大量既有改动；JOB_SKILL 真实激活仍被 270 项 planned 资产和 Pilot 独立门禁阻止；BASE_ABILITY 真实激活仍被 96 DRAFT / 96 NO_SCORE、renderer 与线下评分锚点缺失阻止；默认运行数据库存在内容包漂移，`db:verify` 需后续独立运维修复并先备份。

# Key Decisions
- 可信身份只能由主进程按绑定 `auth_session` 解析；渲染进程路由和状态不能替代主进程权限判定。
- F6 writer 必须显式传 `score_scope`；不得依赖数据库默认值或静默补值。
- F6 结果生成必须通过 `RESULT_CALCULATED` 事件和 `result_record` 投影；四类结果不得平均、相加或展示为单一总分。

# Relevant Files
- `doc/features/scoring-framework-dual-track-impl.md`
- `src/main/ipc/handlers/assessment.ts`
- `src/main/domain/assessment-reducer.ts`
- `src/shared/types/assessment.ts`
- `src/shared/types/ipc-api.ts`
- `src/shared/types/event-payloads.ts`
- `src/main/ipc/index.ts`
- `src/preload/index.ts`
- `src/main/db/schema.sql`
- `src/main/ipc/handlers/__tests__/assessment-ability-scoring.test.ts`
