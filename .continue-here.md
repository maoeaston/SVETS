# Current State
- 任务：5.4 测评功能 Step 8（triggerRedline + calculateResult + result_record 落盘）已完成并提交
- 当前 Vibe Coding 步骤：逐步实现 Step 8 完成，Step 9 待开始
- 已完成（Step 8 全程）：
  - `src/main/ipc/handlers/assessment.ts` — triggerRedline 纯函数（TEACHER/ADMIN）+ calculateResult 纯函数 + 共享辅助 persistRedlineResult/readSessionForRedline/computeRedlineModuleScores/readStrategyForJudge + statusToRedlineErrorCode + IPC 注册 triggerRedline/calculateResult
  - `__tests__/assessment-redline.test.ts` — 22 测试（正常触发 + 批量熔断多 session + EMOTION_INTERRUPTED/OFFLINE_PENDING 被熔断 + 终态拒绝 + STUDENT/ADMIN/枚举校验 + calculateResult 红线/幂等/唯一性）
  - `doc/features/assessment-impl.md` Step 8 同步（实测结论 + 边界处理说明）
  - typecheck 通过，npm test 311/311 全绿（19 文件，旧 289 + 新 22）
- 产出物：assessment.ts（2 handler + 4 辅助）+ redline.test.ts + impl.md 更新

# Next Action
- Step 9：渲染层（5 文件）。先读 `doc/features/assessment-impl.md` Step 9（line 470+）。
  - `src/renderer/src/stores/assessment.ts` — Pinia store 封装 assessment IPC
  - `src/renderer/src/views/student/AssessmentView.vue` — 学生答题页
  - `src/renderer/src/views/teacher/AssessmentCreateView.vue` — 教师发起测评
  - `src/renderer/src/views/teacher/AssessmentListView.vue` — 教师列表 + 终止/触发红线
  - `src/renderer/src/router/index.ts` — 注册路由
  - 测试策略：手工验收 + npm run build（vue-tsc）；webapp-testing skill 或手工 E2E 截图

# Blockers
- None（5.3 题库仍未交付，端到端验证继续推迟；不影响 Step 9 渲染层搭建）

# Key Decisions
- **collapse_count = count(EMOTION_INTERRUPTED) − count(EMOTION_RESUMED)**（事件溯源查询，非 pause_count 字段；pause_count 只增不减）。abortSession 在 collapse_count ≥ emotion_collapse_threshold 时先写 EMOTION_COLLAPSE_THRESHOLD_REACHED 再写 SESSION_ABORTED。impl.md 早期 "+1 后达" 措辞已修正为 ≥
- **writeEvent mock 用 MAX+1 event_sequence**（非硬编码 1）：跨多 aggregate（SAFETY_INCIDENT / ASSESSMENT_SESSION）各自维护递增 sequence；MAX+1 模板对两 aggregate 都正确
- **content_json 严格校验**：submitAnswer 解析 content_json 失败或缺 expected_answer/options/drop_zones → VALIDATION_ERROR；计分按题型硬编码（2/0 exact、2/1/0 drag partial），不读 scoring_rule_json
- **emotionInterrupt 允许从 EMOTION_INTERRUPTED**（escalating collapse）：连续未恢复中断累加到 threshold 的唯一路径
- **triggerRedline 终态拒绝**（Step 8 实测补充）：handler 对 COMPLETED/ABORTED → SESSION_NOT_ACTIVE；REDLINE_HALTED → SESSION_HALTED；开放态允许。impl.md 未明示但必要（避免 persistRedlineResult 事务内抛错回滚到 REDLINE_TRIGGER_SYSTEM_ERROR 语义错位）
- **MemoryAdapter.transaction 无 SAVEPOINT**（sql.js 限制）：persistRedlineResult 不开事务（与 reducer 同模式），由调用方 transaction 包裹。triggerRedline / calculateResult 各自完整事务。生产 better-sqlite3 支持 SAVEPOINT，但保持测试一致性沿用此模式
- **calculateResult MVP 仅红线场景**：session.status === 'REDLINE_HALTED'；正常完成路径推到后续 Step（线下评分后）。幂等：已有 result_record 直接返回
- **批量熔断 result_record 仅落 target session**：schema trigger 批量改 session status，但 handler 仅对入参 sessionId 调 persistRedlineResult。后续若需对所有熔断 session 落 result_record 可扩展（impl.md 未要求）
- **context_phase 不含 BASELINE_ASSESSMENT**（impl.md Step 8 风险点 10 回归覆盖）：handler 前置枚举校验拦截，不进事务（避免被 schema CHECK ABORT 后留脏 jsonl）

# Relevant Files
- `doc/features/assessment-impl.md`（Step 9 规范，line 470+）
- `src/renderer/src/stores/assessment.ts`（Step 9 新建，Pinia）
- `src/renderer/src/views/student/AssessmentView.vue` / `teacher/AssessmentCreateView.vue` / `teacher/AssessmentListView.vue`（Step 9 新建）
- `src/renderer/src/router/index.ts`（Step 9 注册路由）
- `src/preload/index.ts`（assessment 命名空间已声明全部 8 通道）
- `src/main/ipc/handlers/assessment.ts`（triggerRedline / calculateResult 已注册；getSession 仍待后续 Step）
