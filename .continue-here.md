# Current State
- 任务：题库首批最小上线集已激活，准备在下一会话实现可复现伪随机组卷。
- 当前 Vibe Coding 步骤：题库审核激活数据运维已完成；下一会话进入组卷算法行为变更设计/实现。
- 已完成：运行库 `/home/maoea/.config/xc-career-guide/data/xc-career-guide.db` 已激活首批 50 道题（42 ONLINE + 8 OFFLINE）；`node scripts/check-question-bank-launch-gate.mjs --db /home/maoea/.config/xc-career-guide/data/xc-career-guide.db --job SUPERMARKET_SHELVER` PASS；`npm run typecheck`、`npm run lint`、`npm test`（31 files / 413 tests）通过。
- 产出物：`/tmp/question-bank-first-activation.json`、`/tmp/question-bank-first-activation.sql`、`/tmp/xc-career-guide.before-first-question-activation.db`、`doc/features/question-bank-launch-gate-report.json`。

# Next Action
- 在 `src/main/domain/__tests__/paper-generator.test.ts` 中先写失败测试：同一 `paperSeed` 输出一致、不同 `paperSeed` 在有余量题库时输出不同且仍满足 42 ONLINE + 8 OFFLINE 配额。

# Blockers
- `git push origin main` 仍因 GitHub SSH 身份权限失败：`Permission to maoeaston/SVETS.git denied to maoeast`；本地 `main` 领先 `origin/main` 3 个提交。
- 当前运行库只激活了首批 50 道题；可复现伪随机要发挥效果，还需要后续继续审核并激活更多题，否则随机池没有余量。

# Key Decisions
- 组卷随机化采用可复现伪随机，不用完全随机；`paperSeed` 由 `sessionId + studentId + strategyId + strategyVersion` 派生。
- 事件溯源回放仍以 `SESSION_STARTED.question_ids` 为权威；组卷算法只在创建会话时决定题目。
- 本次只做最小闭环：给 `generatePaper` 增加 seed shuffle，并在 `assessment:createSession` 生成 sessionId 后派生 seed 再组卷；学生历史避重复留到后续。

# Relevant Files
- `src/main/domain/paper-generator.ts`
- `src/main/domain/__tests__/paper-generator.test.ts`
- `src/main/ipc/handlers/assessment.ts`
- `src/main/ipc/handlers/__tests__/assessment-create.test.ts`
- `src/shared/types/event-payloads.ts`
