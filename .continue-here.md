# Current State
- 任务：PRD v1.0.6-scoring-closure 已采纳，Waza /check 同步与红线计分问题已修复，正在按会话结束流程提交未跟踪产物
- 当前 Vibe Coding 步骤：/vibe-accept（PRD / schema / 核心评分收口已完成验证；尚有 tracked 改动未提交）
- 已完成：
  - `src/shared/types/event-payloads.ts` / `doc/xc-career-guide-event-payload-schema-v1.0.0.md` — 补齐 `QUESTION_BANK` 聚合根和 v1.0.6 新事件规范
  - `src/main/ipc/handlers/assessment.ts` / `src/main/ipc/handlers/__tests__/assessment-redline.test.ts` — 红线 `ABILITY_SCORE` 计入 `OFFLINE_ABILITY` 线下分，排除 `TASK_OPERATION`
  - `AGENTS.md` — 状态机说明同步 `SUSPENDED_REVIEW_REQUIRED` / `OFFLINE_PENDING`
  - 验证：`npm run typecheck` 通过；`npm test` 通过（26 files / 377 tests）；`npm run build` 通过；`npm run lint` 因 `eslint: not found` 失败（未新增依赖）
- 产出物：
  - `doc/炫灿-职途向导系统_MVP_PRD_v1.0.6.md`
  - `doc/PRD MVP claude.md`
  - `doc/prd审查报告.md`
  - `src/main/db/__tests__/schema-scoring-closure.test.ts`

# Next Action
- 执行 `git diff --stat` 核对剩余 tracked 改动，并按 PRD 收口 / Waza 修复拆分提交边界

# Blockers
- `npm run lint` 仍失败：`package.json` 有 lint 脚本，但项目未安装 `eslint` devDependency
- prod 打包：`AIimages/` 不在 asar 内，打包后全 404（已知 gap，不在当前 scope）

# Key Decisions
- `QUESTION_SUPERSEDED` 固定使用 `aggregate_id = old_question_id`，表达“旧题被新题替代”
- 红线中途终止的 `ABILITY_SCORE`：`max_score = 100`，`normalized_score = clamp(raw_score, 0, 100)`，`raw_score` 包含线上分 + `OFFLINE_ABILITY` 线下分
- `TASK_OPERATION` 只属于 `OPERATION_PASS_RATE`，不得进入 `ABILITY_SCORE.offline_raw_score` 或完成率

# Relevant Files
- `src/shared/types/event-payloads.ts`
- `doc/xc-career-guide-event-payload-schema-v1.0.0.md`
- `src/main/ipc/handlers/assessment.ts`
- `src/main/ipc/handlers/__tests__/assessment-redline.test.ts`
- `AGENTS.md`
