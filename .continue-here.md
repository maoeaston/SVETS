# Current State
- 任务：5.4 测评功能逐步实现，Step 6a（IPC 通道声明）已完成，下一步 Step 6b（createSession handler）
- 当前 Vibe Coding 步骤：逐步实现，Step 6a 完成并提交（8327919），待开始 Step 6b
- 已完成（5.4 全程）：
  - Step 1（2618583）：EventType + payload types
  - Step 2（ac38f94）：assertStudent / assertSessionOwner + seed helpers
  - Step 3（558710c）：paper-generator 纯函数（模块均衡）
  - Step 4（61503e3）：level-judge 纯函数（§17.6.1 九用例）
  - Step 5（6ab86f2 + 26499f5）：assessment-reducer 幂等投影（/vibe-review 通过，230 tests 绿）
  - Step 6a（8327919）：assessment IPC 类型 + preload 白名单（3 文件纯类型）
  - 题库源切换（b56894b）：CSV→XLSX + 6 处文档引用同步
- 产出物：`src/shared/types/assessment.ts`（新）+ `ipc-api.ts` / `preload/index.ts`（改）

# Next Action
- Step 6b：新建 `src/main/ipc/handlers/assessment.ts`（createSession 纯函数 + registerAssessmentHandlers + seedAssessmentErrorCodes）+ `src/main/ipc/index.ts` 注册 + `__tests__/assessment-create.test.ts`
- 核心路径：assertCaller(TEACHER) → 读 strategy_config（type ∈ BASELINE/MOCK）→ 校验 student ACTIVE → 校验无开放 session → 校验无未解决安全事件 → generatePaper → db.transaction{ INSERT session + 50 question + writeEvent(SESSION_STARTED) + applyAssessmentEvent + UPDATE status=ACTIVE }
- 详见 `doc/features/assessment-impl.md` Step 6b（line 304-358）

# Blockers
- 5.3 题库未交付：组卷/单测用 mock 题库，端到端验证推迟

# Key Decisions
- **reducer 是投影唯一写入者**：handler 只调 writeEvent + applyAssessmentEvent，不另 INSERT（Step 6b 的 INSERT session/question 已由 Step 5 reducer 承接 SESSION_STARTED 事件）
- **SESSION_STARTED → status=ACTIVE**：无独立 SESSION_ACTIVATED 事件，保冷启动重放忠实
- **answerPayload 复用 AnswerPayloadDetail**：IPC 与事件 payload 1:1，无映射层。[!] impl.md Step 7 文本（line 378-380）字段名 `selected_option`/`slots` 与 event-payloads.ts `selected`/`placements` 不一致，Step 7 实现 handler 时同步文档

# Relevant Files
- `doc/features/assessment-impl.md`（Step 6b 规范，line 304-358）
- `src/main/ipc/handlers/strategy.ts`（handler 纯函数 + register + seed 模式参考）
- `src/main/domain/assessment-reducer.ts`（reducer，Step 6b 调用）
- `src/main/domain/paper-generator.ts`（组卷，Step 6b 调用）
- `src/shared/types/assessment.ts`（IPC 类型契约）
- `src/main/db/test-helpers.ts`（seedStudent / seedQuestionBank 复用）
