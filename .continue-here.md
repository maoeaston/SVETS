# Current State
- 任务：学生档案管理功能（教师创建/编辑/归档学生档案 + 开通学生登录账号）
- 当前 Vibe Coding 步骤：7 步实现全完成 + 启动崩溃 fix + seed 脚本补丁，待用户 E2E 冒烟
- 已完成（commit 链见 git log，均在 feat/student-profile 分支）：
  - 2d29de5 PRD + 实现文档（doc/features/student-profile-{prd,impl}.md）
  - de3fbf8 sensory_profile 校验 + 共享类型（17 单测）
  - fb46b5f DBAdapter 端口 + IPC 类型 + caller 校验（10 单测）
  - 1e8dcbe student:create handler（18 集成测试，事务 + 审计）
  - d3e0cc6 student:get + list handler（19 集成测试）
  - 0517620 student:update + archive handler（18 集成测试）
  - 230d43d 列表页 + TeacherLayout + 路由 + 表单占位
  - f096283 表单页（新建/编辑/归档）
  - 3fb4c86 启动崩溃修复（seedStudentErrorCodes 推迟到首次 IPC）
  - 0997e96 WSL/Linux seed 脚本（.mjs）
  - 自动验证：typecheck ✅ build ✅ vitest 89/89 ✅
- 产出物：9 个 commit 在 feat/student-profile，分支未推送

# Next Action
- 用户重试登录（teacher / Teacher@123），走 E2E 9 步冒烟：
  1. 教师登录 → 跳 /teacher/students，显示列表或空状态
  2. 新建学生 → 填完整表单 → 保存 → 列表出现 1 条 ACTIVE
  3. 点该学生 → 编辑页字段回填正确（含感官画像）
  4. 修改感官画像 → 保存 → 重新进入验证持久化
  5. 重复 username → 提示「用户名已被占用」
  6. 必填留空 → 浏览器原生阻断
  7. 归档（二次确认）→ 列表状态变 ARCHIVED
  8. 归档前用该学生初始密码登录 → 成功进入 /student
  9. 归档后用同账号登录 → ACCOUNT_DISABLED
- E2E 通过后推送 feat/student-profile 到远端（用户明确要求时再推）

# Blockers
- 我无法在当前环境跑 Electron UI，E2E 9 步冒烟必须由用户手工执行
- 「doc/MVP 后续全量功能演进备忘录.md」是本会话之前就存在的未跟踪文件，非本任务产物，未处理

# Key Decisions
- DBAdapter 端口/适配器（生产 SqliteAdapter 包 better-sqlite3，测试 MemoryAdapter 用 sql.js/WASM）—— 根除 Node/Electron ABI 不匹配，handler 测试完全不触碰原生模块
- 同 UUID 复用：user_account.user_id === student_profile.student_id（schema 无 FK，应用层关联）
- 审计走 error_event_log（error_category=SYSTEM 因 CHECK 枚举不含 STUDENT_PROFILE；related_aggregate_type=STUDENT_PROFILE；INFO 用 recovery_status=IGNORED，ERROR 用 UNRESOLVED）
- seed 脚本拆双版本：原 .ts 留 Windows，新 .mjs 给 WSL/Linux（用 sqlite3 CLI 子进程处理 WAL + node:crypto 算 pbkdf2）

# Relevant Files
- src/main/ipc/handlers/student.ts（所有 student:* handler 核心，bug 排查首选）
- src/renderer/src/views/teacher/StudentFormView.vue（新建/编辑表单，Vue Proxy 序列化要点）
- src/renderer/src/views/teacher/StudentListView.vue（列表页）
- scripts/seed-dev-accounts.mjs（WSL 重新 seed 入口）
- doc/features/student-profile-impl.md（实现文档，含 Step 7 E2E 9 步清单）
