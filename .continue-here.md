# Project Direction
- 项目总目标：先把 Pilot R0 做到可追溯、可验收，再继续修订并交付 Full Product 1.0；Pilot 不是项目终点。
- 必须保留的产品决策：`doc/specs/MVP_PRD_v1.0.9-authoritative.md` 是当前唯一产品合同；Q1 采用方案 A（先经独立批准激活选定 42+8，再做真实课堂试测，试测证据门禁正式解释/发布）；`doc/specs/FULL_PRODUCT_PRD_v2.0-draft.md` 仍是未批准草案。
- 不变边界：安全红线、历史可追溯、教师责任和多设备授权链不得降级；Pilot ACTIVE 不等于正式诊断、就业安置或对外发布；执行激活的 ADMIN 不能自授批准。

# Current Milestone
- Q1 BASE_ABILITY 42+8：独立 R3 实施计划审查已完成但结论为 `BLOCKED`；先修订计划并重新取得独立 R3 `PASS`，再进入编码。

# Current State
- 任务：独立审查 `doc/features/base-ability-42plus8-pilot-readiness-impl.md`，确认是否可以开始编码。
- 当前 Vibe Coding 步骤：`/vibe-review impl` 已完成，结论 `BLOCKED（R3）`；流程退回 `/vibe-impl` 修订。
- 已完成：真正独立 Reviewer 已核验计划、Mini-PRD、权威 PRD、项目不变量和实际代码；确认 Step 7 缺外部原件时失败关闭、Step 8 严格依赖 Step 7、Step 9 不会由构建/测试自动真实激活。
- 已完成验证：`npm run typecheck`、`npm test`（1026/1026）、`npm run build`、`npm run e2e:report`、`npm run docs:index:check`、`npm run asset:validate` 和 `git diff --check` 通过；`npm run lint` 为 0 error、661 条既有 warning；工作树在审查结束时干净。
- 未完成：实施计划存在四项 P0，尚未修订；P1 的默认库保护、审核结果 build 入口、可执行 viewport/全量回归、激活台账和 down migration 联合预检等也需同步收口；未开始编码、人工审核、真实激活或课堂试测。
- 产出物：独立 R3 结论记录于本交接；审查为只读，`doc/features/base-ability-42plus8-pilot-readiness-impl.md` 未被修改。

# Next Action
- 执行 `/vibe-impl` 修订 `doc/features/base-ability-42plus8-pilot-readiness-impl.md`：消除四项 P0，并把已确认的 P1 跨文件副作用和精确验证命令一并写实；修订后不编码，交给新的独立 Reviewer 复审。

# Milestone Queue
1. 修订并独立审查 Q1 BASE_ABILITY 42+8 实施计划。
2. 计划审查通过后，按 9 个步骤逐步编码，并逐步执行 `/vibe-accept`。
3. Q1 工程交付后，继续收口尚未登记完成的 F3-F6、Q2 JOB_SKILL 素材/Pilot 门禁和 R0 发布闸门；稳定 Pilot R0 之后再评审 Full Product 1.0，未批准草案不得作为合同。

# Blockers
- P0-1：Step 2 声明冻结完整 session snapshot，但没有登记 `SESSION_STARTED` payload/validator/reducer/recovery 全链；当前事件仅含题目 ID，恢复时重新读取题库。
- P0-2：Step 2 要求 renderer 可用，而 renderer registry/实现位于 Step 4，在严格逐步 PASS 规则下形成前后依赖死锁。
- P0-3：Step 5 要求完整授权生命周期、revision/租约/时钟水位和写边界，但对应合同与服务直到 Step 9 才出现，Step 5/8 无法独立验收。
- P0-4：生产信任根职责遗漏 `issued_at/effective_at/expires_at` 和 365 天限制，生产 composition root 也缺少使用实际固定公钥的正向已知答案测试。
- `npm run db:verify` 当前运行库存在既有迁移记录缺失、报告 Schema 对象缺失、开发账号密码不符合合同、题库内容包漂移；进入 Step 5 前必须单独核实或修复，不能静默忽略。
- 内容/测评、安全/康复、特教/无障碍审核人和学校试测方案尚未提供；阻断相应人工门禁和业务 Pilot，不阻断计划修订。
- [!] `doc/features/mvp-pilot-freeze-plan.md` 的 R0-G02 仍要求 BASE 全入口禁用，与当前权威 PRD 的方案 A 冲突；不阻断 Q1 计划审查，但必须在 R0 冻结评审前修订。

# Key Decisions
- session 冻结数据必须进入既有事件 payload 并可冷启动重放；不以直接写投影代替事件，也不为此擅自新增 Schema 列。
- Step 2 只冻结 renderer 需求和组卷合同，真实 renderer 可用性及 session 放行在 Step 4 验收；不得提前宣称运行就绪。
- 通用授权合同、生命周期、持久化水位和 session 写边界前移到 Step 5；Step 9 只保留完整生产根装配、独立批准和受控激活入口。

# Relevant Files
- `doc/features/base-ability-42plus8-pilot-readiness-impl.md`
- `doc/features/base-ability-42plus8-pilot-readiness-prd.md`
- `doc/specs/MVP_PRD_v1.0.9-authoritative.md`
- `src/shared/types/event-payloads.ts`
- `src/main/domain/assessment-reducer.ts`
- `src/main/db/migrations.ts`
- `vibe-coding-skills-v2/commands/vibe-impl.md`
- `doc/specs/baseline.yaml`
- `doc/ai/vibe-workflow-contract.md`
- `doc/specs/project-invariants.md`
