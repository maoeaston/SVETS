# Current State
- 任务：5.3 图片链路全链路完成（app:// 协议 + 9-item 全素材 + DB seed）
- 当前 Vibe Coding 步骤：暂无明确下一步；候选见 # Next Action
- 已完成：
  - app:// 协议 4 commits（handler + DTO 透传 imageAssetId + renderer `<img>` + CSP 修复 + 冒烟测试）
  - 冒烟测试 5/5 全绿（200 image/png + 404 unknown/deprecated + 400 path traversal）
  - typecheck + 371/371 vitest + build 全通过
  - DB 已 UPDATE v001 孤儿为 DEPRECATED
  - DRAG-002/005 已扩成 9-item 全素材版 + seed 已写入 dev DB（asset_resource 15 ACTIVE + 1 DEPRECATED；DRAG-002 9/9 zones、DRAG-005 9/3 zones）
  - 5.3 untracked 文件分类提交 4 commits（docs / chore(assets) / feat(scripts) / chore(learnings)）+ push origin/main
  - 诊断关闭 "dropZones accepts DTO gap" blocker（详见 Key Decisions）
  - untrack derived seed SQL（`doc/features/question-bank-image-*-seed.sql` 加 .gitignore）
- 产出物：`75e0c4b` → `271942f`（13 commits on main，已 push origin/main）

# Next Action
- 候选 A：**prod 打包 AIimages/ gap** —— 当前 AIimages/ 在项目根，打包后不在 asar 内会 404。需要配置 electron-builder 把图打进 asar 或转换资产路径（涉及协议 handler 改 prod/dev 双路径）
- 候选 B：**真正的拖拽 UI**（当前是 select-per-item MVP）—— 用 HTML5 drag-drop 或 SortableJS 重写 AssessmentView.vue 的 DRAG 区域
- 候选 C：**查 PRD §17 17 项验收功能**，挑下一个未完成的功能
- 候选 D：跑 dev server 自己眼睛验证 9-item 全素材版渲染（agent 无法看 GUI，需用户手动）

# Blockers
- prod 打包：AIimages/ 不在 asar 内，打包后全 404（已知 gap，不在当前 scope）

# Key Decisions
- **CSP 是根因**：index.html CSP meta 不含 `app:` scheme，Chromium 在 protocol.handle 回调前就拦截请求。修复：img-src + connect-src 加 `app:`
- **app.getAppPath() dev 返回 out/main/**：AIimages/ 相对路径需 `resolve(app.getAppPath(), '..', '..')` 上溯到项目根
- **corsEnabled: true 必不可少**：dev 模式 renderer 在 http://localhost:5173，跨域到 app:// 需要 CORS；加在 registerSchemesAsPrivileged 即可，handler 不需要手动加 CORS 头
- **dropZones DTO 故意不透传 `accepts`**（2026-07-03 诊断关闭 blocker）：评分逻辑用 content_json 原始数据（含 accepts），不依赖 DTO；透传 accepts 等于把答案泄露给 renderer。zone.label 已足够让学生做分类判断（DRAG-005: "日化格位" / "饮料格位" / "零食格位"）。后续若做真正拖拽 UI 需要容量提示，再单独设计 client-side 反馈机制，不接受透传 accepts

# Relevant Files
- `src/main/protocol/app-asset.ts`（协议 handler 核心）
- `src/main/index.ts`（registerSchemesAsPrivileged + registerAppAssetProtocol 调用点）
- `src/renderer/index.html`（CSP meta 标签）
- `src/renderer/src/views/student/AssessmentView.vue`（`<img :src="'app://asset/' + ...">`）
- `scripts/smoke-test-app-protocol.mjs`（E2E 冒烟测试）
- `src/shared/types/assessment.ts`（DTO imageAssetId 字段；`SessionQuestionContent.dropZones` 故意只有 zoneId+label，详见 Key Decisions）
