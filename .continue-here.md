# Project Direction

- 项目总目标：先把 Pilot R0 做到可追溯、可验收，再继续修订并交付 Full Product 1.0；Pilot 不是项目终点。
- 当前产品决策：学校演示和业务闭环优先。预览版与正式版共用同一 Electron 应用，通过可配置地址的普通在线激活进入系统；教师可浏览 298 道 JOB_SKILL 题，正式演示固定使用 18 道线上题 + 6 道线下评分题。依据 `doc/specs/baseline.yaml` 与 `doc/features/school-demo-first-online-activation-prd.md`。
- 不变边界：机器合同仍使用岗位/等级/报告状态等稳定枚举，业务化只改展示映射，不改数据库枚举、事件载荷或历史报告复现；默认数据库不访问、不 promotion，临时演示库和 preview registry `INSTALLING` 边界保持不变。

# Current Milestone

- 学校演示在线激活与业务闭环已完成：激活 → 教师题库 → 学生测评 → 教师线下评分 → 报告生成在隔离临时数据库和 Electron 中跑通。当前下一阶段是学校演示体验收口，不恢复旧的 preview 专属信任链作为主流程前置。

# Current State

- 代码和业务逻辑本轮已完成，当前只需要提交并交接；开发版服务曾使用渲染地址 `http://localhost:5173`、激活地址 `http://127.0.0.1:4318`，测试授权码为 `SVETS-SCHOOL-DEMO-2026`，数据库写入临时目录，未访问生产库。学校演示截图在 `/tmp/svets-school-demo-i4Gf3P`。
- 学校演示 E2E 已通过：题库 `JOB_SPECIFIC=298`（当前使用 `ACTIVE=24`、待审核 `DRAFT=274`），学生完成 18 道线上题，教师完成 6 道线下评分，报告状态 `GENERATED` 且合同校验 `VALID`；preview registry 仍为 `INSTALLING`，preview projection 写入为 0。
- 自动化验收已通过：`npm run typecheck`；`npm run lint`（0 errors，768 warnings）；`npm test`（206 files / 1626 tests）；`npm run build`；显式临时库 `db:sync` / `db:verify`；preview parity（`PASS`，但 `preview_contract=NOT_READY`）；preview IPC（17 channels，6 READ / 11 MUTATION）；M5B target；M4 safety SQL；M5A isolated DB；Electron ABI M4 native；preview migration/rollback native；M4 UI 与 M5B UI 冒烟。
- 文档索引和工作树检查：`npm run docs:index:check`、`git diff --check` 均通过。旧 `npm run contract:m5a:command-boundary:check` 在本轮新增学校演示通道后会报告 17 个 additive channel；M5B runtime inventory 已明确把这些通道作为 school-demo additions 单独登记，冻结的 M5A 74-channel manifest 未改写。该差异是已知合同边界，不得静默当成旧 M5A target 已更新。
- 当前待改的学校演示体验按优先级排序：报告中的 `SUPERMARKET_SHELVER`、`LEVEL_COMPETENT`、“合同/修订”等技术表达；题库“全部 298 / 当前使用 24 / 待审核 274”入口和展开提示；学生页统一按钮颜色及“需要老师帮助”文案；学生页隐藏授权地址、应用版本、题库版本；激活服务器地址保留可编辑但移动到“高级设置”。
- 已知边界：`PREVIEW_CONTRACT_V1` 的 trust chain、签名、principal、registry promotion 和真实安装 migration 门仍是历史/实验能力；fresh/current registry 保持 `INSTALLING`，不能把隔离 parity 或 native harness 写成全局 `READY`。

# Next Action

- 新会话只执行一个原子动作：先审查报告展示层和报告页面状态文案，把岗位、等级、报告状态、版本/修订等内部词映射成学校能理解的中文；保留底层枚举和 `report_content_json` 结构不变，并补充对应展示测试。

# Milestone Queue

1. 完成报告业务语言映射，验证报告列表、详情、导出和异常状态不再暴露不必要的内部术语。
2. 在教师题库页增加“全部 298 / 当前使用 24 / 待审核 274”三个状态入口，并让题目有明确的展开/查看提示。
3. 统一学生答题按钮颜色，将“我遇到困难了”改为“需要老师帮助”，保持求助行为和权限边界不变。
4. 学生页面隐藏授权地址、应用版本、题库版本等技术信息；激活服务器地址仅在“高级设置”中保留可修改入口。
5. 完成学校演示人工走查后，再决定 preview registry promotion；随后恢复 BASE_ABILITY 评估，再推进 Pilot R0 与 Full Product 1.0。Full Product 草案仍是待修订，不是当前合同。

# Blockers

- 当前学校演示闭环没有新的实现阻塞；默认数据库、生产签名和真实安装环境仍不在本轮范围。
- 旧 M5A command-boundary 脚本未纳入新增学校演示 IPC，运行时会报告 additive channels；下一轮如需收口该门禁，必须单独更新合同/fixture 并重新验收，不能为本轮体验改动顺手改写冻结清单。
- 工作区还留有未跟踪的临时诊断脚本 `scripts/.tmp-diagnose-db-startup.mts`，本提交未纳入，也未删除；后续如需处理，先确认其用途再决定。

# Key Decisions

- 展示层把机器枚举翻译成中文，底层稳定 ID、事件、Schema、报告快照和导出复现保持不变。
- 题库目录直接读取共享题库：298 道全部可见，24 道为当前演示使用，274 道为待审核；浏览不激活题目、不改变策略、不写 preview projection。
- 激活是学校演示业务前置，激活地址可配置但属于高级设置；授权 token、默认库和 preview registry 不向学生端暴露。

# Relevant Files

- `src/shared/report-presentation.ts`
- `src/renderer/src/components/report/report-page-state.ts`
- `src/renderer/src/stores/report.ts`
- `src/renderer/src/views/teacher/QuestionBankView.vue`
- `src/renderer/src/views/student/AssessmentView.vue`
- `src/renderer/src/views/ActivationView.vue`
- `src/renderer/src/stores/activation.ts`
- `doc/features/school-demo-first-online-activation-prd.md`
- `doc/features/school-demo-first-online-activation-impl.md`
- `doc/specs/baseline.yaml`
- `src/main/db/schema.sql`

# Goal-mode Handoff: School Demo UX Follow-up

## Goal objective

把已验证可用的学校演示闭环收口为教师和学生能直接理解的界面；不扩大到新数据库合同，不恢复 preview 专属发布前置，也不把业务化文案改动误写成产品最终完成。

## Required boundaries

- 先读取 `AGENTS.md`、`doc/specs/baseline.yaml`、`doc/specs/project-invariants.md`、`doc/ai/vibe-workflow-contract.md`、本文件和学校演示 PRD/impl。
- 报告业务化优先改展示映射和页面状态文案；不得修改 `SUPERMARKET_SHELVER`、`LEVEL_COMPETENT` 等底层枚举及报告 JSON 合同。
- 题库浏览只能读共享题库；不得激活题目、修改策略、覆盖审核结果或写入默认数据库。
- 不降低授权 gate、学生数据权限、事件恢复和 preview registry fail-closed 约束。

## Execution order

1. 读取报告展示相关文件，列出每个技术词的用户可见位置和中文替换方案。
2. 只实现报告展示层替换并补定向测试，完成 typecheck/lint/相关测试。
3. 再进入题库状态入口和学生/激活设置的后续原子动作；每个动作单独验收并更新本文件。

## Stop conditions

出现报告合同/Schema 不一致、需要修改稳定枚举或默认数据库、题库浏览触发写入、权限边界回归、测试失败或需要产品裁决时，立即停止并记录 `[!]`。
