# Current State
- 任务：5.3 题库与资源功能已本地合并；运行库已初始化并从 `doc/通用基础能力评估题库.xlsx` 全量导入 253 道 DRAFT 题，尚未激活上线。
- 当前 Vibe Coding 步骤：`/vibe-accept` 已完成，收尾阶段；下一会话进入题库审核激活数据运维。
- 已完成：`/home/maoea/.config/xc-career-guide/data/xc-career-guide.db` 已通过 `sqlite3` CLI 初始化；`/tmp/question-bank-source-from-xlsx.csv` 已导入；`npm run typecheck`、`npm run lint`、`npm test`（31 files / 413 tests）已通过。
- 产出物：本地提交 `4577426 feat(question-bank): 完成题库与资源功能`；运行库 DRAFT 题库 253 道；`/tmp/question-bank-review-report.json`；`/tmp/question-bank-launch-gate-report.json`。

# Next Action
- 读取 `/tmp/question-bank-review-report.json`，按模块选定第一批最小审核对象（建议先满足每模块 7 道 ONLINE + 总计 8 道 OFFLINE 的上线门禁最低集）。

# Blockers
- `git push origin main` 仍因 GitHub SSH 身份权限失败：`Permission to maoeaston/SVETS.git denied to maoeast`；本地 `main` 领先 `origin/main` 2 个提交。
- 当前 `question_bank` 为 `DRAFT=253`、`ACTIVE=0`；上线门禁失败是预期状态，必须先人工清理 DRAFT 标记并补齐答案/选项/拖拽结构/素材引用。

# Key Decisions
- “单模块 40 题”按“每模块至少 40 题”理解；导入时保留源表全量题目，不因超过 40 题裁剪或标记为冲突。
- 命令行初始化/修复运行库时，如普通 Node 加载 `better-sqlite3` 出现 ABI 不一致，优先用 `sqlite3` CLI 执行 `src/main/db/schema.sql` 和导入 SQL。
- 未跟踪的 `.learnings/ERRORS.md`、`.learnings/FEATURE_REQUESTS.md`、`doc/特殊青少年职业能力发展系统 — 技术设计文档 (TDD).md` 仍不纳入提交。

# Relevant Files
- `/tmp/question-bank-review-report.json`
- `scripts/review-question-bank.mjs`
- `scripts/check-question-bank-launch-gate.mjs`
- `scripts/lib/question-bank-review-gate.mjs`
- `scripts/lib/question-bank-launch-gate.mjs`
