# Current State
- 任务：5.3 图片资产全链路完成（app:// 协议 + 9-item 全素材 + DB seed）
- 当前 Vibe Coding 步骤：无（5.3 收尾完成；下一会话进 `/vibe-feature` 启动训练功能）
- 已完成：
  - app:// 协议 + renderer 图片链路（4 commits）+ 5.3 untracked 文件分类提交（4 commits）+ blocker 诊断关闭 + DB seed
  - DB 验证：asset_resource 15 ACTIVE + 1 DEPRECATED；DRAG-002 9 items/9 zones、DRAG-005 9 items/3 zones
  - 13 commits on main，全 push origin/main；typecheck 通过
- 产出物：`75e0c4b` → `333f748`（13 commits on main，已 push origin/main）

# Next Action
- 启动 `/vibe-feature` 生成训练功能（training_session + training_step_record）的 Mini-PRD

# Blockers
- prod 打包：AIimages/ 不在 asar 内，打包后全 404（已知 gap，不在当前 scope）

# Key Decisions
- **CSP 是根因**：`src/renderer/index.html` CSP 不含 `app:` scheme 时，`protocol.handle` 回调从不触发；img-src + connect-src 加 `app:` 修复
- **app.getAppPath() dev 返回 out/main/**：AIimages/ 相对路径需 `resolve(app.getAppPath(), '..', '..')` 上溯到项目根
- **dropZones DTO 故意不透传 accepts**：评分逻辑用 content_json 原始数据（`src/main/ipc/handlers/assessment.ts:662,685-688`）不依赖 DTO；透传 accepts = 答案泄露。zone.label 已足够支撑分类判断

# Relevant Files
- `src/main/db/schema.sql:630`（training_session 表）
- `src/main/db/schema.sql:718`（training_step_record 表）
- `src/shared/types/event-payloads.ts`（training 相关 event payload）
- `doc/features/assessment-impl.md`（参考其 9 步结构作为训练 impl 模板）
- `doc/炫灿-职途向导系统_MVP_PRD_v1.0.5.md:2531-2538`（§17.1 训练验收 6 条）
