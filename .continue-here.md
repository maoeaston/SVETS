# Project Direction

- 项目总目标：先把 Pilot R0 做到可追溯、可验收，再继续修订并交付 Full Product 1.0；Pilot 不是项目终点。
- 当前会话最终目标：连续完成 Step 2C / M5B 剩余里程碑。M5B-9 的 P0/P1 已在 test-only 边界修复并完成新的独立 R3 code review；后续按 `doc/features/event-batch-v2.2-runtime-impl.md` 的依赖顺序推进 M5B-10 至 M5B-15。
- 必须保留的产品决策：`doc/specs/MVP_PRD_v1.0.9-authoritative.md` 是唯一产品合同；M4 安全聚合使用 `student_id + job_code + task_code`；默认数据库不得读取或修改。
- 不变边界：安全红线、事件追溯、责任归属和工作区中未授权的 BASE_ABILITY 改动不得静默降级或覆盖。

# Current Milestone

- M4 Step 2A 与 M5A Step 2B 已 `PASS / CLOSED`。
- M5B-1 至 M5B-5 已完成并通过分步验收。
- M5B-6 独立 R3 code review 已 `PASS`：writer mutex 内 snapshot 重读、prepared report/closure facts、恢复与故障原子性均有本轮命令证据。evidence-based accept 仍为 `AUTOMATION_PASS_MANUAL_PENDING`，因为 Electron/native/生产接线均未运行；详见 `doc/features/event-batch-v2.2-runtime-m5b6-r3-review.md`。
- M5B-7 自动化验收已通过：十个 gate-only command、same-key replay、logout 自撤销例外、disabled/revoked、A/B 差分、isolated gate transaction 和 source/scope gate 均有实际证据。非独立自审为 `CONDITIONAL_PASS`；独立 R3 复审和原生 Electron 验证未执行。
- M5B-8 自动化验收已通过：六个 training planner/projector、v2 payload union、同 batch 完成 fragment、A/B 差分、末步双 EVENT 回滚及 source/scope gate 均有实际证据。完整 R3 review/accept 和原生 Electron 验证未执行。
- M5B-9 原独立 R3 review 为 `BLOCKED`、evidence-based accept 为 `FAIL`。实现上下文已在 test-only 边界完成 P0/P1 修复：`SESSION_STARTED` PREPARE 冻结完整 session-question 事实，恢复不重新按 live `question_bank` 决定投影；默认 worker 的静态 regression 已稳定通过。新的独立 R3 复验为 code `PASS`、无遗留 P0/P1；范围门禁现以精确路径登记两份正式审查记录并通过。M5B-9 evidence-based accept 为 `AUTOMATION_PASS_MANUAL_PENDING`，Electron/native/生产接线仍为 `NOT_RUN`。
- 当前 Vibe Coding 步骤：M5B-9 的实现、独立 R3 code review 与自动化验收均已结束；下一原子步骤为 M5B-10，继续保持 M5B-14 前 test-only 边界。

# Current State

- 当前分支：`feat/multi-device-m2-prd`。
- 当前提交：本交接随 M4 至 M5B-9 checkpoint 一并提交；启动时以 `git log -1 --oneline` 确认实际 HEAD。
- `git stash list` 为空。
- 本 checkpoint 提交后工作区应保持 clean；不得用 `reset`、`clean`、`restore` 或覆盖操作处理后续出现的改动。若再次 dirty，先核对来源和范围门禁，再决定是否提交。
- 生产基线仍是 schema `v0.1.17-multi-device-m4-safety-rekey`。M5B 在 M5B-14 前不得接入生产 composition 或切换生产 schema。
- M5B-5 最终状态：独立复审 `PASS`；14 个 event-batch 测试文件共 97 项测试以及对应门禁均通过。
- M5B-6 已完成五命令 payload/planner/projector、golden、五条 legacy→v2 differential、source drift、no-op、替换归档和 APPLY 前失败原子性验证；2026-07-30 修复 coordinator 在 writer mutex 外冻结 snapshot 的竞争 P0，改为在 lease fence 后锁内重读。报告/关闭回归现为5个测试文件28项，coordinator/recovery/fencing/fault-matrix/tamper 回归为7个文件48项；typecheck、lint、isolated coordinator、M5B-9 source/scope gate 和 `git diff --check` 均通过。
- 没有修改 M5B-1 冻结的 legacy oracle 文件。差分语义已由固定时钟、确定性 ID 与成对临时根证明；仅实现派生的 closure/report ID 使用显式字段 normalizer。
- M5B-6 的独立 code review 已关闭，自动化接受仍保留 Electron/native `NOT_RUN`。M5B-9 的独立 review 已发现 P0/P1，修复后必须在新的独立上下文复验；不得以本轮或原自动化记录替代。
- M5B-7 未接入 production composition、正式 schema/startup 或默认数据库；其 10 条命令仍保留在 M5B-14 前的 test-only composition。
- M5B-8 未接入 production composition、正式 schema/startup 或默认数据库；其六条训练命令仍保留在 M5B-14 前的 test-only composition。
- M5B-9 未接入 production composition、正式 schema/startup 或默认数据库；其十条 non-redline assessment 命令仍保留在 M5B-14 前的 test-only composition。
- 本轮 M5B-9 P0：v2 `SESSION_STARTED` payload 冻结 `session_question_id`、题目顺序/phase、domain/module、type/usage、job module。prepared projector 的 `SESSION_STARTED` 没有测试适配器时拒绝投影而非回退 legacy reducer；测试适配器先登记同 event 的精确历史题目事实，再投影，不以 live 题库决定快照。覆盖基础能力 online/offline、JOB_SKILL observation、缺失字段拒绝与 `AFTER_PREPARE_FSYNC` 后禁用题目的无 planner recovery。
- 本轮 M5B-9 P1：M5B-8/9 frozen source-delta fixture 测试改为在 Vitest worker 内调用导出的校验函数，仍保留 fixture hash 与 fail-closed 行为，避免并行 worker 下子进程 stdout 竞态。默认 `npm test -- scripts/__tests__/m5b-runtime-inventory.test.mjs scripts/__tests__/m5b-contract-scope.test.mjs` 已独立连续运行 5 次均通过。
- 已执行验证：assessment 七文件 95 tests PASS；M5B inventory/scope test 文件 42 tests PASS；`npm run typecheck` PASS；`npm run lint` 0 error、661 既有 Vue 格式 warning；`npm run contract:m5b:event-batch:check -- --mode migration --step M5B-9` PASS；`npm run db:m5b:isolated:verify -- --stage coordinator` PASS（临时根已移除）；`git diff --check` PASS。
- `npm run contract:m5b:scope:check -- --step M5B-9` 已 `PASS`：两份既有未跟踪审查记录作为精确路径分别自 M5B-6/M5B-9 起允许，回归测试证明任何其他新增文档仍为 scope violation。没有删除、改名、忽略现有文件或放宽目录规则。
- checkpoint 前的完整回归已关闭两个历史视图遗漏：M5A target 断言改为从冻结 manifest 验证，并确认后续 M5B checkout 的 live target 检查按设计失败、Git baseline 仍通过；M4 safety-SQL 清单补齐 M5B-8/9 planner 的5条可执行查询，target 固定为57条。M5B-9 scope 仅增加这两项前序 gate repair 与测试的3条精确路径，8项范围测试证明其他 M4 路径仍被拒绝。
- checkpoint 验收还实际运行了完整 `npm test`、`npm run build`、`npm run db:m4:verify`、`npm run db:m4:native:verify`、`npm run db:m5a:isolated:verify`、报告 E2E 和 M4 原生 Electron UI smoke；全部通过。所有数据库/E2E验证均使用自身 `/tmp` 根，默认数据库未读取或修改。

# Next Action

- M5B-9 scope 门禁已获得符合约束的精确解决方案，自动化验收与独立 R3 code review 均有 PASS 证据。下一原子动作是按实施计划开始 M5B-10；继续禁止 production composition/schema/startup/IPC/preload 接线及默认数据库访问。新上下文先创建或恢复下列 Goal，再读取本文件和权威基线。

# Milestone Queue

1. M5B-9 evidence-based accept 已为 `AUTOMATION_PASS_MANUAL_PENDING`：自动化与独立 R3 review 通过，native/manual/production 项保持 `NOT_RUN`。M5B-7、M5B-8 的独立验收缺口保留到后续最终 R3 前关闭。
2. 严格按照 `doc/features/event-batch-v2.2-runtime-impl.md` 的依赖和门禁顺序推进 M5B-10、M5B-11、M5B-13；M5B-12 等待 M5B-11。
3. M5B 全部通过后，重跑 BASE_ABILITY Step 1 验收并独立重审 42+8 实施计划。
4. 仅在 Step 2A、Step 2B、Step 2C 与计划重审全部通过后继续 BASE_ABILITY Step 3 及以后工作。

# Blockers

- M5B-9 P0/P1 的实现修复、独立 R3 code review 与自动化验收均已完成；M5B-10/M5B-11/M5B-13 的 M5B-9 前置已解除，M5B-12 仍依赖 M5B-11。
- M5B-9 scope command 已通过精确审查记录白名单修复；禁止删除/重命名现有文档或用目录级放宽绕过范围门禁的约束继续有效。
- 仅在需要扩大权限、执行不可逆操作、权威 PRD 与 schema 出现无法自行消解的冲突、必须由用户作产品取舍，或同一外部阻塞连续出现三次且没有安全替代路径时暂停并询问用户。

# Key Decisions

- 不读取或修改默认数据库。所有数据库验证必须使用显式 `/tmp` 路径。
- 本 checkpoint 提交已获用户明确授权；后续不得 commit、push、merge 或发布，除非用户再次明确授权且对应验收完成。
- 不清理、reset、restore 或覆盖工作区；任何新改动均先核对来源和范围。
- M5B-14 前不修改生产 composition，不把 M5B migration 接到生产启动链路。
- 不修改以下 M5B-1 冻结 legacy oracle 文件：
  - `src/main/application/services/reports-service.ts`
  - `src/main/domain/task-closure-service.ts`
  - `src/main/domain/report-service.ts`
  - `src/main/domain/report-reducer.ts`
  - `src/main/domain/report-builders.ts`
  - `src/main/domain/report-contract.ts`
- 可以复用冻结 reducer 和 builders，但不得改变它们的冻结 hash。
- `reports:export`、既有报告内容、HTML、source precedence 和公开响应合同均不属于 M5B-6 的变更范围。

# Relevant Files

- `doc/会话启动.md`
- `doc/features/event-batch-v2.2-runtime-prd.md`
- `doc/features/event-batch-v2.2-runtime-impl.md`
- `doc/features/event-batch-v2.2-runtime-impl-review.md`
- `doc/features/event-batch-v2.2-runtime-validation.md`
- `doc/features/event-batch-v2.2-runtime-m5b6-r3-review.md`
- `doc/features/event-batch-v2.2-runtime-m5b9-r3-review.md`
- `doc/specs/baseline.yaml`
- `doc/specs/project-invariants.md`
- `doc/ai/vibe-workflow-contract.md`
- `vibe-coding-skills-v2/commands/vibe-impl.md`
- `vibe-coding-skills-v2/commands/vibe-review.md`
- `vibe-coding-skills-v2/commands/vibe-accept.md`

# 新会话启动 Prompt

将下面整段原样发给新会话中的 Codex：

```text
读取 `doc/会话启动.md` 和 `.continue-here.md`，恢复 SVETS 当前现场。

读取 `doc/会话启动.md`、`.continue-here.md` 和 `AGENTS.md`，恢复 SVETS 现场。

M5B-9 的 P0/P1 修复已由独立 R3 review 审查为 code `PASS`；scope gate 已通过精确路径修复，evidence-based accept 为 `AUTOMATION_PASS_MANUAL_PENDING`。当前 checkpoint 已提交后，请先运行 `git status --short --branch` 确认工作区 clean。

创建或恢复目标：按 `doc/features/event-batch-v2.2-runtime-impl.md` 的依赖顺序完成 M5B-10、M5B-11、M5B-13，再完成依赖 M5B-11 的 M5B-12，最后推进 M5B-14/15 的 production cutover 与最终 R3 验收。每一步必须读取权威基线和对应 Vibe 工作流、维持 M5B-14 前 test-only 边界、不得读写默认数据库，并执行实际验收。不要接入 production composition/schema/startup/IPC/preload，不清理/reset/restore，不 commit/push/merge，除非用户再次明确授权。
```
