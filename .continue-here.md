# Project Direction
- 项目总目标：先把 Pilot R0 做到可追溯、可验收，再继续修订并交付 Full Product 1.0；Pilot 不是项目终点。
- 必须保留的产品决策：`doc/specs/MVP_PRD_v1.0.9-authoritative.md` 是当前唯一产品合同；Q1 采用方案 A（先经独立批准激活选定 42+8，再做真实课堂试测，试测证据门禁正式解释/发布）；`doc/specs/FULL_PRODUCT_PRD_v2.0-draft.md` 仍是未批准草案。
- 不变边界：安全红线、历史可追溯、教师责任和多设备授权链不得降级；Pilot ACTIVE 不等于正式诊断、就业安置或对外发布；执行激活的 ADMIN 不能自授批准。

# Current Milestone
- Q1 BASE_ABILITY 42+8：实施计划已完成 `/vibe-impl` 修订并取得独立 R3 `PASS`；下一步实施并逐步验收 Step 1。Step 2 是独立 v2.2 batch R3 硬前置，未交付前不得进入 Step 3。

# Current State
- 任务：修订 `doc/features/base-ability-42plus8-pilot-readiness-impl.md` 并重新独立审查。
- 当前 Vibe Coding 步骤：`/vibe-impl` 已完成；`/vibe-review impl` 结论 `PASS（R3）`。下一步进入“逐步实现第 1 步”，尚未编码。
- 已完成：实施计划扩展为 10 步；删除未获批准的 `EVENT_GROUP`，把权威 v2.2 `BATCH_PREPARED/EVENT/BATCH_COMMITTED` 基础设施、全 writer 迁移、durability、startupRecovery、mutation gate 和不变量重基线前移为独立 R3 Step 2；授权原件/导入/撤回、payload 版本、异常 `ABILITY_SCORE`、down 配对、报告 E2E 和默认 v2.2 文件树保护均已写实。
- 独立审查：固定快照 SHA-256 `c7880584b7464e57a32c27e880172e7deb5dc8adf34e6d1998c1e08423f7eb84`，结论 `PASS`；`P0-01..09`、`P1-01..05` 和原 P2 全部关闭，无新增 P0/P1。
- 已完成验证：`npm run typecheck`、`npm test`（104 文件、1026 项）、`npm run build`、`npm run e2e:report`、`npm run docs:index:check`、`npm run asset:validate`、BASE/JOB_SKILL 合同检查和临时库 `db:sync/db:verify` 通过；`npm run lint` 为 0 error、661 条既有 warning；第五/六轮 Reviewer 另执行整批评分 2 文件 20 项测试、typecheck、lint、docs index 和 `git diff --check`，均符合报告。
- 未完成：任何实现步骤、未来 v2.2 R3、人工审核、生产固定根/KAT、真实激活和课堂试测均未执行；默认运行库未修改。
- 产出物：`doc/features/base-ability-42plus8-pilot-readiness-impl.md`；本轮未提交 commit。

# Next Action
- 进入逐步实现 Step 1：重新记录实际 base commit，只创建/验证 42+8 DRAFT authority、v1.2 题目合同及其确定性生成/检查脚本；不得写默认运行库或激活题目。完成后执行 `/vibe-accept step 1`。

# Milestone Queue
1. 实施并验收 Q1 Step 1：冻结 96=50+46 的 DRAFT authority 与 v1.2 题目合同。
2. 另立并交付 Step 2 权威 v2.2 batch R3，更新基线、重跑 Step 1 验收并重新独立审查本计划；通过后依次实施 Step 3-10。
3. Q1 工程交付后，继续收口尚未登记完成的 F3-F6、Q2 JOB_SKILL 素材/Pilot 门禁和 R0 发布闸门；稳定 Pilot R0 之后再评审 Full Product 1.0，未批准草案不得作为合同。

# Blockers
- Step 2 的权威 v2.2 batch runtime 尚未执行独立 `/vibe-feature`，阻断 Step 3-10，但不阻断 Step 1。
- `npm run db:verify` 当前运行库存在既有迁移记录缺失、报告 Schema 对象缺失、开发账号密码不符合合同、题库内容包漂移；进入 Step 5 前必须单独核实或修复，不能静默忽略。
- 内容/测评、安全/康复、特教/无障碍审核人和学校试测方案尚未提供；阻断 Step 8 人工门禁和真实 Pilot，不阻断 Step 1。
- 生产产品负责人 current/history 公钥根、正向 KAT、独立批准链和本次执行授权尚未提供；阻断 Step 10 真实激活。
- [!] `doc/features/mvp-pilot-freeze-plan.md` 的 R0-G02 仍要求 BASE 全入口禁用，与当前权威 PRD 的方案 A 冲突；不阻断 Q1 计划审查，但必须在 R0 冻结评审前修订。

# Key Decisions
- 不引入 `EVENT_GROUP` 或其他第三协议；多事件原子性只能由权威 v2.2 batch 独立 R3 提供，且必须先于任何 42+8 新事件实现。
- session 内容快照使用 envelope/schema 与业务 payload 独立版本；`SESSION_STARTED` payload-v2 冻结内容，payload-v3 只追加授权快照，历史版本不可改义。
- 全局 mutation gate 和默认数据保护覆盖 IPC/CLI/background 及 legacy/v2.2 完整事件文件树；真实授权原件、激活和默认库运维均不得由测试或计划自动执行。

# Relevant Files
- `doc/features/base-ability-42plus8-pilot-readiness-impl.md`
- `doc/features/base-ability-42plus8-pilot-readiness-prd.md`
- `doc/reference/通用基础能力正式测评候选题库_v0.2-软件优先版.xlsx`
- `doc/features/question-bank-import-base-ability-v02.sql`
- `scripts/build-base-ability-42plus8-gate.mjs`
- `scripts/lib/base-ability-42plus8-gate.mjs`
