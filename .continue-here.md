# Current State
- 任务：完成可复现伪随机组卷，并修复主进程打包时的动态 `require` 问题。
- 当前 Vibe Coding 步骤：逐步实现已完成，进入会话收尾 / 等待下一轮指令。
- 已完成：`src/main/domain/paper-generator.ts` 支持 `paperSeed`；`src/main/domain/__tests__/paper-generator.test.ts` 增加 seed 行为测试；`src/main/ipc/handlers/assessment.ts` 为 `createSession` 派生并传入 seed；`src/main/protocol/app-asset.ts` 改为静态导入 `../db/connection`；`AGENTS.md` 增加 Electron 主进程打包约束；`npm run typecheck`、`npm run lint`、相关 `npm test` 通过。
- 产出物：`.learnings/ERRORS.md` 中新增 `ERR-20260704-003`；`npm run build` 通过，`npm run dev` 启动不再报 `Cannot find module '../db/connection'`。

# Next Action
- 若继续排查启动窗口显示问题，先补一张 `WARN:COPY MODE` 的截图或完整窗口文本，再决定是渲染页内容还是 Electron/WSLg 环境提示。

# Blockers
- `WARN:COPY MODE` 的来源尚未确认，当前先不处理。

# Key Decisions
- 组卷随机化采用可复现伪随机，不用完全随机；`paperSeed` 由 `sessionId + studentId + strategyId + strategyVersion` 派生。
- 事件溯源回放仍以 `SESSION_STARTED.question_ids` 为权威；组卷算法只在创建会话时决定题目。
- 主进程本地模块依赖必须使用静态 `import`，避免 electron-vite 产物残留动态 `require('../db/connection')`。

# Relevant Files
- `src/main/domain/paper-generator.ts`
- `src/main/domain/__tests__/paper-generator.test.ts`
- `src/main/ipc/handlers/assessment.ts`
- `src/main/protocol/app-asset.ts`
