# Current State
- 任务：5.4 测评功能逐步实现，Step 2（assertStudent + assertSessionOwner + test-helpers 扩展）已完成 + 验收通过
- 当前 Vibe Coding 步骤：Step 2 `/vibe-accept 2` 通过（commit ac38f94），下一步实现 Step 3 → `/vibe-accept 3`
- 已完成：
  - `doc/features/assessment-prd.md`（5.4 Mini-PRD，两轮 Reviewer 修订全采纳）
  - `doc/features/assessment-impl.md`（10 步实现文档，Step 6 拆 6a/6b；含事件写入事务边界架构决策专节）
  - Step 1：`src/shared/types/event-payloads.ts` +EventType + 4 payload types（commit 2618583）
  - Step 2：`src/main/utils/auth-context.ts` +`assertStudent`/`assertSessionOwner`；`src/main/db/test-helpers.ts` +`seedStudent`/`seedQuestionBank`（commit ac38f94，+202/-2）

# Next Action
- 实现 Step 3（组卷服务，纯函数）：
  - `src/main/domain/paper-generator.ts`：新建，纯函数 `generatePaper(input: GeneratePaperInput): GeneratePaperOutput`，输出 50 题（42 ONLINE + 8 OFFLINE）+ 每题 (question_phase, module_type, question_type, question_order)
  - `src/main/domain/__tests__/paper-generator.test.ts`：新建，正常路径 + 题库不足 + 无效策略 + sensory filter + 确定性 + 极值（onlineQuestionCount=0）
  - 详见 `doc/features/assessment-impl.md` Step 3（line 146-190）
  - **模块均衡算法**：每 required_modules 模块按 `onlineQuestionCount / requiredModules.length` 分配（42/6=7），模块内按 question_ratio 比例抽题型；OFFLINE 8 题按 ability_tags[0] 归属模块
  - seedQuestionBank（Step 2 已实现）默认 6 模块 × {online 3 题型 × 5 + OFFLINE × 3} = 108 道，覆盖组卷余量
- 验收：`/vibe-accept 3`
- commit message：`feat(assessment): paper generator pure function + module balance`

# Blockers
- 5.3 题库未交付：组卷算法单测用 seedQuestionBank mock 题库，端到端验证推迟到 5.3

# Key Decisions
- **事件写入事务边界**：不改 `writeEvent` 签名。DB 部分经 better-sqlite3 singleton connection 自动加入调用者事务；jsonl `appendFileSync` 不在事务内，由 reducer 幂等性 + 冷启动重放兜底。详见 assessment-impl.md「事件写入事务边界」专节
- **assessment-reducer 方案 A**：独立 `assessment-reducer.ts`，幂等（每 apply* 先 SELECT 判已应用），writeEvent 后调，可复用于冷启动回放
- **Step 6 拆为 6a/6b**：6a 纯通道声明，6b createSession handler + 错误码 seed + 注册，满足单步 ≤5 文件约束
- **Step 1 EventType 放置**：`EMOTION_COLLAPSE_THRESHOLD_REACHED` 放在情绪组内（`EMOTION_RESUMED` 之后），遵循现有 EventType 按业务语义分组的风格
- **Step 2 assertStudent 独立实现**：不抽公共 helper（与 assertCaller 平行），避免重构现有代码触发回归；注释标明"逻辑同 assertCaller 的 user_account 校验"
- **测试 seed 用 uuid 派生业务键**：详见 memory `feedback_test_seed_unique_keys.md`——vitest 并行 + sql.js 场景下固定业务键触发 UNIQUE 冲突

# Relevant Files
- `doc/features/assessment-impl.md`（逐步实现的输入，Step 3-10 详述 + 依赖图 + 事务边界决策）
- `src/main/domain/paper-generator.ts`（Step 3 新建）
- `src/main/utils/auth-context.ts`（Step 2 完成，Step 6b createSession 会调 assertStudent）
- `src/main/db/test-helpers.ts`（Step 2 完成，seedQuestionBank 供 Step 3 组卷单测）
- `src/main/db/schema.sql`（v0.1.9 trigger 约束参考，5.4 无需 schema 变更）

# Repo Notes
- 本地 `main` ahead 6 未 push（ccffad7 docs + e22556a handoff + 388fbae docs sync + 2618583 step1 + f1779f6 handoff + ac38f94 step2），待用户决定推送时机
- `doc/通用基础能力评估题库2026.6.30.csv` 删除 + `.xlsx` 新增未处理（题库 csv→xlsx 替换，与 5.4 无关，待用户决定）
