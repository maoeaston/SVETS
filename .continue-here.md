# Current State
- 任务：训练（Training）功能 — /vibe-impl 已完成，待进入逐步实现
- 当前 Vibe Coding 步骤：逐步实现第 1 步（尚未开始）
- 已完成：`doc/features/training-impl.md` — 步骤化实现文档生成完成（8步，含测试设计、约束自检、Reviewer 修订）；typecheck 通过
- 产出物：`doc/features/training-impl.md`

# Next Action
- 执行 Step 1：在 `src/main/db/schema.sql` 末尾补 TRAINING_PRACTICE strategy_config seed，并在 `src/shared/types/event-payloads.ts` 的 EventType 中补 `TRAINING_STEP_RETRIED`、`TRAINING_ABORTED` 以及对应 payload 接口

# Blockers
- None

# Key Decisions
- `TRAINING_SESSION_CREATED`（PRD命名）= `TRAINING_STARTED`（代码现有 EventType），本实现统一使用 `TRAINING_STARTED`，不新增枚举值
- 步骤前序校验：startStep/completeStep 前必须确认前序步骤 status != NOT_STARTED，handler 层执行，schema 不处理乱序
- `INIT → ACTIVE` FSM 升级时机：第一个 TRAINING_STEP_STARTED 事件触发时在 reducer applyStepStarted 中同步更新 session status

# Relevant Files
- `doc/features/training-impl.md`（实现文档，Step 1 起点）
- `src/main/db/schema.sql`（末尾追加 TRAINING_PRACTICE seed）
- `src/shared/types/event-payloads.ts`（补 EventType + payload）
- `doc/features/training-prd.md`（Step 1 seed 字段参考）
