# Project Direction

- 项目总目标：先把 Pilot R0 做到可追溯、可验收，再继续修订并交付 Full Product 1.0；Pilot 不是项目终点。
- 当前产品决策：学校演示和业务闭环优先。预览版与正式版共用同一 Electron 应用，通过可配置地址的普通在线激活进入系统；教师可浏览 298 道 JOB_SKILL 题，正式演示固定使用 18 道线上题 + 6 道线下评分题。依据 `doc/specs/baseline.yaml` 与 `doc/features/school-demo-first-online-activation-prd.md`。
- 不变边界：机器合同仍使用岗位/等级/报告状态等稳定枚举，业务化只改展示映射，不改数据库枚举、事件载荷或历史报告复现；默认数据库不访问、不 promotion，临时演示库和 preview registry `INSTALLING` 边界保持不变。

# Current Milestone

- 学校演示体验收口：报告展示层已完成中文映射；当前下一阶段是题库入口、学生页文案和激活设置的体验收口，不恢复旧的 preview 专属信任链作为主流程前置。

# Current State

- 报告展示层中文映射已完成并通过 `npm run typecheck`、`npm run lint`（0 errors）、报告相关 7 个测试文件（36 tests）和 `npm run build`。
- 展示层改动文件：`src/shared/report-presentation.ts`、`src/renderer/src/components/report/report-page-state.ts`、`src/renderer/src/views/teacher/ReportListView.vue`、`src/renderer/src/views/teacher/ReportDetailView.vue`、`src/main/domain/report-export.ts`、`src/main/domain/report-html.ts`，以及对应的测试文件。
- 岗位映射 `SUPERMARKET_SHELVER → 超市理货员`；等级分两套（岗位技能用 PRD §11 文案，基础能力用"达到要求/在支持下可完成/需要加强训练"）；报告状态 `SUPERSEDED → 已被新版本替代`；内容校验 `VALID/REPAIR_REQUIRED → 内容完整/需要检查`；版本 `closure_revision → 第 N 版`；安全原因和发生环节使用共享枚举 label。
- Electron 启动排查已完成：根因是 Codex 沙箱环境（`NoNewPrivs=1` + Seccomp + 根文件系统 `nosuid`）禁止 Electron 沙箱初始化，`--no-sandbox` 等 flag 均无法绕过；沙箱外 `npm run dev` 正常启动并完成数据库初始化。应用主进程、IPC 边界和数据库初始化均健康。
- 报告 E2E（`npm run e2e:report`）有独立卡死：7/30 之前正常（3 截图），8/2 起全部 0 截图，Electron 进程 CPU 空转（R 状态 50s+）。种子数据写入成功（`action_log.jsonl` 4119 bytes），但卡在首个 `witnessTeacherViewport` 之后。回归早于本轮报告展示改动，最小 Electron 探针（launch → 首窗口 → API 就绪 → close）6 秒走完，排除启动链路问题。怀疑方向：`StartupRecovery.run()` 对种子事件的处理或报告列表渲染对种子报告状态卡住。
- 开发版激活地址 `http://127.0.0.1:4318`，授权码 `SVETS-SCHOOL-DEMO-2026`；教师 `teacher/Teacher@123`，管理员 `admin/Admin@123`，学生 `student/Student@123`。
- 未跟踪临时文件 `scripts/.tmp-diagnose-db-startup.mts` 未纳入提交，未删除；后续如需处理先确认用途。

# Next Action

- 下一原子动作：审查教师题库页 `src/renderer/src/views/teacher/QuestionBankView.vue`，在不激活题目、不修改策略、不写 preview projection 的前提下，增加"全部 298 / 当前使用 24 / 待审核 274"三个状态入口和明确的题目展开提示。

# Milestone Queue

1. [已完成] 完成报告业务语言映射，验证报告列表、详情、导出和异常状态不再暴露不必要的内部术语。
2. 在教师题库页增加"全部 298 / 当前使用 24 / 待审核 274"三个状态入口，并让题目有明确的展开/查看提示。
3. 统一学生答题按钮颜色，将"我遇到困难了"改为"需要老师帮助"，保持求助行为和权限边界不变。
4. 学生页面隐藏授权地址、应用版本、题库版本等技术信息；激活服务器地址仅在"高级设置"中保留可修改入口。
5. 排查报告 E2E 卡死（7/30 后回归，种子后 0 截图），定位是 StartupRecovery 还是报告列表渲染问题。
6. 完成学校演示人工走查后，再决定 preview registry promotion；随后恢复 BASE_ABILITY 评估，再推进 Pilot R0 与 Full Product 1.0。Full Product 草案仍是待修订，不是当前合同。

# Blockers

- 报告 E2E（`npm run e2e:report`）卡死：8/2 起在沙箱内外均无法完成，种子数据写入成功但 0 截图，Electron 进程 CPU 空转。不影响开发模式和学校演示 E2E，但阻碍报告自动化验收。排查方向：给 `StartupRecovery.run()` 加耗时日志，检查种子事件触发的启动恢复是否空转，或报告列表渲染是否对种子报告状态卡住。
- 默认数据库、生产签名和真实安装环境仍不在本轮范围。
- 旧 M5A command-boundary 脚本未纳入新增学校演示 IPC，运行时会报告 additive channels；下一轮如需收口该门禁，必须单独更新合同/fixture 并重新验收。
- 工作区还留有未跟踪的临时诊断脚本 `scripts/.tmp-diagnose-db-startup.mts`，本提交未纳入，也未删除。

# Key Decisions

- 展示层把机器枚举翻译成中文，底层稳定 ID、事件、Schema、报告快照和导出复现保持不变。
- 基础能力和岗位技能使用不同的等级展示文案，因为 PRD 对两者有独立的语义定义。
- 未知的全大写内部文本在展示边界收敛为"待确认"，避免未来新增枚举码直接漏到学校页面。
- 题库目录直接读取共享题库：298 道全部可见，24 道为当前演示使用，274 道为待审核；浏览不激活题目、不改变策略、不写 preview projection。
- 激活是学校演示业务前置，激活地址可配置但属于高级设置；授权 token、默认库和 preview registry 不向学生端暴露。

# Relevant Files

- `src/renderer/src/views/teacher/QuestionBankView.vue`
- `src/shared/report-presentation.ts`（已完成，供下一轮参考映射风格）
- `doc/features/school-demo-first-online-activation-prd.md`
- `doc/specs/baseline.yaml`
