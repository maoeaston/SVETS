# Project Direction
- 项目总目标：先把 Pilot R0 做到可追溯、可验收，再继续修订并交付 Full Product 1.0；Pilot 不是项目终点。
- 必须保留的产品决策：`doc/specs/MVP_PRD_v1.0.9-authoritative.md` 是当前唯一产品合同；AI 自动评分和 AI 岗位推荐必须进入 Full Product 1.0；`doc/specs/FULL_PRODUCT_PRD_v2.0-draft.md` 仍是未批准草案。
- 不变边界：安全红线、历史可追溯和教师责任边界不得降级；题目审核通过前不得激活；不得把 AI 作为无人工确认的录用或淘汰最终裁决者。

# Current Milestone
- Pilot R0 基础底座：F2 至 F6 已完成；当前推进 F7 报告页面框架，完成门槛是实现文档、报告 IPC/UI、权限、锁定、导出框架和验收门禁全部通过。

# Current State
- 任务：F7 Step 7 已完成教师报告读取、候选、闭环确认/替换、显式生成、安置复核和锁定 IPC 接线；真实 `xc-career-guide.db` 未被读取后写入或升级。
- 当前 Vibe Coding 步骤：逐步实现第 8 步（尚未开始）。
- 已完成：`src/main/ipc/handlers/reports.ts` 新增八个 `reports:*` 纯函数和可信 IPC 包装；`src/shared/report-presentation.ts` 新增三类报告 presentation 投影；`src/shared/types/report.ts`、`ipc-api.ts`、`src/preload/index.ts`、`src/main/ipc/index.ts` 同步 DTO、白名单和注册；`src/shared/__tests__/report-presentation.test.ts` 与 `src/main/ipc/handlers/__tests__/reports.test.ts` 覆盖 presentation、权限、读取、候选、输入校验和锁定生命周期。
- 已完成验证：Step 7 定向回归 `npx vitest run src/shared/__tests__/report-presentation.test.ts src/main/ipc/handlers/__tests__/reports.test.ts src/main/ipc/handlers/__tests__/report-integration.test.ts`（3 文件、13 测试）通过；`npm run typecheck` 通过；`npm run lint` 通过（0 error，622 条既有 warning）；`git diff --check` 通过。
- 未完成验收：Electron 实机报告闭环和真实 userData 数据库升级未执行；这些属于后续人工/集成验收，不得据此宣称 F7 已完整验收。
- 产出物：上述 Step 7 文件改动、`src/main/ipc/handlers/reports.ts`、`src/shared/report-presentation.ts`、`src/main/ipc/handlers/__tests__/reports.test.ts`、`src/shared/__tests__/report-presentation.test.ts`。

# Next Action
- 按 `doc/features/report-page-framework-impl.md` 的 Step 8 实现默认脱敏、自包含 HTML 导出；继续不得接触真实 `xc-career-guide.db`。

# Milestone Queue
1. 当前：F7 Step 8 至 Step 10 实现、独立审查与 `/vibe-accept`。
2. 后续：Q1 BASE_ABILITY 42+8 正式施测补料。
3. 后续：Q2 JOB_SKILL R1-R6 与 270 项素材/Pilot 门禁。

# Blockers
- None。

# Key Decisions
- 报告直接渲染持久化 `report_content_json`；F7 只导出默认脱敏、无脚本和外部请求的自包含 HTML，不引入 Markdown 或 PDF 报告渲染。
- BASE_ABILITY 完整报告只能从教师确认的活动 `task_closure` 生成，三类结果必须作为 typed snapshot 固化；闭环修正走 replacement，不覆盖历史 binding。
- F7 schema-v2 事件必须在 JSONL 追加前通过合同校验；自动生成失败只写异常中心，不回滚已提交 result 或已确认 safety fact。

# Relevant Files
- `doc/features/report-page-framework-impl.md`
- `src/shared/types/report.ts`
- `src/shared/types/ipc-api.ts`
- `src/preload/index.ts`
- `src/main/ipc/index.ts`
- `src/main/ipc/handlers/reports.ts`
- `src/shared/report-presentation.ts`
- `src/main/domain/report-service.ts`
- `src/main/domain/report-reducer.ts`
- `src/main/domain/report-lifecycle.ts`
