# Current State
- 任务：DRAG题拖拽渲染组件实现（drag-render）
- 当前 Vibe Coding 步骤：逐步实现第2步（共3步）
- 已完成：
  - drag-render-impl.md — 实现文档（Step 1 已取消，PRD 误描述已标注）
  - drag-drop-logic.ts — applyDrop / applyUnplace 纯函数
  - DragDropQuestion.vue — HTML5 DnD 拖拽组件
  - drag-drop-logic.test.ts — 6 个单测全通过
  - commit 2472b73 | typecheck 通过 | 590 tests passed
- 产出物：
  - src/renderer/src/components/DragDropQuestion.vue
  - src/renderer/src/components/drag-drop-logic.ts
  - src/renderer/src/components/__tests__/drag-drop-logic.test.ts
  - doc/features/drag-render-impl.md

# Next Action
- 执行 Step 2：修改 AssessmentView.vue，将 DRAG v-else 占位（lines 187–220）替换为 `<DragDropQuestion v-model="dragMap" ...>`，并在 script setup 顶部 import 组件，删除已移入子组件的 CSS 样式（.drag-area/.drag-row/.drag-label/.drag-select/.drag-image）

# Blockers
- None

# Key Decisions
- PRD 误描述：DRAG_PARTIAL 实际不返回 1 分（partial_correct_score=0，评分只有 0/2），score 类型保持 0|2，不改
- DragDropQuestion.vue 用 HTML5 原生 DnD（dragstart setData + effectAllowed='move'，dragover preventDefault）
- swap 逻辑提取为纯函数 drag-drop-logic.ts，applyDrop 返回 null 表示 no-op（不 emit）
- backgroundAssetId 来自父组件传入的 currentQuestion.mediaAssetId（SessionQuestionContent 字段已有）

# Relevant Files
- src/renderer/src/views/student/AssessmentView.vue（Step 2 主要改动）
- src/renderer/src/components/DragDropQuestion.vue（已创建，作参考）
- doc/features/drag-render-impl.md（Step 2 说明在 impl.md 的 Step 2 节）
