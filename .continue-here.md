# Current State
- 任务：5.4 测评功能 Step 9a（主进程 read handler：getSession + listSessions）已完成
- 当前 Vibe Coding 步骤：Step 9a 完成（typecheck + vitest 339/339 + build 三件套通过），Step 9b 待开始
- 已完成（Step 9a 全程）：
  - `src/shared/types/assessment.ts` — 新增 SessionQuestionContent / ListSessionsParams / SessionListItem / ListSessionsSuccess / ListSessionsResult；GetSessionSuccess 加 currentQuestion 非可选字段
  - `src/main/ipc/handlers/assessment.ts` — getSession 纯函数（STUDENT ownership / TEACHER-ADMIN assertCaller）+ readCurrentQuestionContent 辅助（题型分支脱敏：剥 expected_answer / is_correct / accepts）+ listSessions 纯函数（OPEN_SESSION_STATUSES 复用，对齐 schema ux 开放态定义）+ 2 个 ipcMain.handle 注册
  - `src/preload/index.ts` — assessment 命名空间加 getSession + listSessions
  - `src/shared/types/ipc-api.ts` — IpcApi.assessment 加 listSessions 签名（getSession 已有，GetSessionSuccess 类型扩展自动传导）
  - `__tests__/assessment-read.test.ts` — 15 测试全绿（脱敏 4 + 权限边界 7 + listSessions 4）
- 产出物：类型契约 + handler + preload + ipc-api + 单测
- 验收：npm run typecheck 通过 / npm run test 339/339 / npm run build 通过

# Next Action
- Step 9b：渲染层 5 文件 + router。建议另起 plan。
  - `src/renderer/src/stores/assessment.ts` — Pinia store 封装 assessment IPC（含 getSession / listSessions / submitAnswer / emotionInterrupt / triggerRedline / calculateResult）
  - `src/renderer/src/views/student/AssessmentView.vue` — 学生答题页（getSession 拉 session + currentQuestion → 渲染 TRUE_FALSE/SINGLE_CHOICE/DRAG → submitAnswer → emotionInterrupt）
  - `src/renderer/src/views/teacher/AssessmentCreateView.vue` — 教师发起测评（student:list + strategy:listVersions → createSession → 跳学生答题页）
  - `src/renderer/src/views/teacher/AssessmentListView.vue` — 教师列表（listSessions 非终态 → abortSession/triggerRedline/calculateResult 按钮）
  - `src/renderer/src/router/index.ts` — 注册 /student/assessment/:sessionId + /teacher/assessments/*
  - 测试策略：手工 E2E + npm run build（vue-tsc）；webapp-testing skill 或手工截图

# Blockers
- None（5.3 题库仍未交付，端到端真实答题验证继续推迟；不影响 Step 9b 渲染层搭建）
- [非本会话引入，待清理] 工作区有未提交修改：`src/main/ipc/handlers/__tests__/assessment-answer.test.ts` 加了 `variants` 脏数据校验测试（5.3 题库图片资源相关）。与 Step 9a 无关，未触碰。
- [非本会话引入，已闭环] `src/main/utils/validate-content-json.ts` + `__tests__/validate-content-json.test.ts` test-first 沉淀已完整，typecheck 通过。

# Key Decisions
- **Step 9 拆 9a + 9b**（用户决策）：9a 主进程 read handler + 单测（本步），9b 渲染层 + router + 手工 E2E（下一步）。各自独立 commit + vibe-accept。
- **getSession 扩展返回 currentQuestion 正文**（用户决策）：一次 IPC 拿全 session 状态 + 当前题正文，避免额外 round-trip。expected_answer / is_correct / accepts 在主进程侧剥离（脱敏）。题型分支字段映射：TRUE_FALSE→variants；SINGLE_CHOICE→options；DRAG→dragItems/dropZones/scoringMode。
- **listSessions 全部非终态**（用户决策）：复用 handler 既有的 `OPEN_SESSION_STATUSES` 常量（INIT/ACTIVE/EMOTION_INTERRUPTED/SUSPENDED_REVIEW_REQUIRED/OFFLINE_PENDING），对齐 schema `ux_assessment_one_open_session_per_student_task_strategy` 的 WHERE 子句。TEACHER/ADMIN only；可选 studentId 筛选；JOIN student_profile 取 studentName；ORDER BY updated_at DESC。
- **schema assessment_session 无 created_at 列**（Step 9a 发现）：v0.1.9 schema 只有 updated_at（默认 datetime('now') 且无 trigger 自动刷新，等同创建时间）。getSession/listSessions SQL 用 `updated_at AS created_at` alias 填充 SessionDetail.createdAt / SessionListItem.createdAt。后续 schema 版本升级时考虑补 created_at 列。
- **reducer applySessionStarted 不设 current_question_id**（Step 9a 实测发现）：session 创建时该字段为 NULL，仅 applyAnswerSubmitted 推进。getSession 测试需显式 UPDATE current_question_id 才能验证 currentQuestion 解析路径。生产路径：学生首次 submitAnswer 后 current_question_id 才非空。**[!] 9b 学生答题页设计影响**：首次进入 session（currentQuestionId = null）需要触发"开始答题"或自动 submitAnswer 推进到第一题，否则 currentQuestion 为 null 无法渲染题目。这是 9b 必须解决的设计点。
- **getSession 权限模型**：STUDENT 走 assertStudent + assertSessionOwner（仅自己可读）；TEACHER/ADMIN 走 assertCaller（跨学生可读）。不基于 status 拦截——终态 session（REDLINE_HALTED 等）仍可读（currentQuestion 仍按 current_question_id 解析）。
- **content_json 损坏不破坏读路径**：JSON.parse 失败 → currentQuestion = null + console.warn。学生看到"无当前题"，教师列表里看到 session 状态。
- **GetSessionSuccess 加非可选字段 currentQuestion 是破坏性变更，但零消费点**：grep 全仓确认 9b 之前无任何文件 import GetSessionSuccess。下游传导风险为零。
- **memory: schema 缺 created_at 是 v0.1.9 遗漏**（备忘，待后续 schema 升级修）：record 一条 project memory 避免下次又被坑。

# Relevant Files
- `doc/features/assessment-impl.md`（Step 9 规范 line 483-516）
- `src/main/ipc/handlers/assessment.ts`（getSession / listSessions 已实现 + 注册；OPEN_SESSION_STATUSES line 84）
- `src/shared/types/assessment.ts`（SessionQuestionContent / ListSessionsParams / SessionListItem 等新类型）
- `src/main/ipc/handlers/__tests__/assessment-read.test.ts`（15 测试，含 setCurrentQuestion helper）
- `src/preload/index.ts`（9 个 assessment 通道全声明）
- `src/shared/types/ipc-api.ts`（IpcApi.assessment 完整签名）
- Step 9b 待新建：`src/renderer/src/stores/assessment.ts` + 3 个 view + router 注册
