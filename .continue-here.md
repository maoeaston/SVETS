# Current State
- 任务：上一任务（学生档案管理 feat/student-profile）已通过 PR #1 squash 合并到 main；下一任务（5.2 任务与岗位配置 / strategy_config CRUD）待启动
- 当前 Vibe Coding 步骤：上一功能已走完全流程（/vibe-feature → /vibe-impl → 实现 → /vibe-accept → PR 合并 main）；下一功能尚未启动 /vibe-feature
- 已完成：
  - PR #1 squash commit `339d66e` 上 main，含：auth 登录（PRD §3/§5.0）、学生档案管理（PRD §5.1，教师 CRUD + 自动开通学生账号 + 归档联动 user_account.status）、Electron E2E 基础设施（Node 版 Playwright `_electron.launch`）、`app.setName` 修复 userData 路径一致性
  - 自动验证（合并前）：typecheck ✅ build ✅ vitest 89/89 ✅ E2E 9/9 ✅
- 产出物：
  - main HEAD：`339d66e feat: student profile, role-based auth, Electron E2E infra (#1)`
  - PR：https://github.com/maoeaston/SVETS/pull/1（state: MERGED）
  - 远端 `feat/student-profile` 保留兜底；本地已删

# Next Action
- `git checkout -b feat/strategy-config`，然后运行 `/vibe-feature` 启动 5.2 任务与岗位配置的 Mini-PRD

# Blockers
- None（5.2 schema 已就绪 `strategy_config` 表，无 schema 变更）

# Key Decisions
- 5.2 选为下一功能：5.4 测评 + 5.5 训练都硬依赖 `strategy_config`（组卷规则、阈值、版本锁定），不做 5.2 就动 5.4 等于硬编码题量，违反 AGENTS.md「单一策略配置源」原则
- E2E 走 Node 版 Playwright `_electron.launch`（Python Playwright 不支持 Electron API）；基础设施已在 `scripts/e2e/` 落地，后续功能 E2E 可复用
- main 提交规范：squash merge 后 main 一个 PR 一个 commit；新功能分支命名 `feat/<功能>`

# Relevant Files
- `doc/炫灿-职途向导系统_MVP_PRD_v1.0.4.md`（§5.2 任务与岗位配置 + §7.6 策略配置版本锁定 + §7.2 统一百分制模型 — Mini-PRD 依据）
- `src/main/db/schema.sql`（`strategy_config` 表定义 — 5.2 实现起点）
- `AGENTS.md`（架构原则「单一策略配置源」+「安全红线优先级」）
