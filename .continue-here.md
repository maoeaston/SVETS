# Project Direction
- 项目总目标：先把 Pilot R0 做到可追溯、可验收，再继续修订并交付 Full Product 1.0；Pilot 不是项目终点。
- 必须保留的产品决策：`doc/specs/MVP_PRD_v1.0.9-authoritative.md` 是当前唯一产品合同；AI 自动评分和 AI 岗位推荐必须进入 Full Product 1.0；`doc/specs/FULL_PRODUCT_PRD_v2.0-draft.md` 仍是未批准草案。
- 不变边界：安全红线、历史可追溯和教师责任边界不得降级；题目审核通过前不得激活；不得把 AI 作为无人工确认的录用或淘汰最终裁决者。

# Current Milestone
- Pilot R0 基础底座：F2 至 F7 已完成本地验收；下一阶段进入 Q1 BASE_ABILITY 42+8 正式施测补料，或按用户要求先提交/推送 F7 收口提交。

# Current State
- 任务：F7 Step 10 独立审查、全量门禁和隔离临时 userData 的 Electron 见证已完成。
- 当前 Vibe Coding 步骤：F7 报告页面框架 Step 1-10 本地验收完成，等待用户决定是否提交/推送本轮收口改动。
- 已完成：修复上一轮独立审查发现的 P1（`BASE_RESULTS` 确认闭环不再隐式生成 BASE 报告），新增 store 回归测试；新增 `scripts/e2e/report-flow.mjs` 和 `npm run e2e:report`，用临时 `/tmp/svets-report-e2e-*` userData 启动真实 Electron，覆盖教师报告列表/详情、三尺寸 `innerWidth/innerHeight`、锁定、脱敏 HTML 导出、ADMIN/STUDENT deep link + 直接 IPC 禁止、重启恢复和恶意文本不执行。
- 已完成验证：`npm run typecheck` 通过；`npm run lint` 通过（0 error，661 条既有 Vue 格式 warning）；`npm test` 通过（104 files / 1026 tests）；`npm run build` 通过；`npm run db:f7:verify` 通过（4 files / 21 tests）；`npm run e2e:report` 通过，最终证据目录 `/tmp/svets-report-e2e-QaK2nr`；`node --check scripts/e2e/report-flow.mjs` 通过。
- 已完成审查：Round 1 独立 Reviewer 发现 1 个 P1；已修复；Round 2 独立 Reviewer 对当前未提交 diff 给出 `PASS`，P0/P1 清零。P2 为 E2E fixture 用 `sqlite3` CLI 而非原伪代码 `sql.js`，已在实现文档记录偏差并增加 realpath preflight。
- 未执行/保留：默认 `npm run db:verify` 会指向真实运行库 `xc-career-guide.db`，按本轮约束未执行；目标学校 Windows 环境的保存对话框、杀毒软件和路径策略仍需现场见证。
- 产出物：当前 HEAD commit `feat(report): add teacher report workspace`；本轮 Step 10 改动尚未提交。

# Next Action
- 用户确认后提交/推送 F7 Step 10 收口改动；若继续开发，则进入 Q1 BASE_ABILITY 42+8 正式施测补料。继续不得接触真实 `xc-career-guide.db`，除非用户明确授权运维。

# Milestone Queue
1. 当前：提交/推送 F7 Step 10 收口改动（待用户确认）。
2. 后续：Q1 BASE_ABILITY 42+8 正式施测补料。
3. 后续：Q2 JOB_SKILL R1-R6 与 270 项素材/Pilot 门禁。

# Blockers
- None。

# Key Decisions
- 报告直接渲染持久化 `report_content_json` 的 shared presentation；页面和导出不得各自解释 raw report JSON，不引入 Markdown 或 PDF 报告渲染。
- F7 只导出默认脱敏、无脚本和外部请求的自包含 HTML；取消不写事件，成功导出创建独立 `REPORT_FILE` asset，`LOCKED` 报告导出后仍保持 `LOCKED`。
- BASE_ABILITY 完整报告只能从教师确认的活动 `task_closure` 生成；闭环修正走 replacement，不覆盖历史 binding。
- 报告候选中的 `BASE_RESULTS` 只能确认闭环；只有确认后出现的 `BASE_CLOSURE` 候选才能触发 BASE_ABILITY 报告生成。

# Relevant Files
- `doc/features/report-page-framework-impl.md`
- `doc/specs/baseline.yaml`
- `doc/specs/project-invariants.md`
- `doc/ai/vibe-workflow-contract.md`
- `.agents/skills/vibe-accept/SKILL.md`
- `vibe-coding-skills-v2/commands/vibe-accept.md`
- `src/renderer/src/views/teacher/ReportListView.vue`
- `src/renderer/src/views/teacher/ReportDetailView.vue`
- `src/main/window-config.ts`
- `scripts/e2e/report-flow.mjs`
