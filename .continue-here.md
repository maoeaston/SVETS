# Current State
- 任务：训练（Training）功能 — /vibe-feature 已完成，待进入 /vibe-impl
- 当前 Vibe Coding 步骤：/vibe-impl（PRD 已就绪，尚未生成实现文档）
- 已完成：`doc/features/training-prd.md` — Mini-PRD 编写完成，Reviewer subagent 审查通过，6处意见已全部修订；typecheck 通过
- 产出物：`doc/features/training-prd.md`

# Next Action
- 运行 `/vibe-impl` 生成 `doc/features/training-impl.md`（步骤化实现计划，每步对应一个 commit，含测试设计）

# Blockers
- None

# Key Decisions
- 训练步骤在 createSession 时一次性插入4条 training_step_record（NOT_STARTED），total_step_count 固定为 4
- EMOTION_INTERRUPTED 状态本次不实现；本次 FSM 路径仅含 INIT / ACTIVE / COMPLETED / REDLINE_HALTED / ABORTED
- 安全红线熔断后，应用层需级联将 IN_PROGRESS 步骤置 FAILED（schema trigger 不级联）

# Relevant Files
- `doc/features/training-prd.md`
- `src/main/db/schema.sql`（training_session + training_step_record 定义）
- `doc/features/assessment-impl.md`（参考 impl 文档结构）
- `src/main/ipc/handlers/assessment.ts`（参考 handler 写法）
