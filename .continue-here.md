# Current State
- 任务：学生档案管理功能（教师创建/编辑/归档学生档案 + 开通学生登录账号）— **E2E 验收完成，已推送远端**
- 分支：`feat/student-profile` 已上 GitHub（origin/feat/student-profile），本地与远端同步，共 16 个 commit ahead of origin/main
- PR 入口：https://github.com/maoeaston/SVETS/pull/new/feat/student-profile
- 自动验证：typecheck ✅ build ✅ vitest 89/89 ✅（上一会话末尾）+ E2E 9/9 ✅（本会话两次跑全过）
- commit 链（本会话新增 2 个）：
  - dae2027 test(e2e): add playwright electron smoke + student-profile 9-step
  - 424a14e fix(main): set app name for consistent userData path
  - 0111590 chore(handoff): update .continue-here.md for session close
  - 0997e96 chore(scripts): add WSL/Linux-compatible seed script (.mjs)
  - 3fb4c86 fix(student): defer error code seeding to first IPC call
  - f096283 feat(student): implement student create/edit form view
  - 230d43d feat(student): add student list view, teacher layout, routes with form placeholder
  - 0517620 feat(student): implement student:update and student:archive handlers
  - d3e0cc6 feat(student): implement student:get and student:list handlers
  - 1e8dcbe feat(student): implement student:create handler with transaction and audit
  - fb46b5f feat(student): add DBAdapter port, IPC types, and caller validation
  - de3fbf8 feat(student): add sensory_profile validator and shared types
  - 2d29de5 docs(student): add student profile PRD and implementation plan

# Next Action
- 创建 PR：访问上面的 GitHub 链接，或 `gh pr create --base main --head feat/student-profile`；PR 描述引用 PRD（doc/features/student-profile-prd.md）+ 9 步 E2E 验收结果
- E2E 残留清理（可选）：`~/.config/xc-career-guide/data/xc-career-guide.db` 里有 `e2e_1782883821512`（DISABLED）+ `E2E学生_1782883821512`（ARCHIVED）；`~/.config/Electron/`（修复前 Playwright 用的隔离 DB）整个目录可删
- 未跟踪文件 `doc/MVP 后续全量功能演进备忘录.md` 非本任务产物，需用户决定保留/删除/合并到 doc/
- 下一功能：按 AGENTS.md 流程，先 `/vibe-feature` 生成 Mini-PRD 再实现

# Blockers
- 无

# Key Decisions
- DBAdapter 端口/适配器（生产 SqliteAdapter 包 better-sqlite3，测试 MemoryAdapter 用 sql.js/WASM）—— 根除 Node/Electron ABI 不匹配，handler 测试完全不触碰原生模块
- 同 UUID 复用：user_account.user_id === student_profile.student_id（schema 无 FK，应用层关联）
- 审计走 error_event_log（error_category=SYSTEM 因 CHECK 枚举不含 STUDENT_PROFILE；related_aggregate_type=STUDENT_PROFILE；INFO 用 recovery_status=IGNORED，ERROR 用 UNRESOLVED）
- seed 脚本拆双版本：原 .ts 留 Windows，新 .mjs 给 WSL/Linux（sqlite3 CLI + node:crypto pbkdf2）
- **E2E 用 Node 版 Playwright `_electron.launch`**（commit dae2027）：Python Playwright 不支持 Electron；用 `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1` 跳过浏览器下载；4 次 Electron 启动覆盖 9 步流程
- **`app.setName('xc-career-guide')` 修复 userData 路径**（commit 424a14e）：直接启动 `out/main/index.js`（绕过 electron-vite dev）时 `app.getName()` 返回 "Electron"，userData 错落到 `~/.config/Electron/`，与 dev 模式 `~/.config/xc-career-guide/` 不一致。修复后所有启动方式路径统一

# Relevant Files
- src/main/index.ts（加了 app.setName，userData 路径修复点）
- src/main/ipc/handlers/student.ts（student:* handler 核心）
- src/renderer/src/views/teacher/StudentFormView.vue（新建/编辑表单）
- src/renderer/src/views/teacher/StudentListView.vue（列表页）
- scripts/seed-dev-accounts.mjs（WSL 重新 seed 入口）
- **scripts/e2e/smoke.mjs（Electron 启动 sanity）**
- **scripts/e2e/student-profile.mjs（9 步 E2E，可复用）**
- doc/features/student-profile-impl.md（实现文档，含 Step 7 E2E 9 步清单）
