# Project Direction
- 项目总目标：先把 Pilot R0 做到可追溯、可验收，再继续修订并交付 Full Product 1.0；Pilot 不是项目终点。
- 必须保留的产品决策：`doc/specs/MVP_PRD_v1.0.9-authoritative.md` 是当前唯一产品合同；AI 自动评分和 AI 岗位推荐必须进入 Full Product 1.0；`doc/specs/FULL_PRODUCT_PRD_v2.0-draft.md` 仍是未批准草案。
- 不变边界：安全红线、历史可追溯和教师责任边界不得降级；题目审核通过前不得激活；不得把 AI 作为无人工确认的录用或淘汰最终裁决者。

# Current Milestone
- Pilot R0 基础底座：F2 至 F6 已完成；当前推进 F7 报告页面框架，完成门槛是实现文档、报告 IPC/UI、权限、锁定、导出框架和验收门禁全部通过。

# Current State
- 任务：F7 `/vibe-impl` 已完成，尚未开始修改业务代码。
- 当前 Vibe Coding 步骤：逐步实现第 1 步（准备开始）。
- 已完成：`doc/features/report-page-framework-impl.md` 已生成，版本 `v1.0.2`，状态为 `CONDITIONAL_PASS`。
- 已完成：独立 Reviewer Round 1 的 2 个 P0、3 个 P1 已全部修订；Round 2 新增的窗口尺寸 P1 条件已写入 Step 9/Step 10，但按最多两轮审查规则未追加第三轮，不能声明 Reviewer `PASS`。
- 已完成验证：`npm run typecheck` 通过；`npm run lint` 通过（0 error，622 条既有 warning）；`npm test` 通过（85 files / 945 tests）；`npm run docs:index:update` 已运行；`npm run docs:index:check` 通过；`git diff --check` 通过。
- 产出物：`doc/features/report-page-framework-impl.md`；`doc/index.md` 已同步索引。

# Next Action
- 按 `doc/features/report-page-framework-impl.md` 的 Step 1 实现报告、IPC 与事件运行时合同，并先执行该步列出的定向测试、`npm run typecheck` 和 `git diff --check`。

# Milestone Queue
1. 当前：F7 报告页面框架实现设计、逐步实现与 `/vibe-accept`。
2. 后续：Q1 BASE_ABILITY 42+8 正式施测补料。
3. 后续：Q2 JOB_SKILL R1-R6 与 270 项素材/Pilot 门禁。

# Blockers
- None。

# Key Decisions
- 报告直接渲染持久化 `report_content_json`；F7 只导出默认脱敏、无脚本和外部请求的自包含 HTML，不引入 Markdown 或 PDF 报告渲染。
- BASE_ABILITY 完整报告只能从教师确认的活动 `task_closure` 生成，三类结果必须作为 typed snapshot 固化；闭环修正走 replacement，不覆盖历史 binding。
- 红线来源禁止普通报告；安全报告使用 `safety-termination-report-v1.0`，Pilot、未完成和 JOB_SKILL 报告不得输出正式就业安置结论。

# Relevant Files
- `doc/features/report-page-framework-impl.md`
- `src/shared/types/json-schemas.ts`
- `src/shared/types/report.ts`
- `src/shared/types/event-payloads.ts`
- `src/main/domain/report-contract.ts`
- `src/main/domain/report-canonical.ts`
- `src/main/domain/__tests__/report-contract.test.ts`
- `src/main/domain/__tests__/report-canonical.test.ts`
