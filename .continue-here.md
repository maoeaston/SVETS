# Current State
- 任务：多设备 v2.2 M3 Grant/Assignment Step M3-5 Assignment reducer 与 DB 投影已完成。
- 当前 Vibe Coding 步骤：逐步实现 Step M3-6。
- 已完成：
  - `src/main/domain/assignment-reducer.ts` — 新增 `applyAssignmentEvent`，投影 `ASSIGNMENT_CREATED`、`ASSIGNMENT_STUDENT_CONFIRMED`、`GRANT_REBOUND`、`ASSIGNMENT_RELEASED`，并推进 assessment assignment phase。
  - `src/main/domain/assessment-reducer.ts` — 接入 `ASSIGNMENT_ASSESSMENT_STARTED`，要求 `STUDENT_CONFIRMED -> ONLINE_IN_PROGRESS`。
  - `src/main/domain/__tests__/assignment-reducer.test.ts` — 覆盖创建、确认、启动、重复 apply、乱序 no-op、rebind 幂等和 release。
  - `src/main/domain/__tests__/assessment-reducer.test.ts` — 调整 M3 D1 生效后的测试前置 phase，保留旧 reducer 行为测试。
  - 验证：`npm test -- src/main/domain/__tests__/assignment-reducer.test.ts src/main/domain/__tests__/assessment-reducer.test.ts` 通过；`npm run typecheck` 通过；`npm run lint` 通过（0 error，保留既有 160 个 Vue warning）；`git diff --check` 通过。
- 产出物：
  - `src/main/domain/assignment-reducer.ts`
  - `src/main/domain/__tests__/assignment-reducer.test.ts`
  - `src/main/domain/assessment-reducer.ts`
  - `src/main/domain/__tests__/assessment-reducer.test.ts`

# Next Action
- 按 `doc/features/multi-device-v2.2-migration-impl.md` 的 Step M3-6 新增 `src/main/ipc/handlers/assignment.ts`、`src/main/ipc/handlers/__tests__/assignment.test.ts` 并在 `src/main/ipc/index.ts` 注册 assignment IPC handler。

# Blockers
- Full `npm test` 可能仍失败：Step M3-7 尚未收窄旧 `assessment:startSession`，旧 IPC 仍可能尝试绕过 assignment 直接启动新 M3 `PREPARED` assessment。

# Key Decisions
- `ASSIGNMENT_ASSESSMENT_STARTED` 作为 M3 合法启动事件接入 assessment reducer；旧 `SESSION_FIRST_QUESTION_ACTIVATED` 的外部入口收窄留给 Step M3-7。
- Assignment reducer 不开启事务，保持与现有 reducer 一致，由 handler 在 `writeEvent` + reducer 外层包事务。
- Rebind 投影按旧 grant 过期、新 grant 插入、assignment 改指新 grant 的顺序执行，并同步更新 assignment `device_id` 以满足 D10。

# Relevant Files
- `doc/features/multi-device-v2.2-migration-impl.md`
- `src/shared/types/assignment.ts`
- `src/main/domain/assignment-reducer.ts`
- `src/main/ipc/handlers/assignment.ts`
- `src/main/ipc/handlers/__tests__/assignment.test.ts`
- `src/main/ipc/index.ts`
