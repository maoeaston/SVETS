# Project Direction

- 当前唯一产品合同：`doc/specs/MVP_PRD_v1.0.9-authoritative.md`。
- M4 安全聚合三元键已完成 Step 2A 独立审查与最终验收；当前工程基线为 `schema v0.1.17-multi-device-m4-safety-rekey (ACCEPTED_STEP_2A)`。
- 默认 `xc-career-guide.db` 未读取或修改；工作区仍含 BASE_ABILITY 与其他无关 dirty 改动，必须保留。

# Current Milestone

- Q1 Step 2A M4 Safety Re-key：**PASS / CLOSED**。
- Step 2B M5A Command Bus Boundary 已解除前置阻断，但尚未开始；Step 2C 仍在其后，Q1 Step 3 继续等待 2B/2C。

# Closure Evidence

- 独立 `/vibe-review code`：PASS，P0/P1/P2 = 0。
- `/vibe-accept step M4-6 / Step 2A`：PASS。
- M4 定向：7 文件 / 56 项 PASS；`db:m4:verify`：7 文件 / 36 项 PASS；SQL inventory：49 hits PASS。
- 原生：Electron 41.9.1 / ABI 145 / SQLite 3.53.2，真实 migration、hash、plan、integrity、backup/restore、preflight 边界 PASS。
- UI：`xvfb-run` + `SVETS_E2E=1` + 唯一临时 userData，跨岗位隔离、同三元红线与 replacement PASS。
- 项目门禁：typecheck、lint（0 error/661 warnings）、full test（112 文件/1081 项）、build、docs index、diff check PASS。

# Next Action

在用户明确授权开始后，使用 `vibe-feature` 为 Step 2B M5A Command Bus Boundary 定义/复核当前 Mini-PRD，再使用 `vibe-impl` 生成原子实施计划。不得把 M4 证据混入 Step 2B 的实现或验收。

# Safety Boundaries

- 不自动 commit、push、stash、reset、迁移或修复默认数据库。
- Step 2B/2C、生产部署和默认数据库运维仍不在 M4 已验收范围内。
- 面对当前 dirty worktree，按路径隔离新工作，先读 `AGENTS.md`、`baseline.yaml`、项目不变量和工作流合同。
