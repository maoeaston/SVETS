# Current State
- 任务：5.4 测评功能逐步实现，Step 4（等级判定纯函数）已完成 + 验收通过
- 当前 Vibe Coding 步骤：Step 4 `/vibe-accept 4` 通过（commit 61503e3），下一步实现 Step 5 → `/vibe-accept 5`
- 已完成：
  - `doc/features/assessment-prd.md` + `doc/features/assessment-impl.md`
  - Step 1（commit 2618583）：EventType + 4 payload types
  - Step 2（commit ac38f94）：assertStudent / assertSessionOwner + seedStudent / seedQuestionBank
  - Step 3（commit 558710c）：paper-generator 纯函数（模块均衡策略 B + 确定性排序 + sensory 占位）
  - Step 4（commit 61503e3）：level-judge 纯函数（safety > 模块兜底 > 情绪兜底 > 分数阈值，9 用例映射 §17.6.1）

# Next Action
- 实现 Step 5（assessment-reducer，事件 → 投影，幂等）—— **高风险步骤**，完成后跑 `/vibe-review`：
  - `src/main/domain/assessment-reducer.ts`：新建，`applyAssessmentEvent(db, event)` 按 event_type switch 分发
  - `src/main/domain/__tests__/assessment-reducer.test.ts`：新建，含冷启动重放 + 孤儿事件幂等关键用例
  - 幂等性硬约束：每个 apply* 先 SELECT 判已应用，重复 no-op（冷启动回放复用同一 reducer）
  - EventType 分发：SESSION_STARTED / ANSWER_SUBMITTED / EMOTION_INTERRUPTED / EMOTION_RESUMED / EMOTION_COLLAPSE_THRESHOLD_REACHED(no-op) / SESSION_COMPLETED / SESSION_ABORTED / REDLINE_TRIGGERED / RESULT_CALCULATED
  - 详见 `doc/features/assessment-impl.md` Step 5（line 245-283）
- 验收：`/vibe-accept 5`
- commit message：`feat(assessment): idempotent reducer for event projection`

# Blockers
- 5.3 题库未交付：组卷/等级判定单测用 mock 数据，端到端验证推迟到 5.3

# Key Decisions
- **事件写入事务边界**：不改 `writeEvent` 签名。DB 部分经 better-sqlite3 singleton connection 自动加入调用者事务；jsonl 不在事务内，由 reducer 幂等性 + 冷启动重放兜底。详见 assessment-impl.md「事件写入事务边界」专节
- **assessment-reducer 方案 A**：独立 `assessment-reducer.ts`，幂等（每 apply* 先 SELECT 判已应用），writeEvent 后调，可复用于冷启动回放
- **Step 6 拆为 6a/6b**：6a 纯通道声明，6b createSession handler，满足单步 ≤5 文件约束
- **Step 3 模块归属用 question_bank.module_type 列**（非 impl.md 提的 ability_tags[0]）：schema 权威 + 已有索引。如 review 要求遵循 impl.md 字面需重构
- **Step 3 策略 B 模块均衡优先**：每模块精确 quota，全局 ratio 近似（14:14:14 → 全局 18:12:12）。遵循 impl.md"模块均衡"指示
- **Step 4 边界语义**：模块兜底严格 `<`（raw/max < threshold，边界 == 不触发），情绪兜底 `>=`（collapseCount >= threshold，等号触发）。多模块兜底命中按 AbilityTag 枚举顺序取第一个，保证确定性
- **测试 seed 用 uuid 派生业务键**：详见 memory `feedback_test_seed_unique_keys.md`

# Relevant Files
- `doc/features/assessment-impl.md`（Step 5-10 详述 + 依赖图 + 事务边界决策）
- `src/main/domain/assessment-reducer.ts`（Step 5 新建）
- `src/main/domain/level-judge.ts`（Step 4 完成）
- `src/main/domain/paper-generator.ts`（Step 3 完成，Step 6b createSession 会调）
- `src/main/utils/auth-context.ts`（Step 2 完成）
- `src/main/db/schema.sql`（v0.1.9 trigger 约束参考，5.4 无需 schema 变更）
- `src/shared/types/event-payloads.ts`（Step 1 EventType + payload，Step 5 reducer switch 依赖）

# Repo Notes
- 本地 `main` ahead 10 未 push（4 docs/handoff + step1-4 feat + step1-4 handoff + step3 handoff），待用户决定推送时机
- `doc/通用基础能力评估题库2026.6.30.csv` 删除 + `.xlsx` 新增未处理（与 5.4 无关，待用户决定）
