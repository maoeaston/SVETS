# Project Direction
- 项目总目标：先把 Pilot R0 做到可追溯、可验收，再继续修订并交付 Full Product 1.0；Pilot 不是项目终点。
- 必须保留的产品决策：`doc/specs/MVP_PRD_v1.0.9-authoritative.md` 是当前唯一产品合同；Q1 采用方案 A（独立批准后激活选定 42+8，再做真实课堂试测）；`doc/specs/FULL_PRODUCT_PRD_v2.0-draft.md` 仍是未批准草案。
- 不变边界：安全红线、历史可追溯、教师责任和多设备授权链不得降级；Pilot ACTIVE 不等于正式诊断、就业安置或对外发布；执行激活的 ADMIN 不能自授批准。

# Current Milestone
- Q1 BASE_ABILITY 42+8 的 Step 2 硬前置链：Step 2A M4 Safety Re-key → Step 2B M5A Command Bus Boundary → Step 2C M5B Event Batch + startupRecovery；三者全部验收 `PASS` 前不得进入 Step 3。

# Current State
- 任务：实施 Step 2A M4 安全聚合三元键升级，M4-1 至 M4-3 已完成并提交。
- 当前 Vibe Coding 步骤：逐步实现第 4 步（M4-4）。
- 已完成：M4-3 将 fresh `schema.sql` 升级至 v0.1.17；F7→M4 启动编排在 DDL 前完成结构分类、历史预检及 F7/M4 分阶段配套备份，成功后才允许 seed/recovery。`npm run db:m4:verify`（29 passed）、内容包 ledger 回归（4 passed）、typecheck、lint（0 error、661 条既有 warning）、docs index 和 diff check 均通过。生产 Electron 的真实启动与 `better-sqlite3` 备份演练因本机 ABI 不匹配尚未运行；默认运行库未修改。
- 产出物：`src/main/db/migration-startup.ts`、`src/main/db/migration-backup.ts`、`src/main/db/safety-rekey-migration.ts`、相关 M4 测试与 `scripts/verify-m4-migration.mjs`。

# Next Action
- 阅读 M4-4 实施步骤和 production safety-SQL inventory，逐项定位尚未三元化的会话创建、评分阻断与训练步骤级联查询。

# Milestone Queue
1. 实施并 `/vibe-accept` Step 2A M4 的 M4-4 至 M4-6；完成后独立验收 Step 2A。
2. Step 2A `PASS` 后依次交付 Step 2B、Step 2C；Step 2C 后更新基线、重跑 Step 1 验收并重新独立审查 42+8 实施计划。
3. Step 2 全链 `PASS` 后实施 Q1 Step 3-10，再收口 Pilot R0；Full Product 1.0 仍待正式裁决。

# Blockers
- 真实 Electron 启动与 `better-sqlite3` 配套备份演练待运行环境 ABI 适配；不阻断 M4-4。

# Key Decisions
- 顺序固定为 M4 三元安全身份 → M5A 最终命令边界 → M5B batch runtime；三个阶段各自独立 R3，不合并实施。
- M4 目标为 schema v0.1.17，严格排在 F7 后；历史三元错绑在 DDL 前失败关闭，不自动修复。
- M4-4 只改生产运行查询，不新增 IPC 参数、事件类型或 UI 输入；`job_code` 只能取自可信 strategy/session/incident 事实。

# Relevant Files
- `doc/features/multi-device-m4-safety-rekey-impl.md`
- `doc/features/multi-device-m4-safety-sql-inventory-v1.json`
- `src/main/ipc/handlers/assessment.ts`
- `src/main/ipc/handlers/training.ts`
- `src/main/domain/ability-scoring.ts`
- `src/main/domain/operation-scoring.ts`
