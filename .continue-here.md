# Current State
- 任务：实操评分（Operation Scoring）— 5 步全部实现完成
- 当前 Vibe Coding 步骤：全部 Step 完成，待 /vibe-accept 正式验收 + 手工冒烟测试

# Next Action
- 运行 `/vibe-accept` 正式验收
- 手工冒烟（OFFLINE_PENDING session → 实操评分流程，见 impl.md 回归验收清单）
- 通过后：git push https://maoeaston@github.com/maoeaston/SVETS.git main

# Blockers
- None

# Commits（本功能，共 5 步）
- feat(operation-scoring): 共享类型 + payload 扩展（9枚举 + rubric + IPC契约）
- feat(operation-scoring): reducer 处理 OFFLINE_SCORE_SUBMITTED → offline_score_record
- feat(operation-scoring): IPC handler — 批量提交 + OPERATION_PASS_RATE 计算
- feat(operation-scoring): preload 白名单 + IPC 类型声明
- feat(operation-scoring): Vue 视图 + 路由 + 列表入口

# 验收状态
- [x] npm run typecheck — 通过
- [x] npm run build — 通过（OperationScoringView 已打包）
- [x] npx vitest run — 453/456（3 个失败为拖拽题 seed 预存失败，与本功能无关）
- [ ] 手工冒烟（见 impl.md 回归验收清单第 1-7 条）

# Key Decisions（已落地）
- D1: criterion_scores 长度为 1（TASK_OPERATION 单维度），criterion_id 用 task_operation_code
- D3: 红线中途触发后不生成 OPERATION_PASS_RATE result_record
- D5: 教具清单确认是 session 级一次性操作，9 条记录统一设 tool_checklist_confirmed=1
- 等级判定：不复用 judgeLevel，直接 if-else（从 strategy_config 读 threshold）
- OfflineScoreSubmittedPayload +3 字段（scoring_rubric_json / observation_note / tool_checklist_confirmed）
- ResultCalculatedPayload.breakdown 扩展为联合类型（AbilityScorePayload | OperationPassRatePayload）

# Relevant Files（已交付）
- src/shared/types/operation-scoring.ts（新建）
- src/shared/types/event-payloads.ts（扩展）
- src/main/domain/assessment-reducer.ts（新增 applyOfflineScoreSubmitted）
- src/main/ipc/handlers/operation-scoring.ts（新建）
- src/main/ipc/index.ts（注册 handler）
- src/preload/index.ts（白名单 +2）
- src/shared/types/ipc-api.ts（类型声明 +2）
- src/renderer/src/views/teacher/OperationScoringView.vue（新建）
- src/renderer/src/router/index.ts（新增路由 assessments/:sessionId/scoring）
- src/renderer/src/views/teacher/AssessmentListView.vue（OFFLINE_PENDING 入口按钮）
