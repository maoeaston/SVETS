# Current State
- 任务：多设备 v2.2 M3 Grant/Assignment 已完成 Step M3-8、M3-9、M3-10 的代码、文档与自动验证。
- 当前 Vibe Coding 步骤：M3 自动工程闸门已完成；剩余仅真实 Electron 手工冒烟与后续 `/check`/合并流程。
- 已完成：
  - Step M3-8：
    - 新增 `src/main/db/__tests__/schema-m3-grant-assignment.test.ts`，集中覆盖 D1、D9、D10、D11 负向约束。
    - `src/main/db/test-helpers.ts` 新增 M3 grant/assignment 测试夹具。
    - `src/main/ipc/handlers/__tests__/assignment.test.ts` 补 release 后不能再次 start、grant 过期时 start 拒绝。
  - Step M3-9：
    - `src/renderer/src/stores/assessment.ts` 增加 `ASSIGNMENT_REQUIRED` / `STUDENT_CONFIRMATION_REQUIRED` 中文错误映射。
    - `src/renderer/src/views/student/AssessmentView.vue` 对 `PREPARED` / `ASSIGNED` / `STUDENT_CONFIRMED` 显示 assignment 阻断状态，不进入答题态、不自动伪造 assignment。
  - Step M3-10：
    - 当前工程基线同步为 `schema v0.1.15-multi-device-m3-grant-assignment`。
    - 更新 AGENTS、README、权威 PRD、多设备 PRD/impl、doc index、新会话模板、本地数据库同步 SOP 等当前基线说明。
    - `doc/features/multi-device-v2.2-migration-impl.md` 的 M3 自动闸门与项目自检已同步为通过；手工冒烟保持未勾选。
- 验证：
  - `npm test -- src/main/db/__tests__/schema-m3-grant-assignment.test.ts src/main/ipc/handlers/__tests__/assignment.test.ts` 通过。
  - `npm test -- src/main/ipc/handlers/__tests__/assignment.test.ts` 通过。
  - `npm run docs:index:check` 通过。
  - `npm run typecheck` 通过。
  - `npm run lint` 通过（0 error，保留既有 160 个 Vue warning）。
  - `npm test` 通过（52 files / 660 tests）。
  - `npm run build` 通过。
  - `git diff --check` 通过。

# Next Action
- 在真实 Electron 环境执行 M3 主链路手工冒烟：教师创建 assessment -> `assignment:create` -> `confirmStudent` -> `assignment:startAssessment` -> 学生答第一题，并把结果记录回 `doc/features/multi-device-v2.2-migration-impl.md`。

# Blockers
- None

# Key Decisions
- `assignment:startAssessment` 是 M3 新会话唯一合法启动入口；旧 `assessment:startSession` 只保留存量进行中会话幂等兼容。
- 学生端 M3 首版不自动创建、确认或查询 assignment，只在未分配/未确认阶段显示阻断状态。
- M3 文档当前标记为“已实现，待最终验收”，不伪造真实 Electron 手工冒烟结果。

# Relevant Files
- `doc/features/multi-device-v2.2-migration-impl.md`
