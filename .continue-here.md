# Project Direction

- 项目总目标：先把 Pilot R0 做到可追溯、可验收，再继续修订并交付 Full Product 1.0；Pilot 不是项目终点。
- 必须保留的产品决策：唯一产品合同是 `doc/specs/MVP_PRD_v1.0.9-authoritative.md`；当前工程 Schema 基线是 `0.1.18-event-batch-v2.2`；安全红线聚合键为 `student_id + job_code + task_code`。
- 不变边界：不读取、初始化或修改默认数据库；生产 mutation 必须保持 v2 durable command/batch/gate 边界；legacy service 仅可作为无 production 写能力的 planning clone oracle。

# Current Milestone

- M5B Event Batch Runtime 已完成收口。production cutover、自动化验收、针对当前 HEAD 的独立 R3 审查和人工教师/学生/安全红线/可访问性验收均有真实证据。
- M5B-14 production cutover 已完成：target inventory 为 75 channels（29 READ / 46 MUTATION）、36 batch-domain / 10 gate-only，pending 为 0。
- M5B-15 当前为 `PASS`：原生 Electron ABI 迁移、isolated full verifier、target/scope gate、Electron health/login/logout smoke、当前 HEAD 独立 R3 和 isolated userData 人工闭环均已通过。

# Current State

- 任务：完成 M5B-15 最终验收并把后续工作转回 BASE_ABILITY。
- 当前 Vibe Coding 步骤：`/vibe-accept` 已完成。
- 已完成：提交 `210b3a2` 的命令状态修复，以及 M5B-15 运行时修复（planning clone、assignment projection seed、answer target、aggregate sequence key、renderer optional payload）；完整测试 1,476 项、typecheck、lint（0 error）、build、M5B isolated verifier、target/scope gate、M4/M5B Electron UI、独立 R3 和人工验收均通过。
- 产出物：`doc/features/event-batch-v2.2-runtime-m5b15-r3-review.md`、`doc/features/event-batch-v2.2-runtime-validation.md`；M5B-15 source delta 已登记且 M5B-1 至 M5B-14 冻结工件未重写。
- 当前分支：`feat/multi-device-m2-prd`。本轮未访问默认运行库；正式部署或实际运行库迁移仍应遵循受控备份流程。
- [!] `AGENTS.md` 的工程基线行仍是 M5B-14 前的 v0.1.17 摘要；当前权威 `doc/specs/baseline.yaml` 与 schema 是 v0.1.18。

# Next Action

- 重验 BASE_ABILITY Step 1，并对 42+8 实施计划执行独立复审；继续保持不访问默认数据库，不改写已接受的 M5B source delta。

# Goal-mode Handoff: BASE_ABILITY Step 1 + 42+8 Plan Review

## Goal objective

在新会话、全新上下文中，重新验收 BASE_ABILITY Step 1，并对当前 42+8 实施计划完成独立 R3/实现计划复审。只在实际证据满足所有条件时记录 `PASS`；否则保留 `BLOCKED` 或 `CONDITIONAL_PASS`，不得把历史自审结论升级为独立通过。

## Required boundaries

- 不访问、初始化或修改默认数据库；不激活题目，不修改题目 `DRAFT`/`ACTIVE` 状态，不生成激活授权。
- 不改写已接受的 M5B-1 至 M5B-14 冻结工件或 M5B-15 source delta；如发现基线冲突，先标记 `[!]` 并停在文档/审查层。
- 不把历史文档中的旧 Schema、旧 base commit 或“待实现”阶段状态当作当前事实。`doc/specs/baseline.yaml`、`src/main/db/schema.sql` 和真实 Git 状态优先。

## Required reading

先读取 `AGENTS.md`、`doc/会话启动.md`、`.continue-here.md`、`doc/specs/baseline.yaml`、`doc/specs/project-invariants.md`、`doc/ai/vibe-workflow-contract.md`，再读取 `doc/features/base-ability-42plus8-pilot-readiness-prd.md`、`doc/features/base-ability-42plus8-pilot-readiness-impl.md`、`doc/features/base-ability-42plus8-vibe-review.md`、`doc/features/base-ability-current-contracts.md`、`doc/features/base-ability-42plus8-authority-v1.json`、`doc/features/base-ability-42plus8-activation-gate-v1.json` 及其 schema、source/authority/gate 脚本和定向测试。

## Execution order

1. 检查分支、工作树和最近提交；不得 reset、restore 或清理用户改动。
2. 按 Step 1 的权威标准重验 42+8 清单、题目合同、来源/hash、未选 46 题、DRAFT/NO_SCORE/无激活和无数据库副作用。
3. 执行 Step 1 精确命令：
   `npm test -- scripts/__tests__/base-ability-42plus8-authority.test.mjs scripts/__tests__/base-ability-42plus8-gate.test.mjs`
   `npm run contract:base-ability:authority:build`
   `npm run contract:base-ability:authority:check`
   `npm run contract:base-ability:gate:check`
   `npm run docs:index:update`
   `npm run docs:index:check`
   `git diff --check`
4. 独立复审 42+8 实施计划：核对当前 Schema/基线漂移、历史 P1-01（旧基线引用）、P1-02（workbook↔SQL 对账未接入强制门禁）、Step 2A/2B/2C 的已接受前置、跨文件副作用、失败恢复、权限/默认数据库保护和计划中的精确验收命令。
5. 输出独立复审记录，区分实际执行、`NOT_RUN`、`BLOCKED` 和历史证据；未关闭的 P0/P1 不得写 `PASS`，也不得进入 BASE_ABILITY Step 3。

## Completion evidence

必须留下 Step 1 实际命令输出、authority/gate hash 与题集统计、无数据库写入证据，以及独立计划复审的范围、发现、结论和残余风险。只有 Step 1 与计划复审都真实 `PASS`，才能把下一原子动作改为 Step 3 评估；本交接本身不代表这两个任务已完成。

## Stop conditions

来源或 hash 漂移、42+8/DRAFT/NO_SCORE 统计不一致、workbook↔SQL 无法对账、当前基线与计划冲突、需要默认数据库/Schema/激活写入、出现 P0/P1，或任何命令未实际执行时，停止并如实记录，不得补写推测性通过结论。

# Milestone Queue

1. BASE_ABILITY Step 1 验收重跑。
2. 42+8 实施计划独立复审。
3. 仅在 Step 2A、Step 2B、Step 2C 与计划重审均通过后继续 BASE_ABILITY Step 3 及以后工作。

# Blockers

- M5B-15 无 blocker。后续 BASE_ABILITY 复审仍是独立工作队列，不回退已完成的 M5B-15 验收。

# Key Decisions

- 不清理、reset、restore、push、merge 或发布，除非用户再次明确授权。
- M5B scope gate 以不可变 `9a0cbb6` accepted checkpoint 为基线；新增路径必须精确登记，不能使用目录级豁免。
- 对已经开始物理 PREPARE 写入的 append/fsync 异常不得写入 `FAILED`；命令必须保留 `PROCESSING`，由启动恢复以冻结 PREPARE 事实处理。
- 不修改 M5B-1 冻结的 legacy oracle 文件：`reports-service.ts`、`task-closure-service.ts`、`report-service.ts`、`report-reducer.ts`、`report-builders.ts`、`report-contract.ts`。

# Relevant Files

- `doc/features/event-batch-v2.2-runtime-m5b15-r3-review.md`
- `doc/features/event-batch-v2.2-runtime-validation.md`
- `src/main/domain/event-batch/batch-coordinator.ts`
- `src/main/domain/event-batch/__tests__/startup-recovery.test.ts`
- `doc/specs/project-invariants.md`

# 新会话启动 Prompt

```text
读取 `doc/会话启动.md`、`.continue-here.md` 和 `AGENTS.md`，恢复 SVETS 当前现场。

M5B-14 production cutover 已完成。提交 `210b3a2` 修复了已受理命令在 PREPARE 前异常遗留 PROCESSING 的路径；对于已开始 append/fsync 的不确定物理写入失败，命令保留 PROCESSING，启动恢复只使用冻结 PREPARE 事实收敛。完整测试 1,476 项、M5B isolated verifier、target/scope gate 和 M4/M5B Electron UI 均已通过，且未读写默认数据库。

M5B-15 已完成独立 R3、自动化和 isolated userData 人工验收。下一步重验 BASE_ABILITY Step 1，并独立复审 42+8 实施计划；不要重复执行生产切换，也不要访问默认数据库。

Goal 模式目标：完成上述两个独立任务并留下可复核证据。先执行 `.continue-here.md` 的 `Goal-mode Handoff`，不要把历史 `base-ability-42plus8-vibe-review.md` 的 `CONDITIONAL_PASS` 或旧实施计划基线当作当前通过依据。
```
