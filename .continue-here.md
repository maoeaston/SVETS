# Current State
- 任务：doc/specs/impl/ 大迁移（v0.1.10 + PRD v1.0.6 → v0.1.12 + PRD v1.0.9，新增 JOB_SKILL_ASSESSMENT 专业岗位测评）
- 按 `doc/specs/impl/07-implementation-task-book.md` 任务书链式执行：T1 → T2 → T3 → T4 → T5 已完成
- **当前准备执行 T6（JOB_SKILL Session 与固定组卷）**

# Next Action（新会话第一件事）
1. 读 `doc/specs/impl/07-implementation-task-book.md` §T6 了解实现细节
2. 读 `doc/specs/impl/05-state-machine-and-events.md` 了解 JOB_SKILL session 事件规范
3. 执行 T6：在 session service 增加 JOB_SKILL_ASSESSMENT 分支（固定组卷 + assessment_session_question 插入）
4. T6 完成后继续链式执行 T7/T8（线上/线下评分 + 观察运行时）

# Blockers
无阻塞。DB 已完整初始化，298条题目已导入（26 ACTIVE / 272 DRAFT），strategy_config 已配置。

# T5 完成（2026-07-09 人工审核节点）

## 决策记录
| # | 决策 | 结果 |
|---|---|---|
| H1 | M1_TF_015 答案键：改为 true | answer_key_status=CORRECTED，scoring_rule.correct_answer=true |
| H2 | 6道线下题三档锚点：采用03§7草稿 | rubric_anchor_status=PROVISIONAL，可后续专业审核覆盖 |
| H3 | M5_OP_040 安全终止条件：采用草稿 | scoring_rule_json.safety_termination 已写入 |
| H4 | "拆箱与上架"训练 task_code：SHELVE_TASK | T10/T11 报告生成链接时使用 |
| H5 | JOB_SKILL session task_code：JOB_SKILL_DEMO_M1M6 | T6 创建 session 时使用 |
| H6 | M5_OP_048/M5_OP_055 重命名：→ M5_OB_048/M5_OB_055 | 导入时已重命名，DB 中为 M5_OB_xxx |

## DB 当前状态（已验证）
- schema：v0.1.12-job-skill-assessment-mvp-closure（已初始化）
- question_bank：298条（26 ACTIVE / 272 DRAFT）
- ACTIVE 26题：18线上 + 6线下 + 2观察（M1_OB_048, M5_OB_055）
- strategy_config（strategy_job_skill_shelver_v1）：fixed_scored_question_ids=24个，embedded_observation_question_ids=["M1_OB_048","M5_OB_055"]

## 本次会话完成的任务（T5）
- H1：修正 M1_TF_015 答案键（lib + test 更新）
- H6：M5_OP_048/M5_OP_055 重命名（lib + test 新增）
- 真实导入298条 DRAFT 题目
- H2：6道线下题写入正式三档锚点（PROVISIONAL）
- 激活24+2=26道题目
- strategy_config 写入固定题集 ID

# 验收状态
- [x] npx vitest run — 526 passed / 3 failed（3个是预存缺失文件，与本次任务无关）
- [x] 26题 ACTIVE，272题 DRAFT
- [x] strategy_config JOB_SKILL 固定题集：24+2 ID 已写入
- [x] M1_TF_015 answer_key_status=CORRECTED，correct_answer=true
- [x] M5_OB_048/M5_OB_055 ID 已正确命名

# 关键配置值（T6-T11 实现时需要）
- JOB_SKILL 策略 ID：`strategy_job_skill_shelver_v1`
- JOB_SKILL session task_code（H5）：`JOB_SKILL_DEMO_M1M6`
- 训练推荐链接 task_code（H4）：`SHELVE_TASK`（M2低分时报告生成）

# Key Decisions（已落地）
- D1（T1）：`QuestionPolicyJson`/`LevelRule` 保留旧结构做兼容层，不硬改 paper-generator.ts
- D2（T2）：`seedQuestionBank` 测试 helper 自动激活题目；需先设 content_json 的用 `seedQuestionBankDraft`
- D3（T4）：导入脚本技术路线从 TS/tsx 改为 .mjs/sqlite3 CLI
- D4（T4）：观察项识别规则 = question_id 含 `_OB_` OR prompt 含"嵌入式观察"
- D5（T5）：M5_OP_048/M5_OP_055 重命名为 M5_OB_048/M5_OB_055

# Relevant Files（本次新建/修改）
修改（T5新增）：
- `scripts/lib/question-bank-import.mjs`（H1答案修正 + H6 ID重命名）
- `scripts/__tests__/question-bank-import.test.mjs`（更新M1_TF_015断言 + 新增H6测试）

# 本次会话的教训（写入 AGENTS.md 或子 agent 提示词，避免重演）

## 教训1：委托子 agent 做"新建大型 TS 文件"任务时，必须预先声明分块写入策略
（见上一会话记录）

## 教训2：子agent 长时间运行无产出时，应更早检查而非等待完整汇报
（见上一会话记录）

## 教训3：任务书写的技术路径可能与项目工程约束冲突，需要主动识别并询问用户
（见上一会话记录）

## 教训4：DB 文件可能是空文件（0字节），需先初始化 schema 才能执行导入
schema.sql 使用 `CREATE TABLE IF NOT EXISTS`，可以直接 `sqlite3 <db> < schema.sql` 初始化空文件。


# Next Action（新会话第一件事）
1. 读 `doc/specs/impl/00-START-HERE.md` 确认前置条件仍成立
2. 处理 T5 前置人工操作 H1-H6（见 07-implementation-task-book.md 末尾表格）：
   - H1: 核验 M1_TF_015 答案键（本次已在 content_json.review.answer_key_review_note 标记，需人工判定真实答案）
   - H2: 逐题补写 6 道线下题三档行为锚点（参考 `03-job-skill-demo-paper-spec.md` §7）
   - H3: 专业审查 M5_OP_040 安全终止条件
   - H4/H5: 确认"拆箱与上架"训练 task_code 实际值 + JOB_SKILL session task_code 取值
   - H6: 三条嵌入观察语义（已数据核验，可直接执行）
3. 逐题将 `03-job-skill-demo-paper-spec.md` §3 选定的 24+2 题转 ACTIVE（调用 T3 的 `validateQuestionContract()` 前置校验）
4. T5 完成后可继续自动链式执行 T6 → T12（用户已同意"失败自动修复后继续"策略）

# Blockers
- T5 是人工节点，本次会话无法绕过。298 条题目当前全部是 DRAFT，尚未正式写入数据库（T4 只跑了 --dry-run，未执行真实导入）
- 若要执行真实导入：`node scripts/question-bank-import.mjs`（不带 --dry-run），需要先确认 DB 路径（`--db` 参数或走默认 Electron userData 路径）

# 本次会话完成的任务（T1-T4，全部已验证）

## T1 — 共享类型与枚举升级
- 文件：`src/shared/types/json-schemas.ts`（164行 → 874行）
- 新增 23+ 个枚举/接口：StrategyType/BankDomain/JobModuleCode/QuestionPhase/ItemUsage/AnswerKeyStatus 等
- 删除 `ContentJsonTrueFalse.expected_answer`、`ContentJsonSingleChoice.expected_answer`、`ContentJsonDrag.scoring_mode`（迁入 scoring_rule_json）
- 新增 ScoringRuleOrderMatch/MappingMatch/SetMatch/MetricThreshold/EventRule/NoScore
- `QuestionPolicyJson` **保留旧结构**（标记 @deprecated），因为 `paper-generator.ts` 用 `QuestionPolicyJson['question_ratio']` 类型索引访问，改了会破坏现有组卷逻辑；新增 `QuestionPolicyBaseAbility`/`QuestionPolicyJobSkillFixedSet` 作为独立类型供 T6 使用
- `ScoringPolicyJson` 改为联合类型（`ScoringPolicyBaseAbility | ScoringPolicyJobSkill`），向后兼容旧 `level_rules`/`score_values` 字段（标记 deprecated）

## T2 — schema v0.1.12 全量基线
- 文件：`src/main/db/schema.sql`（直接用 `doc/specs/impl/01-schema-v0.1.12.sql` 替换）
- **引发的回归修复**（新触发器导致，全部已修复）：
  - `trg_assessment_session_question_insert_validation`：要求题目必须 ACTIVE，域/模块/题型/item_usage 四者匹配
  - `trg_answer_record_session_question_validation`：要求 answer_record.question_type 与 assessment_session_question.question_type 一致
  - `trg_question_bank_active_semantic_immutable`：ACTIVE 题的语义字段冻结（含 content_json）
- 修复涉及文件：`test-helpers.ts`（seedQuestionBank 改为先插 DRAFT 再激活；新增 `seedQuestionBankDraft` 变体供需要"激活前改content"的测试用）、`assessment-reducer.ts`（phase 判定改为按 question_type 而非位置）、6个测试文件（assessment-answer/emotion/read/redline/start-session.test.ts + assessment-reducer.test.ts + schema-scoring-closure.test.ts）

## T3 — validateQuestionContract()
- 新建：`src/main/domain/validators/question-validator.ts`（266行，15条规则+补充规则，照抄 `02-json-contracts-and-types.md` §10）
- 新建：`src/main/domain/validators/__tests__/question-validator.test.ts`（39个测试）

## T4 — 导入脚本与 dry-run
- **[!] 偏离任务书原定路径**：任务书写的是 `src/main/tools/question-importer.ts`，但因与 AGENTS.md「一次性数据运维优先 sqlite3 CLI，不要为此改依赖」的约束冲突（且项目没装 tsx/ts-node，better-sqlite3 在纯 Node 脚本环境可能 ABI 不匹配），改为沿用项目已有 4 个同类脚本的模式：
  - 新建：`scripts/question-bank-import.mjs`（CLI，支持 `--dry-run`/`--db`/`--out`/`--source`）
  - 新建：`scripts/lib/question-bank-import.mjs`（清洗逻辑：字段映射、content_json/scoring_rule_json 重构、C1-C10+D1-D10 全部规则）
  - 新建：`scripts/__tests__/question-bank-import.test.mjs`（31个测试）
- dry-run 验证结果（298条全部通过）：
  - 模块×题型对账矩阵与文档 §6.1 完全一致（M1:48/M2:41/M3:58/M4:48/M5:55/M6:48）
  - SC答案位置分布 A=32/B=57/C=7，与文档 §6.4 完全一致
  - 3条观测项正确识别：M1_OB_048, M5_OP_048, M5_OP_055
  - M1_TF_015 正确标记 answer_key_suspicious + review note
  - 域隔离验证 pass=true
- **尚未执行真实导入**（数据库里目前没有这298条题目）

# 验收状态
- [x] npx tsc --noEmit — 全程保持通过
- [x] npx vitest run — 523 passed / 3 failed（3个是预存的 doc/features/question-bank-image-drag-question-seed.sql 缺失文件问题，与本次任务无关，从会话开始就存在）
- [ ] T4 真实导入未执行（等 T5 人工确认后再导入更安全，避免 DRAFT 题目提前污染库）

# Key Decisions（已落地）
- D1（T1）：`QuestionPolicyJson`/`LevelRule` 保留旧结构做兼容层，不硬改 paper-generator.ts（避免超出本次任务范围的连锁修改）
- D2（T2）：`seedQuestionBank` 测试 helper 现在会自动把种子题激活为 ACTIVE（多数测试期望能直接组卷）；需要"先设置真实 content_json 再激活"的测试改用 `seedQuestionBankDraft`
- D3（T4）：导入脚本技术路线从 TS/tsx 改为 .mjs/sqlite3 CLI，用户已确认此偏离（对话记录见本会话中段 AskUserQuestion）
- D4（T4）：观察项识别规则 = question_id 含 `_OB_` OR prompt 含"嵌入式观察"（M5_OP_048/M5_OP_055 走后者，因为 ID 本身不含 _OB_）

# Relevant Files（本次新建/修改）
新建：
- `src/main/domain/validators/question-validator.ts`
- `src/main/domain/validators/__tests__/question-validator.test.ts`
- `scripts/question-bank-import.mjs`
- `scripts/lib/question-bank-import.mjs`
- `scripts/__tests__/question-bank-import.test.mjs`

修改：
- `src/shared/types/json-schemas.ts`
- `src/main/db/schema.sql`
- `src/main/db/test-helpers.ts`
- `src/main/domain/assessment-reducer.ts`
- `src/main/ipc/handlers/__tests__/assessment-answer.test.ts`
- `src/main/ipc/handlers/__tests__/assessment-emotion.test.ts`
- `src/main/ipc/handlers/__tests__/assessment-read.test.ts`
- `src/main/ipc/handlers/__tests__/assessment-redline.test.ts`
- `src/main/ipc/handlers/__tests__/assessment-start-session.test.ts`
- `src/main/domain/__tests__/assessment-reducer.test.ts`
- `src/main/db/__tests__/schema-scoring-closure.test.ts`

# 本次会话的教训（写入 AGENTS.md 或子 agent 提示词，避免重演）

## 教训1：委托子 agent 做"新建大型 TS 文件"任务时，必须预先声明分块写入策略
**现象**：T4 委托给 general-purpose 子 agent 后，跑了 71 次工具调用、约 70 分钟，最终 `src/main/tools/` 目录完全不存在——零产出。
**根因推断**（未100%证实，但高度吻合）：Kiro API 对单次工具调用的大 payload（Write/Edit）有截断限制，超限会报 `InputValidationError: ... file_path is missing`。若首次尝试就是一次性 Write 几百行的复杂文件，会反复撞同一个截断错误，且报错信息完全不提示"文件太大"，容易让子 agent 陷入无效重试而不换策略。
**证据**：主线程自己写 `json-schemas.ts`（目标 700+行）时连续遇到 6 次同样的 `file_path is missing` 报错，换成"先建 <150 行骨架 → Edit 分块追加，每块 <50行/<4000字符"后才成功。
**应对**：以后凡是委托子 agent 产出预期超过 150 行/4000字符的单个文件，必须在 prompt 里明确写："文件较大，请先用 Write 建骨架（不超过150行），再用 Edit 分块追加每次不超过50行/4000字符；如果 Write 或 Edit 报 file_path/content 相关的 InputValidationError，说明 payload 过大，立刻切分更小块重试，不要重复相同大小的调用"。

## 教训2：子agent 长时间运行无产出时，应更早检查而非等待完整汇报
**现象**：子 agent 结束后只返回了一句思考文本（"Now I have all the context needed..."），没有汇报总结，主线程一开始误以为它完成了。
**应对**：子agent 返回后，先用 `git status --porcelain` 核实实际文件变更再采信任何文字汇报，尤其当 duration_ms 或 tool_uses 数字明显偏高时。

## 教训3：任务书写的技术路径可能与项目工程约束冲突，需要主动识别并询问用户
**现象**：07-implementation-task-book.md 写的 T4 产出路径是 `src/main/tools/question-importer.ts`，但这与 AGENTS.md「一次性数据运维优先 sqlite3 CLI，不为此改依赖」的既有约束相冲突。
**应对**：执行第三方/历史设计文档中的任务说明前，先扫一遍 AGENTS.md/.claude/rules/ 是否有更具体的工程约束覆盖此路径；发现冲突用 AskUserQuestion 列出方案让用户选，不要默认服从任务书字面写法。

# 前置人工操作清单（T5 之前必须完成，见 00-START-HERE.md §5）
| # | 操作 | 依据 |
|---|---|---|
| H1 | 核验 M1_TF_015 答案键（正向组孤例 false，疑似键错） | PRD v1.0.9 附录 D 第 10 条 |
| H2 | 逐题补写 6 道线下题三档行为锚点 | 审查报告 v2 D2 |
| H3 | 专业审查 M5_OP_040 安全终止条件 | PRD v1.0.8 §5.3 |
| H4 | 确认"拆箱与上架"训练实际 task_code 值 | PRD v1.0.9 附录 D 第 7 条 |
| H5 | 确认 JOB_SKILL session task_code（建议 JOB_SKILL_DEMO_M1M6） | PRD v1.0.9 附录 D 第 8 条 |
| H6 | 三条嵌入观察语义已由数据核验确认（可直接执行） | 03 文档 §4.1 |
