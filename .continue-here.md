# Current State
- 任务：多设备 v2.2 M3 Grant/Assignment Step M3-4 Assignment 共享类型、事件 payload、IPC API 类型与 preload 白名单已完成。
- 当前 Vibe Coding 步骤：逐步实现 Step M3-5。
- 已完成：
  - `src/shared/types/assignment.ts` — 新增 assignment IPC 参数、结果、状态、确认方式与错误码类型。
  - `src/shared/types/event-payloads.ts` — 新增 `BUSINESS_SESSION`、M3 五个事件类型与可重建 grant/assignment/phase 的 payload。
  - `src/shared/types/ipc-api.ts` — 新增 `assignment.create`、`confirmStudent`、`startAssessment`、`rebind`、`release` 类型声明。
  - `src/preload/index.ts` — 新增五个 `assignment:*` IPC 白名单通道。
  - 验证：`npm run typecheck` 通过；`npm run lint` 通过（0 error，保留既有 160 个 Vue warning）；`git diff --check` 通过。
- 产出物：
  - `src/shared/types/assignment.ts`
  - `src/shared/types/event-payloads.ts`
  - `src/shared/types/ipc-api.ts`
  - `src/preload/index.ts`

# Next Action
- 按 `doc/features/multi-device-v2.2-migration-impl.md` 的 Step M3-5 新增 `assignment-reducer.ts` 与 reducer 测试，投影 `ASSIGNMENT_CREATED` / `ASSIGNMENT_STUDENT_CONFIRMED` / `GRANT_REBOUND` / `ASSIGNMENT_RELEASED` 并接入 `ASSIGNMENT_ASSESSMENT_STARTED`。

# Blockers
- Full `npm test` 当前失败：既有 `assessment:startSession` / assessment reducer 仍尝试 `PREPARED -> ONLINE_IN_PROGRESS`，被 v0.1.15 D1 拒绝为 `PREPARED can only advance to ASSIGNED`；需在后续 M3 assignment reducer / start flow 收窄步骤修复。

# Key Decisions
- M3 Assignment 首版只把 `TEACHER_ATTESTATION` / `NONE_REQUIRED` 定义为成功确认路径；`PIN` / `PHOTO_MATCH` 仅作为 schema 枚举保留。
- Assignment 事件 payload 使用 schema 字段名 `identity_confirmation_method`，并显式携带 grant、assignment、delivery_phase 投影字段，不引用未落地 M5 `command_log`。
- 本次未修改 `assessment.ts` 的 `DeliveryPhase`；该类型仍缺 `ASSIGNED` / `STUDENT_CONFIRMED`，建议在后续收窄 `assessment:startSession` 时同步修正。

# Relevant Files
- `doc/features/multi-device-v2.2-migration-impl.md`
- `src/shared/types/event-payloads.ts`
- `src/main/domain/assignment-reducer.ts`
- `src/main/domain/__tests__/assignment-reducer.test.ts`
- `src/main/domain/assessment-reducer.ts`
