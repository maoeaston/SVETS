# Project Direction

- 项目总目标：先把 Pilot R0 做到可追溯、可验收，再继续修订并交付 Full Product 1.0；Pilot 不是项目终点。
- 必须保留的产品决策：唯一产品合同是 `doc/specs/MVP_PRD_v1.0.9-authoritative.md`；当前工程 Schema 基线是 `0.1.18-event-batch-v2.2`；安全红线聚合键为 `student_id + job_code + task_code`。
- 不变边界：不读取、初始化或修改默认数据库；生产 mutation 必须保持 v2 durable command/batch/gate 边界；legacy service 仅可作为无 production 写能力的 planning clone oracle。

# Current Milestone

- M5B Event Batch Runtime 收口。完整 `PASS` 的门槛是 production cutover、自动化验收、独立 R3 审查和人工教师/学生/安全红线/可访问性验收均有真实证据。
- M5B-14 production cutover 已完成：target inventory 为 75 channels（29 READ / 46 MUTATION）、36 batch-domain / 10 gate-only，pending 为 0。
- M5B-15 当前为 `AUTOMATION_PASS_MANUAL_PENDING`：原生 Electron ABI 迁移、isolated full verifier、target/scope gate 和 Electron health/login/logout smoke 已通过。

# Current State

- 任务：完成 M5B-14 production cutover 和 M5B-15 自动化验收后的提交与可恢复交接。
- 当前 Vibe Coding 步骤：`/vibe-accept`。
- 已完成：`src/main/application/runtime/application-runtime.ts`、`src/main/db/connection.ts`、`src/main/ipc/handler-registry.ts`、`src/preload/index.ts` 和 `src/renderer/src/App.vue` 已接通 v2 runtime；`doc/features/event-batch-v2.2-runtime-m5b15-r3-review.md` 留存自动化验收结论。
- 产出物：M5B-10 至 M5B-15 的 runtime、schema、IPC、迁移、测试、source/scope fixtures 和验收文档，已于本轮提交。
- 当前分支：`feat/multi-device-m2-prd`。本轮未访问默认运行库；正式部署或实际运行库迁移仍应遵循受控备份流程。
- [!] `AGENTS.md` 的工程基线行仍是 M5B-14 前的 v0.1.17 摘要；当前权威 `doc/specs/baseline.yaml` 与 schema 是 v0.1.18。

# Next Action

- 在新的独立上下文启动 M5B-15 R3 审查：先读取 `event-batch-v2.2-runtime-m5b15-r3-review.md`、validation 和对应 runtime/migration 源码，再输出独立审查结论；不得修改生产代码。

# Milestone Queue

1. M5B-15 独立 R3 复核，以及 isolated userData 下的人工教师、学生、安全红线和可访问性验收。
2. M5B 全部接受后，重跑 BASE_ABILITY Step 1 验收并独立重审 42+8 实施计划。
3. 仅在 Step 2A、Step 2B、Step 2C 与计划重审均通过后继续 BASE_ABILITY Step 3 及以后工作。

# Blockers

- 独立 R3 审查、人工教师核心流、学生核心流、安全红线流和可访问性验收尚未执行；因此 M5B-15 不能提升为完整 `PASS`。

# Key Decisions

- 不清理、reset、restore、push、merge 或发布，除非用户再次明确授权。
- M5B scope gate 以不可变 `9a0cbb6` accepted checkpoint 为基线；新增路径必须精确登记，不能使用目录级豁免。
- 不修改 M5B-1 冻结的 legacy oracle 文件：`reports-service.ts`、`task-closure-service.ts`、`report-service.ts`、`report-reducer.ts`、`report-builders.ts`、`report-contract.ts`。

# Relevant Files

- `doc/features/event-batch-v2.2-runtime-m5b15-r3-review.md`
- `doc/features/event-batch-v2.2-runtime-validation.md`
- `src/main/application/runtime/application-runtime.ts`
- `src/main/db/connection.ts`
- `src/main/ipc/handler-registry.ts`
- `doc/specs/project-invariants.md`

# 新会话启动 Prompt

```text
读取 `doc/会话启动.md`、`.continue-here.md` 和 `AGENTS.md`，恢复 SVETS 当前现场。

M5B-14 production cutover 已完成，M5B-15 自动化验收为 AUTOMATION_PASS_MANUAL_PENDING：target inventory、isolated full database、exact M4 原生 Electron ABI 迁移、Electron health/login/logout smoke 均已通过，且未读写默认数据库。

不要重复执行生产切换，也不要修改生产代码。先以只读方式完成 M5B-15 R3 审查；当前上下文不具独立性，应明确标记审查身份。随后在 isolated userData 下完成人工教师、学生、安全红线和可访问性验收。只有这些证据实际完成后，才能把 M5B-15 写成完整 PASS。
```
