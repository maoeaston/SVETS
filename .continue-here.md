# Project Direction

- 项目总目标：先把 Pilot R0 做到可追溯、可验收，再继续修订并交付 Full Product 1.0；Pilot 不是项目终点。
- 必须保留的产品决策：唯一产品合同是 `doc/specs/MVP_PRD_v1.0.9-authoritative.md`；当前工程 Schema 基线是 `0.1.18-event-batch-v2.2`；安全红线聚合键为 `student_id + job_code + task_code`。
- 不变边界：不读取、初始化或修改默认数据库；生产 mutation 必须保持 v2 durable command/batch/gate 边界；legacy service 仅可作为无 production 写能力的 planning clone oracle。

# Current Milestone

- M5B Event Batch Runtime 收口。完整 `PASS` 的门槛是 production cutover、自动化验收、针对当前 HEAD 的独立 R3 审查和人工教师/学生/安全红线/可访问性验收均有真实证据。
- M5B-14 production cutover 已完成：target inventory 为 75 channels（29 READ / 46 MUTATION）、36 batch-domain / 10 gate-only，pending 为 0。
- M5B-15 当前为 `AUTOMATION_PASS_MANUAL_PENDING`：原生 Electron ABI 迁移、isolated full verifier、target/scope gate 和 Electron health/login/logout smoke 已通过；当前 HEAD 的独立 R3 审查和人工业务验收仍待执行。

# Current State

- 任务：修复已受理命令在 PREPARE 前异常遗留 `PROCESSING`，并保守处理 PREPARE 物理写入的不确定失败。
- 当前 Vibe Coding 步骤：待新的独立 `/vibe-review`。
- 已完成：提交 `210b3a2` 在批处理 planner、gate-only 事务、报告导出对话框失败时持久化 `FAILED`；已开始 PREPARE 写入的 append/fsync 失败保留 `PROCESSING`，仅由启动恢复读取冻结事实收敛。完整测试 1,475 项、typecheck、lint（0 error/663 既有 warning）、build、M5B isolated verifier、target/scope gate 与 M4/M5B Electron UI 验收均通过。
- 产出物：`210b3a2 fix(event-batch): finalize pre-prepare failures safely`；M5B-10 至 M5B-15 的既有 runtime、schema、IPC、迁移、测试、source/scope fixtures 和验收文档保持可追溯。
- 当前分支：`feat/multi-device-m2-prd`。本轮未访问默认运行库；正式部署或实际运行库迁移仍应遵循受控备份流程。
- [!] `AGENTS.md` 的工程基线行仍是 M5B-14 前的 v0.1.17 摘要；当前权威 `doc/specs/baseline.yaml` 与 schema 是 v0.1.18。

# Next Action

- 在新的独立上下文对提交 `210b3a2` 执行 M5B-15 R3 审查：先读取既有 R3/validation 记录、当前 `batch-coordinator.ts` 与恢复测试，再输出独立审查结论；不得修改生产代码。

# Milestone Queue

1. M5B-15 针对 `210b3a2` 的独立 R3 复核，以及 isolated userData 下的人工教师、学生、安全红线和可访问性验收。
2. M5B 全部接受后，重跑 BASE_ABILITY Step 1 验收并独立重审 42+8 实施计划。
3. 仅在 Step 2A、Step 2B、Step 2C 与计划重审均通过后继续 BASE_ABILITY Step 3 及以后工作。

# Blockers

- 针对 `210b3a2` 的独立 R3 审查、人工教师核心流、学生核心流、安全红线流和可访问性验收尚未执行；因此 M5B-15 不能提升为完整 `PASS`。

# Key Decisions

- 不清理、reset、restore、push、merge 或发布，除非用户再次明确授权。
- M5B scope gate 以不可变 `9a0cbb6` accepted checkpoint 为基线；新增路径必须精确登记，不能使用目录级豁免。
- 对已经开始物理 PREPARE 写入的 append/fsync 异常不得写入 `FAILED`；命令必须保留 `PROCESSING`，由启动恢复以冻结 PREPARE 事实处理。
- 不修改 M5B-1 冻结的 legacy oracle 文件：`reports-service.ts`、`task-closure-service.ts`、`report-service.ts`、`report-reducer.ts`、`report-builders.ts`、`report-contract.ts`。

# Relevant Files

- `doc/features/event-batch-v2.2-runtime-m5b15-r3-review.md`
- `doc/features/event-batch-v2.2-runtime-validation.md`
- `src/main/domain/event-batch/batch-coordinator.ts`
- `src/main/domain/event-batch/__tests__/startup-recovery.test.ts`
- `doc/specs/project-invariants.md`

# 新会话启动 Prompt

```text
读取 `doc/会话启动.md`、`.continue-here.md` 和 `AGENTS.md`，恢复 SVETS 当前现场。

M5B-14 production cutover 已完成。提交 `210b3a2` 修复了已受理命令在 PREPARE 前异常遗留 PROCESSING 的路径；对于已开始 append/fsync 的不确定物理写入失败，命令保留 PROCESSING，启动恢复只使用冻结 PREPARE 事实收敛。完整测试 1,475 项、M5B isolated verifier、target/scope gate 和 M4/M5B Electron UI 均已通过，且未读写默认数据库。

不要重复执行生产切换，也不要修改生产代码。先以独立上下文对 `210b3a2` 完成 M5B-15 R3 审查；既有 R3 记录早于该 P0 修复，不能替代本次结论。随后在 isolated userData 下完成人工教师、学生、安全红线和可访问性验收。只有这些证据实际完成后，才能把 M5B-15 写成完整 PASS。
```
