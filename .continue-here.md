# Current State
- 任务：JOB_SKILL_ASSESSMENT 前端 UI 对接（教师侧）
- 当前 Vibe Coding 步骤：逐步实现完成（无 /vibe-feature / /vibe-impl 流程，直接编码）
- 已完成：
  - `src/main/ipc/handlers/job-skill-scoring.ts` — 新增 `getSessionScoringQuestions()`，返回 session 线下/观察题 ID + 模块信息，typecheck + 584/3 tests 通过
  - `src/shared/types/strategy.ts` — `StrategyType` 加入 `'JOB_SKILL_ASSESSMENT'`
  - `src/renderer/src/views/teacher/AssessmentCreateView.vue` — 加测评类型 radio（BASELINE/JOB_SKILL），taskCode/策略列表联动
  - `src/renderer/src/views/teacher/JobSkillScoringView.vue`（新建）— 6模块线下评分，已评只读
  - `src/renderer/src/views/teacher/TeacherObservationView.vue`（新建）— 2条观察项逐条录入，已录只读
  - `src/renderer/src/views/teacher/AssessmentListView.vue` — OFFLINE_PENDING 按 strategyType 分叉：JOB_SKILL→"线下评分"+"观察录入"；其他→"实操评分"
  - `src/renderer/src/router/index.ts` — 新增 `/:sessionId/job-skill-scoring` 和 `/:sessionId/observations` 路由
- 产出物：commit（见 Next Action）

# Next Action
- 启动 Electron app（`npm run dev`），用测试账号手动跑一遍 JOB_SKILL 完整流程（创建 session → 学生答题 → 教师线下评分 → 观察录入 → 确认 COMPLETED 状态），确认 UI 可用

# Blockers
- None（报告查看页 JobSkillReportView 暂未实现，属有意延后，非阻塞）

# Key Decisions
- D1：`getSessionScoringQuestions` 新 IPC 返回题目 ID + 模块码，前端靠此构建评分/观察表单（问题：前端无法从 session 直接拿 OBSERVATION 题 ID）
- D2：AssessmentCreateView 仍用单一 `/teacher/assessments/new` 入口，加 radio 切换类型，避免增加路由
- D3：报告查看视图（JobSkillReportView）留待后续，当前完成后跳回列表页即可

# Relevant Files
- `src/renderer/src/views/teacher/JobSkillScoringView.vue`
- `src/renderer/src/views/teacher/TeacherObservationView.vue`
- `src/renderer/src/views/teacher/AssessmentCreateView.vue`

# T10 完成（2026-07-09）

## [!] 任务书与实际架构冲突（同 T7/T8/T9 处理方式：延续 handler 模式）
07-implementation-task-book.md §T10 写"产出文件 `src/main/domain/report-service.ts`"，
但项目实际用纯函数+薄IPC包装的 handler 模式。T10 新建
`src/main/ipc/handlers/job-skill-report.ts`，不创建 domain/report-service.ts。

## 关键设计决策
- **触发方式**：自动触发——`maybeGenerateJobSkillResult` 的 `txn()` 后链式调用 `maybeGenerateJobSkillReport`（同T9触发T10的模式）
- **聚合根**：REPORT_GENERATED 事件的 aggregateType='TASK_REPORT'（非 ASSESSMENT_SESSION）；直接 INSERT task_report，不走 assessment-reducer
- **幂等**：source_aggregate_id + source_aggregate_type + report_type 已存在则直接返回
- **TC-P06 安全保证**：`placement_advice = { enabled: false, reason_disabled: 'MVP_DEMO_PROFILE_ONLY' }`，硬性阻止就业安置结论
- **回归修复**：`job-skill-result.test.ts` beforeEach 新增 `DELETE FROM task_report`（T10 自动生成报告后，若不清理，下轮 `DELETE FROM domain_event_projection` 会触发 FK 约束失败）

## 验收状态（T10+T12）
- [x] `npx tsc --noEmit` — 通过
- [x] `npx vitest run` — 584 passed / 3 pre-existing failed（同一缺失文件问题，无新增）
- [x] TC-P01~P09 全通过（见上一会话记录）
