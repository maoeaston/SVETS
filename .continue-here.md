# Current State
- 任务：5.4 测评功能逐步实现，Step 6b（createSession handler）已完成，下一步 Step 7（答题 + 情绪中断/崩溃 handler）
- 当前 Vibe Coding 步骤：逐步实现，Step 6b 完成（commit bbf831c），Step 7 待开始
- 已完成（5.4 全程）：
  - Step 1-5：EventType/payloads + assertStudent + paper-generator + level-judge + assessment-reducer（230 tests 绿）
  - Step 6a（8327919）：assessment IPC 类型 + preload 白名单
  - Step 6b（bbf831c）：createSession handler + seedAssessmentErrorCodes + IPC 注册 + 14 单测（244 tests 全绿，typecheck + build 通过）
- 产出物：`src/main/ipc/handlers/assessment.ts` + `__tests__/assessment-create.test.ts`

# Next Action
- Step 7：扩展 `src/main/ipc/handlers/assessment.ts` 加 submitAnswer 纯函数（STUDENT 路径），新建 `__tests__/assessment-answer.test.ts`
- submitAnswer 核心路径：assertStudent + assertSessionOwner → 校验 session.status=ACTIVE → 校验 question ∈ session ONLINE → 校验无 VALID answer_record → 按 question_type 校验 answer_payload → 计算分数 → 事务{ writeEvent(ANSWER_SUBMITTED) + applyAssessmentEvent }
- 详见 `doc/features/assessment-impl.md` Step 7（line 362-405）

# Blockers
- 5.3 题库未交付：单测用 mock 题库（seedQuestionBank），端到端验证推迟

# Key Decisions
- **reducer 是投影唯一写入者**：handler 只调 writeEvent + applyAssessmentEvent，不另 INSERT。Step 7 submitAnswer 同样遵循——reducer applyAnswerSubmitted 承担 INSERT answer_record + 计数前移
- **writeEvent mock 模式**（Step 6b 建立）：vi.mock('.../event-writer') + vi.hoisted 共享 db 引用，mock 内 INSERT domain_event_projection。`assessment-create.test.ts` 有完整模板，Step 7/8 测试直接复用
- **[!] answerPayload 字段名不一致**：impl.md Step 7 文本（line 378-380）写 `selected_option`/`slots`，但 event-payloads.ts 实际是 `selected`/`placements`。Step 7 实现 handler 时以 event-payloads.ts 为准，同步修正 impl.md

# Relevant Files
- `doc/features/assessment-impl.md`（Step 7 规范，line 362-405）
- `src/main/ipc/handlers/assessment.ts`（Step 7 扩展 submitAnswer）
- `src/main/ipc/handlers/__tests__/assessment-create.test.ts`（writeEvent mock 模式参考）
- `src/main/domain/assessment-reducer.ts`（applyAnswerSubmitted，Step 7 调用）
- `src/shared/types/event-payloads.ts`（AnswerPayloadDetail / AnswerSubmittedPayload）
