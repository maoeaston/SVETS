# Current State
- 任务：多设备 v2.2 M3 Grant/Assignment Step M3-3 本地 runtime/auth 引导服务已完成。
- 当前 Vibe Coding 步骤：逐步实现 Step M3-4。
- 已完成：
  - `src/main/domain/local-runtime-context.ts` — 新增 `ensureLocalRuntimeContext(db, teacherUserId)`，事务内校验 ACTIVE TEACHER/ADMIN，复用 ACTIVE runtime，必要时创建本地 organization/node/device/runtime，并复用或创建 `DEVICE_KEY` auth_session。
  - `src/main/domain/__tests__/local-runtime-context.test.ts` — 覆盖首次创建、重复复用、既有拓扑复用、过期 auth 重建、固定 ID/明文 token 防回归、ADMIN 允许、非法账号拒绝。
  - `src/main/db/test-helpers.ts` — 新增 `seedLocalRuntimeContextFixture`，供后续 assignment 测试复用。
  - 验证：`npm test -- src/main/domain/__tests__/local-runtime-context.test.ts` 通过；`npm run typecheck` 通过；`npm run lint` 通过（0 error，保留既有 160 个 Vue warning）；`git diff --check` 通过。
- 产出物：
  - `src/main/domain/local-runtime-context.ts`
  - `src/main/domain/__tests__/local-runtime-context.test.ts`
  - `src/main/db/test-helpers.ts`

# Next Action
- 按 `doc/features/multi-device-v2.2-migration-impl.md` 的 Step M3-4 新增 Assignment 共享类型、M3 事件 payload、IPC API 类型与 preload 白名单。

# Blockers
- None

# Key Decisions
- 本地 runtime/auth 引导服务只提供领域骨架，不接入 `initDatabase()`；后续只应在 assignment handler 需要时调用。
- 本地 topology 与 token/hash 均使用 UUID 派生值，不创建固定 `org_default`、固定 device/runtime ID 或明文 token。
- `ensureLocalRuntimeContext` 只允许 ACTIVE TEACHER/ADMIN；复用未过期 ACTIVE auth_session，否则创建 `DEVICE_KEY` auth_session。

# Relevant Files
- `doc/features/multi-device-v2.2-migration-impl.md`
- `src/shared/types/assignment.ts`
- `src/shared/types/event-payloads.ts`
- `src/shared/types/ipc-api.ts`
- `src/preload/index.ts`
