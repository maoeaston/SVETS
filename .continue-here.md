# Project Direction
- 项目总目标：先把 Pilot R0 做到可追溯、可验收，再继续修订并交付 Full Product 1.0；Pilot 不是项目终点。
- 必须保留的产品决策：`doc/specs/MVP_PRD_v1.0.9-authoritative.md` 是当前唯一产品合同；AI 自动评分和 AI 岗位推荐必须进入 Full Product 1.0；`doc/specs/FULL_PRODUCT_PRD_v2.0-draft.md` 仍是未批准草案。
- 不变边界：安全红线、历史可追溯和教师责任边界不得降级；题目审核通过前不得激活；不得把 AI 作为无人工确认的录用或淘汰最终裁决者。

# Current Milestone
- Pilot R0 基础底座：F2 至 F6 已完成；当前推进 F7 报告页面框架，完成门槛是实现文档、报告 IPC/UI、权限、锁定、导出框架和验收门禁全部通过。

# Current State
- 任务：F7 Step 8 已完成默认脱敏、自包含 HTML 导出；报告框架和导出能力已提交并推送到 `origin/feat/multi-device-m2-prd`。
- 当前 Vibe Coding 步骤：逐步实现第 9 步（尚未开始）。
- 已完成：`src/shared/report-presentation.ts` 新增 page/export 同源投影和静态导出 allowlist；`src/main/domain/report-html.ts` 新增无脚本自包含 HTML 渲染；`src/main/domain/report-export.ts` 新增 prepare/完成、hash、asset、事件和失败清理；`src/main/ipc/handlers/reports.ts` 接入 `reports:export`；`src/shared/types/report.ts`、`src/shared/types/ipc-api.ts`、`src/preload/index.ts` 同步导出 API；`src/main/domain/__tests__/report-html.test.ts`、`src/main/ipc/handlers/__tests__/report-export.test.ts`、`src/shared/__tests__/report-presentation.test.ts` 覆盖脱敏、CSP、取消、失败、LOCKED 和 asset 审计。
- 已完成验证：`npm run typecheck` 通过；`npm run lint` 通过（0 error，622 条既有 warning）；`npm test` 通过（101 files / 1015 tests）；`npm run build` 通过；`npm run db:f7:verify` 通过；`npm run docs:index:check` 通过；`git diff --cached --check` 通过。
- 未执行验收：默认 `npm run db:verify` 会指向真实运行库 `xc-career-guide.db`，按本轮约束未执行；Electron 实机报告闭环和三尺寸截图仍待 Step 10。
- 产出物：commit `301849c feat: add report framework and html export`，已推送到 `origin/feat/multi-device-m2-prd`。

# Next Action
- 按 `doc/features/report-page-framework-impl.md` 的 Step 9 实现教师报告列表、详情与操作界面；继续不得接触真实 `xc-career-guide.db`。

# Milestone Queue
1. 当前：F7 Step 9 教师报告 UI，然后 Step 10 独立审查、全量门禁和真实 Electron 见证。
2. 后续：Q1 BASE_ABILITY 42+8 正式施测补料。
3. 后续：Q2 JOB_SKILL R1-R6 与 270 项素材/Pilot 门禁。

# Blockers
- None。

# Key Decisions
- 报告直接渲染持久化 `report_content_json` 的 shared presentation；页面和导出不得各自解释 raw report JSON，不引入 Markdown 或 PDF 报告渲染。
- F7 只导出默认脱敏、无脚本和外部请求的自包含 HTML；取消不写事件，成功导出创建独立 `REPORT_FILE` asset，`LOCKED` 报告导出后仍保持 `LOCKED`。
- BASE_ABILITY 完整报告只能从教师确认的活动 `task_closure` 生成；闭环修正走 replacement，不覆盖历史 binding。

# Relevant Files
- `doc/features/report-page-framework-impl.md`
- `src/renderer/src/router/index.ts`
- `src/renderer/src/views/teacher/TeacherLayout.vue`
- `src/renderer/src/views/teacher/TeacherHomeView.vue`
- `src/renderer/src/stores/report.ts`
- `src/renderer/src/views/teacher/ReportListView.vue`
- `src/renderer/src/views/teacher/ReportDetailView.vue`
- `src/renderer/src/components/report/ReportSectionList.vue`
- `src/renderer/src/components/report/report-page-state.ts`
- `src/shared/report-presentation.ts`
- `src/main/window-config.ts`
- `src/main/index.ts`
