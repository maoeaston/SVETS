# Project Direction
- 项目总目标：先把 Pilot R0 做到可追溯、可验收，再继续修订并交付 Full Product 1.0；Pilot 不是项目终点。
- 必须保留的产品决策：`doc/specs/MVP_PRD_v1.0.9-authoritative.md` 是当前唯一产品合同；AI 自动评分和 AI 岗位推荐必须进入 Full Product 1.0；`doc/specs/FULL_PRODUCT_PRD_v2.0-draft.md` 仍是未批准草案。
- 不变边界：安全红线、历史可追溯和教师责任边界不得降级；题目审核通过前不得激活；不得把 AI 作为无人工确认的录用或淘汰最终裁决者。

# Current Milestone
- Pilot R0 基础底座：F2 登录会话与权限、F3 数据保存与恢复、F4 暂停恢复与安全红线、F5 基础页面与异常中心、F6 评分计算框架与双轨隔离均已完成；当前进入 F7 报告页面框架，完成门槛是报告 Mini-PRD、实现文档、报告 IPC/UI/权限/锁定/导出框架和验收门禁全部通过。

# Current State
- 任务：F6-6 双轨隔离回归已完成，准备在新会话开启 F7 报告页面框架。
- 当前 Vibe Coding 步骤：/vibe-feature（F7 报告页面框架 Mini-PRD 尚未创建）。
- 已完成：F6 评分计算框架与双轨隔离完整收口；`ABILITY_SCORE`、`JOB_SKILL_SCORE`、`OPERATION_PASS_RATE`、`TRAINING_COMPLETION` 四类结果通过 `RESULT_CALCULATED` / `result_record` 投影生成和读取，互不混算。
- 已完成：`results:getCurrent` 与 `results:listCurrentByStudent` 已接入主进程、preload 和共享类型；只读 current `result_record`，支持结果类型、来源类型、来源 ID 和学生过滤。
- 已完成：岗位技能阶段推进、线下评分入口和结果核心已收紧为只读取 `JOB_SPECIFIC` 题目快照，避免 BASE_ABILITY 噪声卡住或污染 JOB_SKILL 结果。
- 已完成：F6-6 新增四轨隔离、schema 混轨结构失败和 current 唯一约束回归。
- 已完成验证：F6-6 指定回归通过（5 files / 57 tests）；`npm test` 通过（85 files / 945 tests）；`npm run build` 通过；`npm run typecheck` 通过；`npm run lint` 通过（0 error，622 warnings，均为既有 Vue 格式 warning）；`npm run docs:index:check` 通过；`git diff --check` 通过。
- 产出物：本地提交已完成；主要文件包括 `src/main/domain/ability-scoring.ts`、`src/main/ipc/handlers/assessment.ts`、`src/main/ipc/handlers/results.ts`、`src/main/ipc/handlers/job-skill-result.ts`、`src/main/ipc/handlers/job-skill-scoring.ts`、`src/main/domain/assessment-reducer.ts`、相关测试和共享类型。

# Next Action
- 执行 F7 `/vibe-feature`：编写 `doc/features/report-page-framework-prd.md`。输入以 `doc/features/pilot-r0-foundation-development-plan-2026-07-22.md` 的 F7 段和 `doc/specs/MVP_PRD_v1.0.9-authoritative.md` §17 为准；先确认报告列表、详情、生成、锁定、导出、红线后普通报告阻断、未复核安置建议不可导出、版本/坐次展示和 `report_content_json` 直接渲染的验收标准。

# Milestone Queue
1. 当前：F7 报告页面框架。
2. 后续：Q1 BASE_ABILITY 42+8 正式施测补料。
3. 后续：Q2 JOB_SKILL R1-R6 与 270 项素材/Pilot 门禁。

# Blockers
- None（F7 Mini-PRD 可从现有 Pilot R0 计划和权威 PRD 推进）。
- 非阻塞背景：JOB_SKILL 真实激活仍被 270 项 planned 资产和 Pilot 独立门禁阻止；BASE_ABILITY 真实激活仍被 96 DRAFT / 96 NO_SCORE、renderer 与线下评分锚点缺失阻止；默认运行数据库存在内容包漂移，`db:verify` 需后续独立运维修复并先备份。

# Key Decisions
- 报告必须用 `report_content_json` 直接渲染，不引入 Markdown 渲染库。
- F7 不能激活真实题库、不能生成正式安置结论；未复核安置建议可查看但不可导出。
- 红线后普通报告必须被阻断，只能生成安全中止报告；报告快照、锁定、导出失败都必须可追溯。

# Relevant Files
- `doc/features/pilot-r0-foundation-development-plan-2026-07-22.md`
- `doc/specs/MVP_PRD_v1.0.9-authoritative.md`
- `doc/features/report-page-framework-prd.md`
- `doc/index.md`
