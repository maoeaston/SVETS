# Project Direction
- 项目总目标：先把 Pilot R0 做到可追溯、可验收，再继续修订并交付 Full Product 1.0；Pilot 不是项目终点。
- 必须保留的产品决策：`doc/specs/MVP_PRD_v1.0.9-authoritative.md` 是当前唯一产品合同；Q1 采用方案 A（先经独立批准激活选定 42+8，再做真实课堂试测，试测证据门禁正式解释/发布）；产品、学校和逐学生同意链已采用可信签发、有限期限、撤销/轮换与晚到失效合同；`doc/specs/FULL_PRODUCT_PRD_v2.0-draft.md` 仍是未批准草案。
- 不变边界：安全红线、历史可追溯、教师责任和多设备授权链不得降级；Pilot ACTIVE 不等于正式诊断、就业安置或对外发布；执行激活的 ADMIN 不能自授批准，AI 不能成为无人工确认的录用或淘汰最终裁决者。

# Current Milestone
- Pilot R0 基础底座 F2 至 F7 已完成；Q1 BASE_ABILITY 42+8 Mini-PRD 已完成 R3 独立审查，`P0-01 / P0-02 / P0-03` 全部关闭，下一阶段是生成原子、可回滚的实现计划并逐步实现。

# Current State
- 任务：补齐 Q1 `P0-03` 授权生命周期并重新执行独立 PRD 审查；当前 Schema 基线仍为 `schema.sql v0.1.16-report-framework`，运行时代码和数据库未修改。
- 当前 Vibe Coding 步骤：`/vibe-impl`。
- 已完成：权威 PRD、Q1 Mini-PRD 和当前合同已同步产品批准、学校授权、逐学生同意、24/4 小时离线索引租约、晚到失效永久隔离、稳定失效事实 ID、两个 reason 枚举的 expand-only 回滚边界。
- 已完成：最终独立 R3 `/vibe-review prd` 对冻结 SHA-256 `6a0671ae6a87f33fbfa25f502552277a46832b5f482e85142768e375d63bb231` 给出 `PASS`，P0/P1/P2 均为 0，P0-01/P0-02/P0-03 均未重开。
- 已完成验证：`npm run typecheck` 通过；`npm run lint` 通过（0 error，661 条既有 warning）；`npm test` 通过（104 files / 1026 tests）；`npm run contract:base-ability:gate:check` 通过且如实保持 `BLOCKED_DRAFT_REVIEW_RENDERER_MATERIAL_AND_TRIAL_GATE`；`npm run docs:index:check` 与 `git diff --check` 通过。
- 未执行：`npm run build`、Electron 技术演练、真实激活、人工审核和学校课堂试测均为 `NOT_RUN`；本轮只完成合同和 PRD 审查。
- 产出物：`doc/specs/MVP_PRD_v1.0.9-authoritative.md`、`doc/features/base-ability-42plus8-pilot-readiness-prd.md`、`doc/features/base-ability-current-contracts.md` 及本交接文件。

# Next Action
- 执行 `/vibe-impl`，把已审 Q1 Mini-PRD 转为原子实现计划；第一步必须登记两个 CHECK 枚举 migration、两个新增事件及全部读写/重放/回滚副作用，不开始编码。

# Milestone Queue
1. 当前：生成并独立审查 Q1 `/vibe-impl`，再逐步实现和 `/vibe-accept`。
2. 后续：Q2 JOB_SKILL R1-R6 与 270 项素材/Pilot 门禁。
3. 后续：Full Product 1.0 修订与 AI 自动评分/AI 岗位推荐方案裁决（`FULL_PRODUCT_PRD_v2.0-draft.md` 仍为草案）。

# Blockers
- 进入 `/vibe-impl`：None。
- 内容/测评、安全/康复、特教/无障碍审核人和学校试测方案尚未提供；不阻断实现计划和前置工具开发，但阻断相应人工门禁通过及业务 Pilot 完成。

# Key Decisions
- 产品批准、学校授权和逐学生同意均从不可变签发件与签名生命周期事件派生；最大期限和 24/4 小时索引租约机器强制，ADMIN 不能覆盖。
- 晚到失效按稳定 `invalidation_fact_id` 和最早 `invalidation_at` 追溯；运行区间重叠的已完成 session 永久隔离普通结果/报告/课堂证据，安全事实始终保留。
- Q1 只增量扩展 sitting/assignment 两个 reason CHECK；首次新值/事件写入后 Schema 为 expand-only，只能回滚到兼容应用版本。

# Relevant Files
- `doc/features/base-ability-42plus8-pilot-readiness-prd.md`
- `doc/specs/MVP_PRD_v1.0.9-authoritative.md`
- `src/main/db/schema.sql`
- `src/shared/types/event-payloads.ts`
- `src/shared/types/assignment.ts`
