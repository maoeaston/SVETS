# Current State
- 任务：训练（Training）功能 — 全部 8 步实现完成
- 已完成：Step 1–8，typecheck 通过，41 个单元/集成测试全绿，8 个 commits
- 最后 commit：2057be1 feat(training): 三个 Vue 视图 + 路由

# Next Action
- 运行 `/vibe-accept`（typecheck + build + vitest + 领域专项）
- 手工冒烟：教师创建训练 → 学生依序完成四步 → 检查 result_record
- 通过后运行 `waza check`（ship mode）推送到 remote

# Blockers
- None

# Key Decisions
- TRAINING_STARTED（代码）= TRAINING_SESSION_CREATED（PRD），统一用 TRAINING_STARTED
- INIT → ACTIVE：第一个 TRAINING_STEP_STARTED 时由 applyStepStarted 触发
- 安全红线触发链：schema trigger 批量 HALT → haltTrainingSessionSteps 级联 IN_PROGRESS 步骤归档 FAILED
- result_record 无 task_code 列（已修正，用 job_code + source_aggregate_id 关联）
- 每个测试用新 MemoryAdapter 实例（training_session 有 DELETE 触发器，不可复用 DB 清理）
- mock writeEvent 需按 aggregateId 递增 event_sequence（否则 UNIQUE 约束冲突）

# Relevant Files
- `doc/features/training-impl.md`（实现文档参考）
- `src/main/ipc/handlers/training.ts`（handler 全部实现）
- `src/main/domain/training-reducer.ts`（reducer 全部实现）
- `src/main/ipc/handlers/__tests__/training-*.test.ts`（4 个测试文件，41 个用例）
