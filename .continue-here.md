# Current State
- 任务：5.4 测评功能逐步实现，Step 1（共享类型补缺）已完成 + 验收通过
- 当前 Vibe Coding 步骤：Step 1 `/vibe-accept 1` 通过（commit 2618583），下一步实现 Step 2 → `/vibe-accept 2`
- 已完成：
  - `doc/features/assessment-prd.md`（5.4 Mini-PRD，9 节 + 25 验收 + 10 风险，两轮 Reviewer 修订全采纳）
  - `doc/features/assessment-impl.md`（10 步实现文档，Step 6 拆 6a/6b；含事件写入事务边界架构决策专节；Reviewer 2 阻塞 + 多项次要全解决）
  - Step 1：`src/shared/types/event-payloads.ts` +EventType `EMOTION_COLLAPSE_THRESHOLD_REACHED` + `CollapseRecord`/`EmotionCollapseThresholdReachedPayload`/`ModuleScore`/`AbilityScorePayload`（commit 2618583，+36 行纯新增）

# Next Action
- 实现 Step 2（assertStudent 身份校验 + test-helpers 扩展）：
  - `src/main/utils/auth-context.ts`：保留 `assertCaller`（TEACHER/ADMIN），新增 `assertStudent(db, callerUserId, callerRole)` + `assertSessionOwner(db, callerUserId, sessionId)`
  - `src/main/db/test-helpers.ts`：新增 `seedStudent(db, over?)` + `seedQuestionBank(db, over?)`（mock 题库，6 模块 × 各题型，供 Step 3 组卷单测用）
  - `src/main/utils/__tests__/auth-context.test.ts`：扩展 assertStudent / assertSessionOwner 用例
  - 详见 `doc/features/assessment-impl.md` Step 2（line 109-143）
- 验收：`/vibe-accept 2`
- commit message：`feat(auth): add assertStudent + assertSessionOwner for assessment paths`

# Blockers
- 5.3 题库未交付：组卷算法单测用 mock question_bank（Step 3），端到端验证推迟到 5.3

# Key Decisions
- **事件写入事务边界**：不改 `writeEvent` 签名。DB 部分（domain_event_projection INSERT + reducer）经 better-sqlite3 singleton connection 自动加入调用者事务；jsonl `appendFileSync` 不在事务内（fs 不可回滚），由 reducer 幂等性 + 冷启动重放兜底（符合事件溯源原则）。详见 assessment-impl.md「事件写入事务边界」专节
- **assessment-reducer 方案 A**：独立 `assessment-reducer.ts`，幂等（每 apply* 先 SELECT 判已应用），writeEvent 后调，可复用于冷启动回放
- **Step 6 拆为 6a/6b**：6a 纯通道声明（类型 + preload 白名单，3 文件），6b createSession handler + 错误码 seed + 注册（3 文件），满足单步 ≤5 文件约束
- **Step 1 EventType 放置**：`EMOTION_COLLAPSE_THRESHOLD_REACHED` 放在情绪组内（`EMOTION_RESUMED` 之后），非字面"追加末尾"——遵循现有 EventType 按业务语义分组的风格

# Relevant Files
- `doc/features/assessment-impl.md`（逐步实现的输入，Step 2-10 详述 + 依赖图 + 事务边界决策）
- `src/shared/types/event-payloads.ts`（Step 1 已完成，下游 reducer/event-writer 依赖）
- `src/main/utils/auth-context.ts`（Step 2 主改动文件）
- `src/main/db/test-helpers.ts`（Step 2 新增/扩展）
- `src/main/db/schema.sql`（v0.1.9 trigger 约束参考，5.4 无需 schema 变更）

# Repo Notes
- 本地 `main` ahead 4 未 push（ccffad7 docs + e22556a handoff + 388fbae docs sync + 2618583 step1），待用户决定推送时机
- `doc/通用基础能力评估题库2026.6.30.csv` 删除 + `.xlsx` 新增未处理（题库 csv→xlsx 替换，与 5.4 无关，待用户决定）
