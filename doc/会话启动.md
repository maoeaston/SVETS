# 会话启动：恢复现场（SVETS 项目专用）

当前阶段只允许读取文件和检查 Git 状态。
不允许修改文件、不允许运行构建或测试、不允许恢复 stash。

连续推进模式例外：若用户明确要求"自动推进"、"连续推进"或等价表述，完成启动摘要后不停止，直接按 `.continue-here.md` 的 `Next Action` 执行；每完成一个 Step 后更新 `.continue-here.md` 并继续下一个 Step，直到遇到 blocker、验证失败、需要产品/架构决策，或当前里程碑全部完成。

本次 298 题 V4 复审结果交接例外：若用户在启动消息中已经提供陈晓青与赫东的 V4 审核 JSON，并明确要求接收或按结果继续，输出启动摘要后直接读取 `doc/features/job-skill-shelver-298-review-revision-handoff.md` 并执行原件固化、Schema、集合和 hash 校验及合并，不需要用户再次回复“继续”。

---

<!-- HANDOFF:START -->
## 本次交接（自动生成，请勿手编）
- 项目总目标：先把 Pilot R0 做到可追溯、可验收，再继续修订并交付 Full Product 1.0；Pilot 不是项目终点。
- 当前里程碑：BASE_ABILITY Pilot 就绪链——Step 1 已验收 `PASS`，Step 2A/2B/2C 已验收前置（M4/M5A `ACCEPTED`、M5B-15 `PASS`），P1-01/P1-02 已关闭，第二轮独立 R3 复审 `PASS`。
- 当前任务：第二轮独立 R3 复审已完成（结论 `PASS`）。
- 下一步唯一原子动作：进入 BASE_ABILITY Step 3 评估——先读 `doc/features/base-ability-42plus8-pilot-readiness-impl.md` 的 Step 3 段（v1.2 策略 / 组卷合同 / SESSION_STARTED v2 快照链），确认前置就绪后起草 Step 3；未开始实现前不得修改 Schema、题库状态、默认运行库或 M5B 冻结工件。
- 后续路线：Step 3 → Step 4–10 → R0 Pilot 冻结 → Full Product 1.0。
- 验证命令：`npm run contract:base-ability:authority:check`（应得 hash `sha256:1ebd14451caffcf71a73c409831d068723d7436823f75ebc45910059e313875f`）。
- 现场约束：不读写默认数据库；production v2 runtime 已接通，legacy service 只可作无生产写入能力的 planning clone oracle；不清理、reset、restore、push、merge 或发布，除非用户再次明确授权。
- 交接说明：复审记录见 `doc/features/base-ability-42plus8-impl-r3-revalidation-review-2.md`（首轮 `CONDITIONAL_PASS` 保留为 `-review.md`）；Step 1 验收见 `doc/features/base-ability-42plus8-step1-revalidation-acceptance.md`。完整状态见 `.continue-here.md`。
<!-- HANDOFF:END -->

---

## 步骤一：读取上下文

先读上方 HANDOFF 区块，当前任务和下一步原子动作已在其中。

然后按序读取：
1. `AGENTS.md`：架构原则、禁止清单、Vibe Coding 开发流程
2. `.continue-here.md`：完整交接状态（HANDOFF 区块是其摘要）；必须同时恢复 `Project Direction` / `Current Milestone` / `Next Action` / `Milestone Queue`

如果 `.continue-here.md` 提到了额外必读文件，再补读；Goal 模式下还必须执行其中 `Goal-mode Handoff` 的顺序、停止条件和证据要求。
不要主动扩读大型历史文档（PRD / schema），除非明确要求。

启动时必须区分：
- `Next Action` 是眼前唯一原子任务，不是整个项目的终点。
- `Project Direction` 和 `Milestone Queue` 是跨会话持久方向，不得因当前任务较小而忽略或删除。
- 草案、审查快照和权威产品合同的状态必须如实表达，不得在交接中静默升格。

---

## 步骤二：检查 Git 现场

```bash
git stash list
git status --short --branch
git log --oneline -5
```

规则：
- 只列出 stash，不要自动 pop
- 当前只在功能分支上工作（feat/xxx），不在 main 上直接开发
- 看到 main 分支上有未提交改动，先停止并说明

---

## 步骤三：输出启动确认摘要

固定用以下格式，不要加额外说明：

```
项目总目标：
当前里程碑：
当前任务：
当前 Vibe Coding 步骤：/vibe-feature / /vibe-impl / 逐步实现第N步 / /vibe-accept
当前分支：
下一步唯一原子动作：
后续里程碑：
相关文件：
是否存在 stash：
最近提交：
Repo 状态：clean / dirty / stashed
```

**Vibe Coding 步骤说明（判断依据）：**
- 若 `doc/features/<feature>-prd.md` 不存在 → 应先运行 `/vibe-feature`
- 若 PRD 存在但 `doc/features/<feature>-impl.md` 不存在 → 应先运行 `/vibe-impl`
- 若实现文档存在 → 看 `.continue-here.md` 记录的当前步骤编号
- 若所有步骤完成但未验收 → 应运行 `/vibe-accept`

---

## 步骤四：遇到以下情况先停止并说明

- `.continue-here.md` 不存在（说明是全新会话，告知用户）
- `.continue-here.md` 与当前 Git 状态明显冲突
- HANDOFF 区块内容与 `.continue-here.md` 矛盾（以 `.continue-here.md` 为准，说明差异）
- 存在无法判断用途的 stash
- 工作区有与当前任务直接冲突的未提交改动
- 当前在 main 分支且有未提交改动

完成以上检查后：
- 默认停止，等待用户下一步指令。不要提前开始干活。
- 若用户已明确要求连续推进模式，则输出启动摘要后继续执行 `.continue-here.md` 的 `Next Action`，并按连续推进模式自动进入后续 Step。
