# 会话启动：恢复现场（SVETS 项目专用）

当前阶段只允许读取文件和检查 Git 状态。
不允许修改文件、不允许运行构建或测试、不允许恢复 stash。

连续推进模式例外：若用户明确要求"自动推进"、"连续推进"或等价表述，完成启动摘要后不停止，直接按 `.continue-here.md` 的 `Next Action` 执行；每完成一个 Step 后更新 `.continue-here.md` 并继续下一个 Step，直到遇到 blocker、验证失败、需要产品/架构决策，或当前里程碑全部完成。

本次298题V4复审结果交接例外：若用户在启动消息中已经提供陈晓青与赫东的V4审核JSON，并明确要求接收或按结果继续，输出启动摘要后直接读取 `doc/features/job-skill-shelver-298-review-revision-handoff.md` 并执行原件固化、Schema/集合/hash校验和合并，不需要用户再次回复“继续”。

---

<!-- HANDOFF:START -->
## 本次交接（自动生成，请勿手编）
- 项目总目标：先把 Pilot R0 做到可追溯、可验收，再继续修订并交付 Full Product 1.0；Pilot 不是项目终点。
- 当前里程碑：Pilot R0 基础底座的 F2 登录会话与权限、F3 数据保存与恢复、F4 暂停恢复与安全红线、F5 基础页面与异常中心均已完成；F6-1、F6-2、F6-3 已完成，准备进入 F6-4 基础能力结算核心。
- 当前任务：F6-3 已完成：基础能力线下评分提交/读取入口已接入 `assessment:submitOfflineAbilityScores` / `assessment:getOfflineAbilityScores`，事件显式写 `score_scope='OFFLINE_ABILITY'`，支持部分补交和事务回滚；本步未生成 `ABILITY_SCORE`。
- 下一步唯一原子动作：执行 F6-4：新增基础能力结算核心，计算 `ABILITY_SCORE` 只计 `BASE_ABILITY` 线上题和 `OFFLINE_ABILITY` 线下评分；`assessment:calculateResult` 支持 `OFFLINE_PENDING` 且线下基础能力题全量评分后的正常结算，写 `RESULT_CALCULATED` 后写 `SESSION_COMPLETED`，重复调用幂等返回当前结果；红线场景继续强制 `LEVEL_FAIL_BY_SAFETY` 且不混入 `JOB_SKILL` / `TASK_OPERATION` / `TEACHER_OBSERVATION`。
- 后续路线：F6-4 基础能力结算核心 → F6-5 统一结果读取 → F6-6 双轨隔离回归 → F7 报告页面框架 → Q1/Q2 补料与门禁。
- 验证命令：`./node_modules/.bin/vitest run src/main/ipc/handlers/__tests__/assessment-ability-scoring.test.ts`
<!-- HANDOFF:END -->

---

## 步骤一：读取上下文

先读上方 HANDOFF 区块——当前任务和下一步原子动作已在其中。

然后按序读取：
1. `AGENTS.md` — 架构原则、禁止清单、Vibe Coding 开发流程
2. `.continue-here.md` — 完整交接状态（HANDOFF 区块是其摘要）；必须同时恢复 `Project Direction` / `Current Milestone` / `Next Action` / `Milestone Queue`

如果 `.continue-here.md` 提到了额外必读文件，再补读。
不要主动扩读大型历史文档（PRD / schema），除非明确要求。

启动时必须区分：
- `Next Action` 是眼前唯一原子任务，不是整个项目的终点。
- `Project Direction` 和 `Milestone Queue` 是跨会话持久方向，不得因当前任务较小而忽略或删除。
- 草案、审查快照和权威产品合同的状态必须如实表达，不得在交接中静默升格。

---

## 步骤二：检查 Git 现场

```bash
git stash list
git status --short --branch
git log --oneline -5
```

规则：
- 只列出 stash，不要自动 pop
- 当前只在功能分支上工作（feat/xxx），不在 main 上直接开发
- 看到 main 分支上有未提交改动，先停止并说明

---

## 步骤三：输出启动确认摘要

固定用以下格式，不要加额外说明：

```
项目总目标：
当前里程碑：
当前任务：
当前 Vibe Coding 步骤：/vibe-feature / /vibe-impl / 逐步实现第N步 / /vibe-accept
当前分支：
下一步唯一原子动作：
后续里程碑：
相关文件：
是否存在 stash：
最近提交：
Repo 状态：clean / dirty / stashed
```

**Vibe Coding 步骤说明（判断依据）：**
- 若 `doc/features/<feature>-prd.md` 不存在 → 应先运行 `/vibe-feature`
- 若 PRD 存在但 `doc/features/<feature>-impl.md` 不存在 → 应先运行 `/vibe-impl`
- 若实现文档存在 → 看 `.continue-here.md` 记录的当前步骤编号
- 若所有步骤完成但未验收 → 应运行 `/vibe-accept`

---

## 步骤四：遇到以下情况先停止并说明

- `.continue-here.md` 不存在（说明是全新会话，告知用户）
- `.continue-here.md` 与当前 Git 状态明显冲突
- HANDOFF 区块内容与 `.continue-here.md` 矛盾（以 `.continue-here.md` 为准，说明差异）
- 存在无法判断用途的 stash
- 工作区有与当前任务直接冲突的未提交改动
- 当前在 main 分支且有未提交改动

完成以上检查后：
- 默认停止，等待用户下一步指令。不要提前开始干活。
- 若用户已明确要求连续推进模式，则输出启动摘要后继续执行 `.continue-here.md` 的 `Next Action`，并按连续推进模式自动进入后续 Step。
